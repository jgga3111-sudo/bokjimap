# 러닝온 색인·실적 실측 — 복지맵 설계 근거

조사일: 2026-08-31 · 출처: 러닝온 Google Search Console (본인 계정 실데이터)

이 문서는 추정이 아니라 **실측치**다. 복지맵의 페이지 전략은 여기서 나온다.

---

## 1. 색인 현황

| 구분 | 페이지 |
|---|---|
| 색인 생성됨 | **522** |
| 색인 안 됨 | **626** |
| 사이트맵 총 URL | 1,189 |

색인 안 된 626건의 사유:

| 사유 | 건수 |
|---|---|
| **발견됨 – 현재 색인이 생성되지 않음** | **598** |
| 사용자가 선택한 표준이 없는 중복 페이지 | 16 |
| 적절한 표준 태그가 포함된 대체 페이지 | 6 |
| 리디렉션이 포함된 페이지 | 3 |
| 크롤링됨 – 현재 색인이 생성되지 않음 | 3 |
| 찾을 수 없음(404) | 0 |

**미색인의 96%가 "발견됨 – 색인 안 됨"** 한 가지다. 구글이 URL은 알지만
크롤링할 가치가 낮다고 보고 미뤄둔 상태다.

### 그 598건이 무엇인가

3페이지(30건) 표본을 뽑았더니 **전부 `/courses/rc-*`(러닝 코스)** 였다.

```
/courses/rc-123  /courses/rc-124  /courses/rc-13   /courses/rc-132
/courses/rc-134  /courses/rc-140  /courses/rc-148  /courses/rc-15
/courses/rc-150  /courses/rc-151  /courses/rc-155  /courses/rc-16
... (30건 전부 /courses)
```

그래프상 이 수치는 7월 말 ~200에서 8월 중순 600으로 급증했다.
**코스를 늘릴수록 미색인이 늘었다.**

---

## 2. 실적 (최근 90일, 페이지 유형별)

총 1,486 클릭 / 531 페이지.

| 유형 | 페이지 | 클릭 | 비중 | 노출 | 페이지당 클릭 |
|---|---|---|---|---|---|
| **`/races` 개별 대회** | 207 | **1,288** | **86.7%** | 35,146 | **6.22** |
| `/courses` 러닝 코스 | 155 | 107 | 7.2% | 1,698 | 0.69 |
| `/shoes` 러닝화 | 87 | 31 | 2.1% | 1,272 | 0.36 |
| `/marathon` 조합 랜딩 | 34 | 23 | 1.5% | 1,268 | 0.68 |
| (홈) | 1 | 17 | 1.1% | 252 | 17 |
| `/guide` 직접 쓴 글 | 25 | 7 | 0.5% | 181 | 0.28 |
| 나머지 전부 | ~20 | 13 | 0.9% | ~200 | ~0.5 |

---

## 3. 읽어낸 것

### ① 이긴 건 "URL 대량 생성"이 아니라 "개별 개체 페이지"였다

`/races` 207개가 클릭의 86.7%를 가져온다. 페이지당 6.22클릭으로 다른 유형의
**9~22배**다. 반면 사이트맵의 56%를 차지하는 `/courses` 665개는 클릭 7.2%에
그치고, 그중 155개만 트래픽이 있다. **나머지 510개는 색인조차 안 됐다.**

차이는 **고유명으로 검색되는가**이다.
- 대회는 이름으로 검색된다 — "2026 브레이브 쿠키런", "진주마라톤"
- 코스는 이름으로 검색되지 않는다 — 일반명사 조합이라 검색어가 안 생긴다

### ② 축 조합 랜딩의 실적은 생각보다 낮다

`/marathon/*`(지역×시기×종목) 34개, 23클릭, 페이지당 0.68. 러닝온이 의도적으로
편 전략인데 실적은 미미하다. **기대치를 낮게 잡아야 한다.**

### ③ 직접 쓴 글은 거의 안 먹힌다

`/guide` 25개, 7클릭. 페이지당 0.28로 전 유형 최하위. 콘텐츠를 쓰는 것보다
개체를 모으는 게 압도적으로 효율이 높다.

---

## 4. 복지맵에 적용

### 대응 관계

| 러닝온 | 복지맵 | 기대 |
|---|---|---|
| `/races` 개별 대회 (86.7%) | **`/service/[id]` 개별 복지서비스 5,219건** | **주력** |
| `/marathon` 조합 랜딩 (1.5%) | `/region` `/target` `/life` 허브 | 보조 |
| `/courses` 얇은 개체 (미색인 다수) | 본문 없는 서비스 | **만들지 말 것** |
| `/guide` 직접 글 (0.5%) | 가이드 | 후순위 |

복지 서비스는 러닝온의 대회처럼 **고유명**을 가진다 — "청년월세 특별지원",
"발달장애인 직업훈련연계주택 운영 지원". 검색어가 생기는 유형이다.

### 결정 사항

1. **`/service/[id]`에 집중한다.** 허브·조합 페이지는 내부 링크 통로로만 쓰고
   트래픽을 기대하지 않는다.
2. **본문 없는 서비스는 만들지 않는다.** 목록 API의 요약문은 중앙값 59자로,
   그대로 쓰면 `/courses`의 재현이 된다. **상세 API로 본문(지원대상·선정기준·
   지원내용·신청방법)을 채우는 것이 색인의 선결 조건이다.**
3. **5,219개를 한꺼번에 올리지 않는다.** 본문이 충실한 것부터 단계적으로 올려
   색인률을 보고 다음 배치를 정한다. 한 번에 다 올렸다가 "발견됨 – 색인 안 됨"에
   무더기로 빠지면 회복이 느리다.
4. `isIndexable` 임계값은 실측으로 조정한다. 현재 200자는 근거 없이 정한 값이다.

---

## 5. 부수 확인

- 러닝온 사이트맵 최종 업데이트: 26.8.21.
- 색인 522 vs 사이트맵 1,189 → **색인률 44%**
- 복지맵도 5,219개를 올리면 절반 이상이 미색인이 될 가능성이 높다.
  본문 품질로 이 비율을 올리는 게 핵심 과제다.
