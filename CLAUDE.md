@AGENTS.md

# CLAUDE.md — 복지클릭(bokjiclick) 프로젝트 가이드

Claude Code가 이 프로젝트에서 작업할 때 세션 시작 시 자동으로 읽는 지침이다.
Next.js 16 학습데이터 불일치 경고는 `AGENTS.md`(위 import) 참조.

---

## 0. 러닝온과 절대 혼동하지 말 것 (최우선)

이 컴퓨터에는 **같은 사람이 만든 Next.js 사이트가 두 개** 있다. 스택이 거의
같아서 경로·설정·계정을 헷갈리기 쉽다. 아래 표를 먼저 확인하고 작업한다.

| 항목 | 러닝온 (건드리지 말 것) | 복지클릭 (이 프로젝트) |
|------|------------------------|---------------------|
| 폴더 | `C:\Users\jgga_\클로드코드\marathon-hub` | `C:\Users\jgga_\클로드코드\welfare-hub` |
| 도메인 | `runningon.co.kr` | `bokjiclick.co.kr` |
| GitHub | `jgga3111-sudo/runningon` | `jgga3111-sudo/bokjimap` |
| dev 포트 | 3000 | **3001** |
| 주제 | 마라톤·러닝 대회 | 복지·지원금 정보 |
| 계정 | — | 깃·버셀 `jgga3111`, 서치콘솔·빙·사이트 연락처 `jgga1234567` |
| 애드센스 | 승인됨 (`runningon93`) | **같은 계정에 사이트로 추가** — 1인 1계정이라 새로 못 만든다 |
| 인스타 | `@runningon_office` | 아직 없음 |
| Supabase | 사용 중 | **사용 안 함** |
| 쿠팡 파트너스 | 사용 중(러닝화) | 아직 없음 |

### 계정 셋은 **역할로** 기억한다 (2026-09-09 사용자 확정)

이름이 셋 다 비슷해서(`jgga3111` · `jgga1234567` · `runningon93`) 한 줄로
외우면 반드시 헷갈린다. **09-09에 실제로 애드센스 계정을 뒤바꿔 적었다가
되돌렸다.** 이름이 아니라 **하는 일**로 붙여 둔다.

| 하는 일 | 계정 | 어디서 쓰나 |
|---|---|---|
| **코드를 올린다** | `jgga3111@gmail.com` | 깃 커밋 작성자 · GitHub · Vercel |
| **검색엔진에 말한다** | `jgga1234567@gmail.com` | 구글 서치콘솔 · 빙 웹마스터 · 사이트 연락처 |
| **광고비를 받는다** | `runningon93@gmail.com` | 애드센스 `pub-3333691556845206` |

- **애드센스만 러닝온과 같은 계정이다.** 1인 1계정이라 새로 못 만들어서,
  러닝온으로 이미 승인받은 그 계정에 복지클릭을 **사이트로 추가**했다.
  나머지 둘은 복지클릭 전용이다.
- 구글 화면은 주소에 **`?authuser=<이메일>`**을 붙인다. 크롬 기본 계정이
  `jgga3111`이라 그냥 열면 서치콘솔에서 "이 속성에 액세스할 수 없습니다"가
  뜬다. **계정 번호(`/u/N/`)는 로그인 순서 따라 바뀌니 외우지 않는다.**
- ⚠ **이 표를 말 한마디로 고치지 않는다.** 사람도 헷갈리는 자리다.
  기록과 다른 말을 들으면 **먼저 되물어 확인하고** 고친다.

**금지 사항**
- 러닝온 폴더의 파일을 이 프로젝트로 **복사하지 않는다.** 참고만 하고 새로 쓴다.
  (러닝온 CLAUDE.md에는 인스타 계정·`PROMO_SECRET`·쿠팡 API 한도가 박혀 있어
  그대로 옮기면 두 사이트 설정이 섞인다.)
- 이 프로젝트에서 러닝온 저장소에 커밋·푸시하지 않는다. 반대도 마찬가지.
- 러닝온 데이터(`races.json`, `courses.ts`, `shoes.ts`)를 참조하지 않는다.

**러닝온에서 가져와도 되는 것**: 설계 패턴과 교훈뿐이다(4절 참조). 코드는
읽고 이해한 뒤 이 프로젝트에 맞게 새로 작성한다.

---

## 1. 프로젝트 개요

- **이름:** 복지클릭 (bokjiclick) — 전국 복지·지원금 정보 사이트
- **주소:** https://bokjiclick.co.kr (2026-09-01 연결 완료)

  ⚠ 처음 이름은 **복지맵**이었다. 배포 직후 `bokjimap.com`이 같은 주제로
  이미 운영 중(2026-02 개설, 애드센스 게재)인 것을 발견해 이름을 바꿨다.
  가비아 **도메인 이름 변경**으로 추가 비용 없이 옮겼다(등록 후 7일 이내만
  가능한 기능이라 운이 좋았다). 교훈은 docs/03-경쟁.md 6절에 적어 두었다.
  코드·문서에 `bokjimap`이 남아 있으면 잔재이니 고친다.
- **기술 스택:** Next.js 16.3.3 (App Router, Turbopack) · React 19.2.8 ·
  TypeScript · Tailwind CSS 4
- **저장소:** GitHub `jgga3111-sudo/bokjimap` (main 브랜치 — 저장소 이름은
  도메인을 바꾼 뒤에도 그대로 두었다. 리네임하면 Vercel 연결이 끊긴다)
- **호스팅:** Vercel (프로젝트 `bokjimap`)

### 명령어

| 용도 | 명령 |
|------|------|
| 개발 서버 | `npm run dev` (포트 **3001**) |
| 빌드 | `npm run build` |
| 린트 | `npx eslint src --max-warnings=0` |
| 타입 검사 | `npx tsc --noEmit` |

---

## 2. 로컬 환경

- **OS:** Windows 11 / **셸:** PowerShell(주) + Bash(POSIX 스크립트용)
- **작업 디렉토리:** `C:\Users\jgga_\클로드코드\welfare-hub`
- **경로 주의:** 폴더명 `클로드코드`는 한글이다. 직접 타이핑하지 말고 복사해서 쓸 것.
- **Node가 PATH에 없다.** `C:\Program Files\nodejs`에 설치돼 있으나 세션 PATH에는
  안 잡힌다. Bash에서 `npx: command not found`가 나면 PowerShell로 아래처럼 실행한다.
  ```powershell
  $env:Path = "C:\Program Files\nodejs;" + $env:Path
  ```
- **git 사용자는 저장소 로컬 설정으로 고정**되어 있다(`fccls <jgga3111@gmail.com>`).
  전역 설정은 비어 있으니 건드리지 않는다. 다른 이메일로 커밋하면 Vercel이
  Blocked 처리한 전례가 러닝온에 있다.

---

## 3. 데이터 원칙 (이 프로젝트의 핵심)

지원금 정보는 사람의 신청·금전에 직접 영향을 준다. 정확성이 최우선이다.
이 영역은 AI 양산 블로그가 틀린 정보를 도배하는 곳이라, **정확성 자체가 이
사이트의 경쟁력**이다.

- **확인 못 하면 null.** 추측하지 않는다. 빈칸이 부실해 보여도, 틀린 값으로
  채워 사람을 헛걸음시키거나 신청 기회를 놓치게 하는 것보다 낫다.
- **작년 공고를 올해로 옮기지 않는다.** 지원금은 매년 금액·요건이 바뀐다.
  기준연도를 반드시 대조한다.
- **여러 출처가 엇갈리면 공식(지자체 공고·복지로)을 따른다.** 어떻게 엇갈렸는지 기록한다.
- **금액·소득기준·연령제한은 원문 표현을 그대로 옮긴다.** 임의로 반올림하거나
  요약하지 않는다.
- **`verifiedAt`(정보 확인일)을 항목마다 남긴다.**
- **마감일이 지난 공고는 숨기지 말고 "마감"으로 표기**한다. 내년 재공고를 찾는
  검색 수요가 있다.
- 법령 해석이나 수급 자격 판정을 하지 않는다. 안내와 링크까지가 이 사이트의 역할이다.

---

## 4. 러닝온에서 승계할 설계 패턴

러닝온 사이트맵 실측(1,189 URL) 분석에서 나온 것들이다. **코드 복사가 아니라
패턴만 가져온다.**

- **수집형 구조.** 러닝온은 1,189 URL 중 95%가 "실존 개체 1개 = URL 1개"였다.
  글을 쓰는 게 아니라 개체를 모아 페이지를 편다.
- **축 조합으로 URL 증폭 + 하한선.** 러닝온은 지역×시기×종목 조합 랜딩을 만들되
  `MIN_RACES = 3` 미만 조합은 만들지 않았다. 복지맵도 `MIN_SERVICES` 하한을 둔다.
  얇은 페이지 양산은 저품질 신호다.
- **sitemap의 lastmod를 나눠 쓴다.** 전 URL이 같은 날짜로 나가면 구글이 통째로
  무시한다. 매일 바뀌는 화면만 오늘 날짜, 나머지는 실제 수정일.
- **네이버 사이트맵 파서는 날짜만(YYYY-MM-DD) 읽는다.** ISO 밀리초를 넣으면 500 에러.
- **얇은 페이지는 스스로 noindex하거나 지운다.** 러닝온은 본문 160자짜리 도구
  페이지를 noindex 처리했고, 얇은 제휴 페이지 241개는 아예 삭제했다.
  사유: *"애드센스는 색인 여부와 무관하게 사이트 전체를 검토한다."*

---

## 5. 데이터 소스

| 소스 | 링크 | 상태 |
|---|---|---|
| 중앙부처복지서비스 API | data.go.kr/data/15090532 | 자동승인 |
| 지자체복지서비스 API | data.go.kr/data/15108347 | 자동승인 |
| 복지서비스정보 (파일) | data.go.kr/data/15083323 | 367행, 연 1회 갱신 |

- 이용허락범위 **제한 없음** = 상업적 이용 가능(애드센스 게재 OK)
- 개발계정 **1,000건/일** 한도. 정적 빌드로 소화한다.
- **미측정**: 지자체복지서비스 실제 건수·필드 목록. 인증키 발급 후 첫 작업.
  이 수치가 나오기 전에는 URL 총량을 확정하지 않는다.

### 중앙과 지자체는 주는 필드가 다르다 (헛다리 방지)

빈칸을 세다 보면 **큰 구멍처럼 보이는데 구멍이 아닌 것**이 있다. 09-07에
"신청 방법이 비어 있는 사업이 전체 조회수의 90%"라는 수치를 보고 결함인 줄
알았다. 아니었다 — **중앙부처 330건 전부**이고, 중앙 API는 그 필드를 아예
안 보낸다. 대신 `applmetList`(신청 절차)를 보내고 그쪽은 330건 전부 차 있다.

| 필드 | 중앙 330 | 지자체 570 |
|---|---|---|
| `applyMethod`·`applyMethods` (신청 방법) | **전부 없음** | 대부분 있음 |
| `applySteps`·`outline`·`baseYear` | 전부 있음 | **전부 없음** |
| `updatedAt` (원문 최종수정일) | **전부 없음** | 있음 |

빈칸을 셀 때는 **먼저 provider로 갈라 본다.** 한쪽이 100%면 결함이 아니라
구조다. `guide/apply/page.tsx` 머리말에 같은 함정이 이미 적혀 있었다.

### 법령 원문을 받는 법 (2026-09-07 확인)

지급일·요율처럼 **공공데이터에 없는 값은 조문에 있다.** 그런데 국가법령정보센터
`law.go.kr/법령/<이름>`은 **프레임 껍데기**라 WebFetch로 열면 제목만 오고 조문이
안 온다(09-07에 두 번 헛다리를 짚었다). 화면 링크로는 그 주소가 맞으니 **읽는
주소와 거는 주소를 구분한다.**

전문은 공개 API로 받는다. 인증키 없이 `OC=test`로 열린다.

```bash
curl -s -A "<브라우저 UA>" \
  "https://www.law.go.kr/DRF/lawService.do?OC=test&target=law&type=JSON&LM=$(urlencode '고용보험법')"
```

- `법령.기본정보.시행일자`가 **지금 시행 중인 판**이다. 이걸 같이 적어 둔다.
- `법령.조문.조문단위[].조문내용 / 항[].항내용 / 호[].호내용`에 본문이 있다.
- `법령.별표.별표단위[].별표내용`은 **괘선 문자로 그린 표**다. 고용보험법 별표1
  (소정급여일수)이 여기 있다. 눈으로 옮겨 적는다.
- **여러 법을 한꺼번에 받아 정규식으로 훑는 쪽이 훨씬 촘촘하다.** `매월\s*\d{1,2}일`
  하나로 09-07에 다섯 건을 새로 찾았다(장애수당·장애아동수당·보훈 셋).
  한 조씩 눈으로 찾던 09-06보다 나았다.
- **준용 규정을 놓치지 말 것.** 독립유공자 보훈급여금 지급일은 그 법 시행령에
  없고, 제9조제3항이 국가유공자법 시행령 제29조를 **준용**한다고만 적혀 있다.
  "조문 없음"으로 판정하기 전에 준용을 찾아본다.
- 조문이 있어도 **수록 사업이 없으면 싣지 않는다**(5·18 학습보조비 4/15·10/15).

---

## 6. 배포

**Vercel Pro 사용 (2026-08-31 사용자 확정).**

사유: Vercel Fair Use Guidelines(2026-07-29)상 **Hobby 플랜은 비상업적 개인
용도 한정**이고, 상업적 사용 예시에 *"The inclusion of advertisements, including
but not limited to online advertising platforms like Google AdSense"*가 명시돼
있다. 애드센스를 달 사이트는 Hobby로 운영할 수 없다.

Pro는 **팀 단위 과금($20/월)**이라 같은 팀에 있는 러닝온·복지맵이 함께 커버된다.
프로젝트마다 따로 결제하지 않는다.

### 배포 규칙
- 배포는 `git push origin main`으로만 한다. Vercel CLI로 직접 배포하지 않는다.
- **커밋 작성자는 반드시 `fccls <jgga3111@gmail.com>`.** 다른 이메일로 커밋하면
  Vercel이 Blocked 처리해 배포가 멈춘다(러닝온에서 세 커밋이 막힌 전례).
  이 저장소는 `git config --local`로 고정해 두었다. 전역 설정은 건드리지 않는다.
- push 후 "배포 완료"라고 말하기 전에 **실제 사이트에서 반영 여부를 확인**한다.
  커스텀 도메인 연결 후에는 `curl -sL`로 커스텀 도메인을 대상으로 확인한다
  (리다이렉트를 안 따라가면 껍데기만 보고 오판한다).

### 완료 (2026-09-01)
- [x] Vercel 프로젝트 생성 및 GitHub 연결
- [x] `bokjiclick.co.kr` 연결 — apex가 Production, `www`는 apex로 308
- [x] 구글 서치콘솔·빙 웹마스터 등록 및 사이트맵 제출

### 남은 일
- [x] **수집 완료 (2026-09-05) — 상세 5,219건 전량**(중앙 461 · 지자체 4,758).
      마지막 976건(중앙 61 / 지자체 915)을 실패 0으로 받았다.
      · **수록은 900건 그대로다.** 마지막 976건은 전부 조회수 3,738위 아래라
        900 컷 바깥이고, `build-data.mjs 900`을 다시 돌려도 바뀌는 것은
        생성 날짜 두 줄뿐이었다. 그 두 줄을 커밋하면 **사이트맵 lastmod가
        900개 페이지에서 오늘로 밀린다** — 내용은 그대로인데 전부 고친 것처럼
        보이는 거짓 신호라 되돌렸다. 4절의 "lastmod를 나눠 쓴다"가 여기 걸린다.
      · 수록을 900에서 올릴지는 **서치콘솔 색인 보고서를 보고** 정한다.
        그때 필요한 원문은 이제 전부 로컬에 있다(`data-research/detail-raw/`).
      · **09-06에 확대의 값어치를 재 봤다. 낮다.** 조회수 집중도가 극단적이다 —
        상위 10건이 전체 조회수의 52.4%, 상위 50건 79.8%, **상위 100건 86.1%**,
        상위 300건 93.6%. 900을 1,500으로 올려도 새로 담기는 것은 3,738위
        아래뿐이라 트래픽 기여가 거의 없고 얇은 페이지만 600개 는다.
        **늘리는 것보다 상위 100건을 깊게 파는 쪽이 같은 노력으로 훨씬 크다.**
      · 수집이 늘면 **글의 숫자가 조용히 바뀐다.** 돌린 뒤 `/guide` 열두 편의
        수치가 본문·metadata 양쪽에서 계산값인지 확인한다(2026-09-02에 실제로
        metadata만 상수로 남아 191건이라고 나가고 있었다).
- [ ] 수집 완료 후 `MIN_BODY_LENGTH` 기준선 재확인 (types/welfare.ts 주석 참고)
      · noindex 303 → 215 → 187 → **193건**(색인대상 707건 / 사이트맵 803개,
        09-06 실측). 목록에 링크된 채 크롤링만 당하고 버려지는 몫이다. 링크를
        끊지 말고 본문을 채운다 — 수집이 늘 때마다 저절로 넘어왔다(88건, 28건).
        09-06에 6건 **늘어난** 것은 데이터가 얇아져서가 아니라 상투 문구를
        본문 길이에서 뺐기 때문이다(바로 아래 항목).
      · **09-07에 구글이 "'NOINDEX' 태그에 의해 제외되었습니다"를 메일로 보냈다.
        고칠 것 없다 — 우리가 일부러 한 것이다.** 걸린 46건을 전수 대조했다.

        | | 건수 | 판정 |
        |---|---|---|
        | 지금도 noindex · 사이트맵에도 없음 | 38 | 의도대로 ✓ |
        | 지금은 noindex 아님 · 사이트맵에 있음 | 8 | 이미 고쳐졌고 구글이 안 다시 긁었을 뿐 |

        뒤의 8건(장애입양아동 의료비지원·산재근로자 보험급여 등)은 수집이
        늘면서 본문이 300자를 넘어 색인 대상으로 넘어온 것들이다. **구글
        보고서는 마지막 크롤링 시점의 상태**라 옛 판정이 남아 있다.
        조회수 669~888위라 수동 요청을 쓸 자리가 아니고, 사이트맵으로
        다시 긁히면 저절로 풀린다.
        · **이 메일이 다시 오면 46건이 아니라 "사이트맵에 있으면서 noindex인
          건"이 몇인지를 센다.** 그 수가 0이 아니면 그때가 진짜 결함이다
          (`audit-indexability.mjs`가 보는 것과 같은 조건).
- [x] **상투 문구 선정기준 149건 정리 (2026-09-06).** 원본 API가 선정기준 자리에
      "지원대상의 내용을 참고해주시기 바랍니다."만 보내는 사업이다. 그 149건이
      가져가는 복지로 조회수가 **전체의 30.8%** — 가장 많이 여는 페이지의 한 칸이
      아무 말도 안 하고 있었다. 절과 목차 칩을 빼고 지원 대상 밑 한 줄로 바꿨다.
      "선정 기준을 두지 않는 사업"이라고는 쓰지 않는다. 그건 제도 해석이다.
      · 판정은 **40자 이하**로 제한한다. 한부모가족처럼 그 문장 뒤에 실제 내용이
        이어지는 108자짜리가 있는데, 그건 지우면 안 되는 본문이다.
      · **규칙이 두 군데 있다** — `types/welfare.ts`의 `isCriteriaBoilerplate`와
        `scripts/audit-sitemap.mjs`의 사본. 한쪽만 고치면 소스는 713이라 하고
        빌드 결과는 707이라 하는 상태가 된다(09-06에 실제로 그랬다).
- [x] **지난 신청 기간에 띠 달기 9건 (2026-09-06)** — `src/lib/applyPeriod.ts`.
      조회수 1위 청년내일저축계좌 요약문이 "(2026년 모집기간: '26.5.4.~'26.5.20.)"
      으로 끝나는데 9월에도 끝났다는 말이 없었다. `applyEnd`는 570건 중 554건이
      `9999-12-31`이라 **날짜가 필드가 아니라 문장 안에** 있다.
      · **기준일을 마감일로 읽지 않는 것이 핵심이다.** 범위 표현이어야 하고, 그
        앞 40자에 신청·모집·접수·공모·응모 + 기간/기한이 붙어 있어야 한다.
        이 조건이 없으면 대상포진의 "2026. 1. 1. 이후 출생"과 희망두배의
        "기준기간: 2025.6.1.~2026.5.31."이 걸려 들어온다.
      · 지났는지는 **브라우저가 판정한다.** 빌드에서 정하면 빌드한 날이 정적
        HTML에 굳어, 배포를 안 하는 동안 조용히 틀려 간다(`ThisMonth`와 같은 함정).
- [x] **"지급일"이 통째로 빠져 있었다 (2026-09-06).** 데이터랩에서 "근로장려금
      지급일" **한 낱말**이 "청년월세·청년월세지원·청년월세 신청" 셋을 합친 것의
      3배였는데(26년 8월 51.1 대 11.8), `grep 지급일 src`은 0건이었다.
      · 복지로 기초연금 상세를 열어 봤더니 소득인정액 계산식까지 빼곡한데
        **지급일 칸이 없다.** 조회수 상위 120건 본문에 "매월 N일"이 적힌 것도
        2건뿐이다. 지급일은 시행령·시행규칙에 있었다(`src/lib/payDates.ts`).
      · **데이터에 없는 것을 1차 출처에서 찾아 메우는 것** — 이게 복지로 원문을
        그대로 옮기는 것과 우리가 갈라지는 자리다(4절 docs/03 §4와 같은 결론).
      · 부모급여는 조회수 11위인데도 뺐다. 아동수당법에도 영유아보육법에도
        지급일 조문이 없다. 확인 못 한 것은 싣지 않는다(3절).
      · **09-07에 6건 → 11건으로 늘렸다.** 법령을 통째로 받아 `매월 N일`을
        기계로 훑었다(5절 「법령 원문을 받는 법」). **매월 15일이라는 무리가
        통째로 빠져 있었다** — 보훈 보상금·생활조정수당·참전명예수당.
        20일에는 장애수당(조회수 259,070)과 장애아동수당이 더 붙었다.
      · **글 제목에 숫자를 세우면 데이터가 늘 때 거기가 조용히 틀려진다.**
        "지원금은 20일과 25일에 들어옵니다"가 15일이 생기는 순간 틀린 말이
        됐다. 본문에도 "전부 같은 괄호를 달고 있습니다 — '…그 전날로 한다'"가
        있었는데 보훈 조문은 "…그 전날에 지급한다"로 문장이 다르다.
        **늘릴 때는 제목과 '전부/모두'라고 쓴 문장을 같이 본다.**
- [x] **통칭으로 검색하면 우리 페이지가 안 잡혔다 (2026-09-07)** —
      `src/lib/aliases.ts`. 조회수 294,386짜리 「통합문화이용권」을 사람들은
      **"문화누리카드"**로 찾는다. 데이터랩 월평균 45.6 대 0.14로 **320배**.
      본문엔 열 번 나오는데 제목·설명·h1에는 없었고, 헤더 색인이 이름만 보므로
      검색창에서는 **0건**이었다. 정작 `lib/calendar.ts`는 처음부터
      "문화누리카드(통합문화이용권)"로 적고 있었다.
      · **넣는 기준 둘.** ㉠ 원문이 함께 적어 놓은 이름일 것(원문에
        "통합문화이용권(문화누리카드)을 발급하여"가 그대로 있다) ㉡ 통칭
        검색량이 공식명을 넘을 것. 평생교육이용권은 공식명 13.8 > 통칭 7.9라
        넣지 않았다. **900건을 훑어 두 조건에 걸린 건 이 하나뿐이다.**
        목록이 길어지면 기준이 느슨해진 것이니 이 두 줄을 다시 읽는다.
      · 검색 두 갈래(`search.ts`·`searchFull.ts`)의 규칙을 **같이** 고쳐야
        한다. 한쪽만 고치면 드롭다운과 결과 페이지가 다른 답을 낸다.
      · **이 페이지는 내용이 바뀌었는데 사이트맵 lastmod가 안 움직인다.**
        상세는 전부 `SERVICES_UPDATED`를 쓰기 때문이다(800개를 오늘로 미는
        것보다 낫다). 대신 **다음 색인 요청 때 이 URL을 맨 앞에 넣는다.**
- [x] **별칭 기준을 한 번 넓혔다 — K-패스 (2026-09-07)** — `src/lib/aliases.ts`.
      조회수 58위(168,033) 국토교통부 사업의 제목이 「대중교통비 환급
      지원(모두의카드)」인데, 사람들이 치는 말은 **K패스**다. 데이터랩
      12개월 월평균 **K패스 12.7 대 모두의카드 3.7 — 3.5배**이고, 실업급여를
      1로 놓으면 K패스 2.4·문화누리카드 1.3이다(둘 다 글을 새로 쓸 만하다고
      판단했던 검색량보다 크다).
      · **문화누리카드보다 나빴다.** 그때는 0건이었는데 여기는 **틀린 1건**이
        나왔다 — "K패스"로 치면 강남구 교통비(203위), "K-패스"로 치면
        광주G-패스가 나오고 정작 본체는 안 나왔다. 본문에 그 낱말이 있는
        곁가지 지자체 사업이다. 0건이면 "여긴 없구나" 하고 다시 찾지만,
        틀린 1건은 "이 사이트엔 강남구 것만 있네" 하고 떠난다.
      · **기준 ㉠을 넓혔다.** 원래 "복지로 원문이 함께 적어 놓은 이름"이었는데
        복지로 본문에는 "K패스"가 한 글자도 없다. 병기하는 것은 소관부처다 —
        국토부 대광위에 「K-패스 사업」·「모두의카드」 페이지가 나란히 있고,
        정책브리핑은 *"그렇게 나온 것이 K-패스 '모두의 카드'다"*라고 쓴다.
        그래서 **"원문 또는 소관부처의 공식 표기"**로 읽는다. 넓힌 만큼
        근거는 **1차 출처여야 한다** — 블로그·위키는 근거가 못 된다.
      · **붙임표를 `norm`에서 지웠다**(`searchText.ts`). 공식은 「K-패스」,
        사람들은 「K패스」다. 붙임표 하나로 다른 낱말이 되면 공식 표기를
        그대로 실은 쪽이 오히려 안 잡힌다. 표기를 둘 적으면 제목이
        "(모두의카드·K-패스·K패스)"가 되어 읽기만 나빠진다.
      · 이름이 이미 괄호로 끝나면 **그 괄호 안에 잇는다**. 안 그러면
        "…(모두의카드)(K-패스)"가 된다.
      · 화면 이동경로도 별칭 이름으로 바꿨다. JSON-LD BreadcrumbList가 이미
        별칭을 내보내고 있어서 **구조화 데이터와 화면이 어긋나 있었다.**
      · **탈락시킨 후보를 같이 적어 둔다**(다시 후보로 올라오지 않게).
        청년미래적금 ✗ — 옛 이름 청년도약계좌(8.7)보다 **새 이름(24.7)이
        이미 2.8배** 더 검색된다. 노란우산공제 0.49 ✗ · 본인부담상한제
        0.18 ✗ · 늘봄학교 0.05 ✗ · 전세사기 지원 0.003 ✗ (실업급여=1 기준).
- [x] **별칭에 절대량 하한을 더했다 — 기저귀바우처 (2026-09-08)** —
      `src/lib/aliases.ts` 기준 ㉢. 검색량 큰 낱말 **43개를 라이브
      `/search?q=`에 실제로 쳐 보고** 0건인 것을 골랐다(이름을 눈으로
      훑지 않는다 — 다음에도 이 방법을 쓴다). 0건은 넷이었다.
      · **기저귀바우처만 넣었다.** 조회수 20위(1,026,518)인데 0건이었다.
        원문에 "기저귀 바우처 지원대상은 다음과 같습니다"가 그대로 있어
        **좁은 기준 ㉠도 통과**한다(K-패스처럼 넓힐 필요가 없었다).
        공식명("기저귀 조제분유" 0.003)과 자릿수가 셋 다르다.
      · **㉡에 구멍이 있었다.** 비율만 보니 둘 다 거의 0이어도 통과한다.
        「통신비감면」이 공식명보다 24배 많이 검색돼 ㉡을 통과하는데
        절대량은 실업급여의 **0.007배**다. 그래서 **㉢ 통칭 검색량이
        실업급여의 10분의 1 이상**을 더했다. 실업급여는 글을 한 편 새로
        쓸 값어치가 있다고 판단한 선이고, 별칭은 글보다 훨씬 싸다.
        실측: 문화누리카드 1.30 · K패스 2.43 · **기저귀바우처 0.113** ←
        여기까지. 아이사랑카드 0.023 · 마음투자 0.019 · 통신비감면 0.007 ✗
      · **43개가 전부 0건으로 나와 한 번 속았다.** 방금 브라우저에서 되는
        걸 확인한 문화누리카드까지 0이었다. `/search` 응답은 RSC가 아니라
        서버 렌더 HTML이고 React 주석이 숫자 사이에 낀다(`">17<!-- -->건`).
        **세는 쪽을 먼저 의심한다**(7절과 같은 함정, 오늘만 두 번째다).
      · 나머지 39개는 맨 위 결과가 제대로 나왔다 — 오늘 넣은 문화누리카드·
        K패스 포함. 검색은 대체로 건강하다.
- [x] **상위 100건을 다시 받아 봤다 — 본문은 한 글자도 안 바뀌었다 (2026-09-07).**
      "원본이 이미 고쳐졌으면 코드로 땜질할 이유가 없다"를 확인한 것이다.
      상위 100건은 **중앙 82 · 지자체 18**이라 중앙 일일한도(100)에 딱 든다.
      100건 성공·실패 0.
      · 38건이 달라 보였지만 **20건은 배열 순서, 18건은 조회수 카운터**뿐이었다.
        `lifeArray`·`trgterIndvdlArray` 같은 쉼표 목록의 **순서가 호출마다
        흔들린다**("저소득, 장애인" → "장애인, 저소득"). 내용은 같다.
      · **그래서 받은 원본을 되돌렸다.** 그대로 두면 다음 `build-data`가
        38개 페이지에 순서만 다른 diff를 만들고, `SERVICES_UPDATED`가 밀려
        **800개 lastmod가 전부 오늘로 간다.** 내용은 그대로인데 전부 고친
        것처럼 보이는 거짓 신호다(4절과 09-05에 되돌린 것과 같은 이유).
      · **재수집을 강제하려면 파일을 지워야 한다.** 스크립트가 "이미 받은
        servId는 건너뛴다"라서, 백업해 두고 지운 뒤 돌린다.
      · 결론: **상위 100건에 관해서는 우리 데이터가 최신이다.** 다음에 또
        의심스러우면 900건이 아니라 이 100건만 돌린다(한 시간이 아니라 몇 분).
- [x] **"2026년 6월 예정"이 9월까지 그대로 나가고 있었다 (2026-09-07)** —
      `statedPlan` in `src/lib/applyPeriod.ts`. 조회수 96위 청년미래적금
      (80,450) 요약문 끝이 그렇게 끝나는데, 위의 재수집으로 **원본이 아직
      안 고쳐진 것**을 확인했다. 기존 「지난 신청 기간」 띠는 `~`로 이어진
      **범위**만 봐서 단일 시점은 안 걸렸다.
      · **"예정"만 본다.** 처음엔 `예정|시행|출시|오픈`으로 훑어 6건을 잡았는데
        그중 다섯이 "2025년 10월부터 시행", "2016.07.01.시행" 같은 것이었다.
        **시행일이 지난 것은 정상이다** — 이미 시행 중이라는 뜻이지 문장이
        낡은 게 아니다. 띠를 달면 멀쩡한 사업이 문제 있어 보인다. 아직 안
        일어난 일을 가리키는 말은 "예정"뿐이다.
      · 끝날을 **그달 마지막 날**로 잡는다. "6월 예정"은 6월 15일에 안 지났다.
      · 900건 중 2건이 걸리고 **지금 뜨는 것은 1건**이다. 참전명예수당의
        "2026년 10월부터 월별 지급 예정"은 아직 미래라 안 뜨고, 11월이 되면
        **배포 없이 저절로 뜬다.** 판정을 브라우저에 맡긴 이유가 이것이다.
      · "아직 시작 안 됐습니다"라고 쓰지 않는다. 우리가 아는 것은 원문의
        시점이 지났다는 사실뿐이다(3절).
- [x] **원문 최종수정일을 확인일 옆으로 올렸다 (2026-09-07).** 여태 맨 아래
      「공식 안내」 상자에만 있었다. 상단에는 "2026-09-04 확인"만 있는데
      그건 **우리가 받아 온 날**이지 원문이 고쳐진 날이 아니다.
      · 수록 900건 중 **40건은 원문이 2025년 이전**이고 가장 오래된 것은
        **2022-06-29**(대구 현장체험학습비)이다. 4년 된 공고를 "9월에
        확인함"으로만 적어 두면 날짜로 거짓말을 하는 셈이 된다.
      · "오래됐습니다" 같은 **판정은 붙이지 않는다.** 며칠이 지나야 낡은
        것인지는 제도마다 다르다. 두 날짜를 나란히 두고 읽는 사람이 정한다.
      · 중앙부처 330건은 이 값이 아예 없어(API가 안 준다) 조각이 안 붙는다.
- [x] **수록 데이터에 없는 제도를 처음 실었다 — 실업급여 (2026-09-07)** —
      `/guide/unemployment`. 검색량이 문화누리카드급인데(월평균 35 대 45)
      이름에 "실업"이 든 사업이 **0건**이다. 고용보험 소관이라 복지로 API에
      애초에 안 들어온다.
      · 그래서 **집계 숫자가 한 줄도 없는 첫 글**이다. 근거가 전부 조문과
        정부 안내다. 이런 글을 또 쓸 때의 선은 이 글에 그어 두었다 —
        자격 판정 안 함(제58조 이직 사유는 아예 안 펼침), 곱한 값은
        **"조문에 없는 숫자입니다"라고 화면에 밝힘**(11만 3,500 × 60%),
        사람마다 다른 하한액은 **금액을 안 적음**.
      · **글을 새로 내면 `opengraph-image.tsx`를 같이 만든다.** 09-06에
        빠뜨려 두 편이 카톡에서 썸네일 없이 나갔던 그 함정에 또 걸릴 뻔했다.
- [x] **본문에 HTML 코드가 그대로 나가고 있었다 (2026-09-06).** `&rarr;`가
      화살표가 아니라 글자 그대로 찍혔다. 900건 중 **49건 · 148군데**이고,
      그 49건이 가져가는 조회수가 **전체의 28.3%**다 — 장애인연금 3위,
      주거급여 4위, 에너지바우처 5위, 기초연금 8위가 들어 있다.
      · 원인은 `build-data.mjs`가 **숫자 엔티티만 풀고 이름 엔티티는 아무도
        풀지 않아서**였다. `&#9312;`(①)는 2차 해독이 있었는데 `&rsquo;`
        `&times;` `&ldquo;` 같은 것은 통과해 버렸다. 기호 46종 표를 더했다
        (`SYMBOL_ENTITIES` / `decodeNamedSymbols`).
      · **`&amp; &lt; &gt; &quot; &apos;`는 그 표에 넣지 않는다.** 1차에서
        이미 풀었고 2차에서 또 풀면 원문에 없던 해독이 한 단계 더 일어난다.
        표에 있는 것은 전부 구조와 무관한 기호뿐이라 두 번 풀려도 안전하다.
      · **재생성할 때 받은 날짜를 인자로 준다** — `build-data.mjs 900 2026-09-04`.
        안 주면 `SERVICES_UPDATED`가 오늘로 밀리는데, 그러면 ① 상세 페이지가
        "오늘 받아 정리했다"고 **사실이 아닌 말**을 하고 ② 사이트맵 허브
        lastmod가 전부 오늘로 밀린다. 이번엔 09-04로 고정해 **라이브 사이트맵과
        803개 전부 동일**(날짜 변경 0건)임을 대조하고 배포했다.
      · 화면에 `&#x27;`이 보이는 건 버그가 아니다. React가 `'`를 이스케이프한
        것이고 브라우저는 제대로 그린다. **HTML이 아니라 innerText로 세야
        한다** — 09-06에 HTML만 보고 10쪽이 깨진 줄 알았다.
- [x] **크롤러 정책을 갈랐다 (2026-09-06)** — `src/app/robots.ts`.
      "크롤링을 막는다"는 이 사이트에서 두 갈래다. **검색엔진을 막으면
      사이트가 죽는다** — 검색이 유일한 유입 경로다.
      · 막은 것: AI 학습 24종(GPTBot·ClaudeBot·CCBot·Google-Extended·
        Bytespider·meta-externalagent…) + SEO 분석 11종(Ahrefs·Semrush·MJ12…).
        학습을 막아도 **구글 검색 순위에는 영향이 없다**(구글 명시).
      · 연 것: 검색엔진 전부, 카톡·페북 링크 미리보기(`facebookexternalhit`),
        **애드센스 크롤러(Mediapartners-Google)** — 막으면 광고가 안 붙는다.
        AI **검색**봇(OAI-SearchBot·ChatGPT-User·Claude-User·PerplexityBot)도
        열어 뒀다. 출처를 달고 사람을 보내 주는 쪽이라 막으면 손해다.
      · 판단 기준은 하나다 — **그 봇이 사람을 우리 사이트로 보내 주는가.**
        새 이름이 계속 생기니 이 기준으로 넣고 뺀다.
      · `FacebookBot`(학습)과 `facebookexternalhit`(미리보기)은 **다른 봇**이다.
        헷갈려 뒤엣것을 막으면 카톡·페북 공유 카드가 죽는다.
      · robots.txt는 **지키는 봇에게만** 통한다. 진짜로 요청을 끊으려면
        Vercel 방화벽(WAF)이 필요하다 — 대시보드 일이라 사용자가 한다. **미완.**
- [x] **CSP를 셋 더 걸었다 (2026-09-06)** — `base-uri 'self'` ·
      `form-action 'self'` · `object-src 'none'`. 넣기 전에 `<form>` 0건,
      `<iframe>·<object>·<embed>` 0건, `<base>` 0건을 확인했다.
      · `script-src`는 **여전히 안 넣는다.** 지금 넣으면 애드센스가 죽는다.
        nonce 미들웨어와 함께 손봐야 한다(next.config.ts 머리말).
      · `frame-src`도 건드리지 않는다. 광고 iframe을 직접 막는 자리다.
        CSP는 다른 출처 iframe **안으로 상속되지 않아** 위 셋은 광고와 무관하다.
- [x] **확인일을 상세 상단으로 올렸다 (2026-09-06).** 같은 값이 맨 아래
      「공식 안내」 상자에 있었는데, 375px에서 상위 상세는 5,400px이 넘어서
      **끝까지 내려간 사람만** 이게 언제 정보인지 알 수 있었다. 복지 정보는
      날짜가 없으면 맞는지 틀린지 판단할 수가 없다.
      · 첫 화면 수치 밑에도 출처 한 줄을 넣었다. 여태 푸터까지 내려가야
        나왔는데, 처음 온 사람이 "믿어도 되나"를 정하는 자리는 첫 화면이다.
      · 남아 있던 이모지 둘(🔍·📅)을 선 아이콘으로 바꿨다. **기기마다 다른
        그림이 나온다** — 헤더처럼 늘 떠 있는 자리가 사람마다 달라 보이면
        만들다 만 사이트로 읽힌다(Badge.tsx·display.ts가 이미 걷어낸 이유).
- [ ] **애드센스는 사용자가 직접 한다 (2026-09-10 확정).**
      **여기서 진행하지 않는다. 상태를 확인하겠다고 열지도, 매번 다시 묻지도
      않는다.** 사용자가 요청할 때만 손댄다. 계정이 러닝온과 같은
      `runningon93`이라 이 브라우저에 로그인돼 있지도 않다(바로 아래 pub 번호
      함정 참고). 아래 내용은 **참고 기록**이지 할 일 목록이 아니다.
- [ ] 애드센스 참고 — 사이트 등록·소유확인 완료, **승인 심사 「준비 중」**(09-07 확인,
      09-06과 같음. 상태 세부정보는 `—`, 최종 업데이트 09-05 09:49 KST 그대로)
      · **계정은 `runningon93@gmail.com`의 `pub-3333691556845206`이다**
        (2026-09-09 사용자 재확인).
        **애드센스는 1인 1계정**이라 새로 못 만든다. 그래서 러닝온으로 이미
        승인받은 그 계정에 복지클릭을 **사이트로 추가**한 것이다. 애드센스
        한 계정이 사이트를 여럿 갖는 건 정상이지만, 0절의 계정 분리와
        어긋나므로 **애드센스만은 예외임을 알고 있어야 한다.** jgga3111도
        jgga1234567도 이 계정에 접근 권한이 없다 —
        `?authuser=runningon93@gmail.com`.
        · ⚠ **`jgga1234567`에도 애드센스 계정이 따로 있다 — `pub-4973793162848744`.**
          2026-09-10에 `?authuser=runningon93@gmail.com`을 붙여 열었는데
          **파라미터가 안 먹고** 그 계정으로 들어갔다. 브라우저에 로그인된
          것이 `jgga1234567`뿐이면 애드센스는 **말없이 그 계정을 연다**
          (서치콘솔은 "액세스할 수 없습니다"라고 말해 주는데 여기는 안 그런다).
          화면이 「사이트 추가 · 필수」로 시작하는 **빈 온보딩**이라, 모르고 보면
          *"애드센스가 초기화됐다"*로 오판하기 딱 좋다. 실제로는 활성화도 안 된
          남의 빈 계정이다.
          → **애드센스를 열면 `pub-` 번호부터 확인한다.**
          `pub-3333691556845206`이 아니면 그 화면은 우리 것이 아니다.
          → **이 빈 계정에 사이트를 붙이거나 활성화하지 않는다.** 붙이면
          광고 코드가 엉뚱한 게시자 번호로 나간다.
        · ⚠ **09-09에 이 줄을 jgga3111로 고쳤다가 되돌렸다.** 세 계정이
          섞여 있어 사람도 헷갈리는 자리다. **셋을 한 줄로 외우지 말고
          역할로 기억한다** — 코드를 올리는 곳(jgga3111) · 검색엔진에
          말하는 곳(jgga1234567) · 광고비를 받는 곳(runningon93).
      · `ads.txt 상태`는 09-06 기준 **승인됨**이다(09-05 09:49 KST 갱신).
        "찾을 수 없음"은 결함이 아니라 재크롤링 지연이었다.
      · 계정에 **"주소가 확인되지 않아 지급이 보류 중"** 알림이 떠 있다.
        러닝온 쪽 사안이라 여기서 건드리지 않는다.
      · 2026-09-02 서치콘솔 속성 등록·소유확인 완료(URL 접두어, jgga1234567)
      · **계정 번호(`/u/N/`)를 외우지 말 것.** 09-03에 `/u/2/`였던 자리가
        09-04에는 runningon93 계정이 됐다. 순번은 로그인 순서 따라 바뀐다.
        주소에 `?authuser=jgga1234567@gmail.com`을 붙이면 구글이 알아서 찾아간다.
      · **09-07에 「페이지 색인 생성」 보고서가 열렸다**(속성 등록 09-02, 닷새째).
        **색인 생성됨 270 · 색인 안 됨 576.** 클릭은 6회(09-06 4회).
        미색인 576의 내역:

        | 사유 | 건수 |
        |---|---|
        | 발견됨 – 현재 색인이 생성되지 않음 | 528 |
        | 'NOINDEX' 태그에 의해 제외 | 46 |
        | 크롤링됨 – 현재 색인이 생성되지 않음 | 2 |

        **수록 900→1,500을 올리지 않는다.** 사이트맵 803개 중 270개만 색인됐고
        528개가 "발견됐지만 크롤링할 가치가 낮다"고 대기 중이다. 러닝온이
        똑같이 598건을 쌓아 두고 있었다(docs/02 1절). **이 상태에서 URL을
        600개 더 넣으면 대기열만 길어진다.** 09-06에 적은 "상위 100건을 깊게
        파는 쪽이 낫다"가 이 수치로 확인됐다.
- [x] **계산기를 처음 냈다 — 셋 (2026-09-08).** 여태 사이트에 계산하는 곳은
      `/check`(중위소득) 하나뿐이었다. 원문의 금액을 **산문으로만** 보여 주고
      한 걸음을 안 갔다.
      · `/guide/k-pass` — 원문이 "20%~53.3%", "기준금액(3~10만원)"이라고
        **범위만** 말한다. 표는 운영기관인 **한국교통안전공단**에 있었다.
      · `/guide/tax-credit-amount` — **밖에서 가져온 숫자가 하나도 없다.**
        산식이 수록 원문(23위 「근로·자녀장려금」)에 통째로 들어 있었다.
        `/guide/tax-credit`(언제)과 짝이다 — 이쪽은 얼마다.
      · `/guide/youth-savings` — 원문이 "납입액의 **일정비율**"이라고만 한다.
        비율(일반형 6%·우대형 12%)은 금융위 보도자료에 있었다.
      · `/guide/emergency`에 생계지원 금액표를 더했다(아래 별항).

      ── 이번에 얻은 규칙 넷 ──────────────────────────────────
      · **요약본을 믿지 말고 병합 셀까지 본다.** 공단 페이지를 요약해 읽었더니
        "어르신 50%"였는데 원문 표는 `<td colspan="3">30%</td>`로 청년·어르신·
        2자녀를 묶고 있었다. 요약이 병합을 못 읽고 값을 오른쪽으로 민 것이다.
        국토부 보도자료 제목이 "어르신 유형(**환급률 30%**) 신설"이라 대조하면
        걸린다. **그대로 실었으면 어르신에게 실제의 1.7배를 알려 줄 뻔했다.**
      · **교차 검증되는 자리를 찾는다.** K-패스 기준금액 표의 최솟값 3만원·
        최댓값 10만원이 복지로 원문의 "기준금액(3~10만원)"과 정확히 맞았다.
        서로 다른 두 출처가 같은 수를 가리키면 옮겨 적은 것이 맞다는 뜻이다.
      · **두 출처가 어긋나면 계산하지 않고 어긋난 사실을 싣는다.** 청년미래적금
        일반형 소득기준이 복지로 7,500만 대 금융위 6,000만이다. 가입 상한과
        기여금 상한이 다른 것으로 읽히지만 **그건 우리 해석이지 어느 문서에도
        없다.** 그 구간을 계산에서 빼고 「두 문서가 다릅니다」 절로 적었다.
      · **모르는 값을 그럴듯한 수로 채우지 않는다.** 청년미래적금 이자는 은행마다
        달라 계산에서 뺐고("계산 안 함"이라고 화면에 적음), K-패스 일반형은
        3,000원 미만 이용분을 모르면 **0이 아니라 "계산 안 함"**으로 둔다 —
        0은 "없다"는 뜻이라 빈칸과 다르고, 뭉개면 실제보다 적게 알려 준다.

      ── 판정은 여전히 하지 않는다 ────────────────────────────
      계산기는 되고 **판정소는 안 된다.** 고시·원문의 산식에 넣어 "이 값이
      나옵니다"는 계산이지만 "당신은 받을 수 있습니다"는 판정이다(3절).
      유형·지역·재산 구간은 **고르는 것이지 우리가 맞히는 것이 아니다.**
      곱한 값에는 전부 "저희가 계산한 값입니다"를 붙였다(실업급여 글의
      "조문에 없는 숫자입니다"와 같은 자리).
- [x] **긴급복지 글에 금액표를 넣었다 — 결정을 뒤집으며 주석을 고쳐 썼다 (09-08).**
      원래 이 글은 **금액을 일부러 안 실었다.** 주석에 이유가 적혀 있었다 —
      "여기 옮겨 적으면 고시가 바뀌는 순간 이 글만 옛 숫자로 남는다."
      · 그 걱정은 **손으로 적을 때만** 맞다. 새로 넣은 표는 수록 원문의
        `supportContent`를 빌드 때 파싱해 그린다. 재수집으로 금액이 바뀌면
        글도 같이 바뀌고, 상세 페이지와 **같은 한 곳**을 보므로 어긋날 수 없다.
        파싱이 실패하면 절 자체가 사라진다 — 틀린 표를 그리느니 없는 게 낫다.
      · **남의 결정을 뒤집을 때는 주석을 지우지 말고 고쳐 쓴다.** 원래 판단과
        무엇이 달라져서 바꿨는지를 같이 남긴다. 안 그러면 다음 사람이 같은
        고민을 처음부터 다시 한다.
- [x] **토스 미니앱 인기 100선을 수요 신호로 썼다 (09-08).** 사용자가 화면을
      찍어 줬다. 100개 중 **복지·지원금이 28개**였고 갈래가 뚜렷했다.

      | 갈래 | 토스 순위 | 개수 |
      |---|---|---|
      | 지원금 찾기·매칭 | 7·28·35·37·42·48·61·75·93 | 9 |
      | **근로장려금 계산** | 22·26·30·55·74(+10 반기) | **6** |
      | K패스 환급 계산 | 53·78·100 | 3 |
      | 기초생활 수급 **판정** | 38·66 | 2 |

      · **검색량과 겹치는 것이 강한 신호다.** K패스는 데이터랩에서 실업급여의
        2.4배였고 토스에도 셋이 있었다 — 두 곳이 같이 가리켰다.
      · **우리는 이미 가진 숫자를 안 쓰고 있었다.** 상위 100건 중 **72건**에
        구체 금액이 원문에 있고 그중 22건은 산식·비율·한도까지 있다. 상위
        20건 중 17건이다. 그런데 계산하는 곳은 `/check` 하나뿐이었다.
      · **인기가 있어도 안 하는 것**: 「수급 점검」·「판정소」류(38·66·74).
        3절이 못 박은 자리다. 급식·택배·운세·주식과 공공 대관(45)·
        어린이집(84)·환급금 조회(89)는 주제 밖이다.
      · 다음 후보로 재 볼 것: 국민연금 미리보기(32위) — 우리 데이터엔
        194위 출산크레딧 1건뿐이고, 계산이 가입이력에 달려 있어 조심스럽다.
- [x] **09-08 색인 작업 — 구글 25건 요청 · 빙 100건 제출.**
      구글은 조회수 **167~200위**를 훑어 25건 요청, **15건은 이미 색인돼
      건너뛰었다**(40건 검사). **다음은 201위 `WLF00002235`(서울형 긴급복지)부터.**
      · 우선 6건 먼저 넣었다 — `WLF00005440`(K-패스) · `WLF00000092`
        (기저귀바우처) · `WLF00006266`(청년미래적금) · `WLF00000055`
        (문화누리카드) · `/guide/unemployment` · `/guide/pay-dates`.
        **조회수 20위 기저귀바우처가 아예 미색인이었다**(발견됨-크롤링 안 됨).
      · **이미 색인된 페이지도 내용이 바뀌었으면 재요청한다.** 화면에
        「페이지가 변경되었나요? 색인 생성 요청」이 따로 뜬다. "이미 색인되면
        건너뛴다"는 규칙은 **내용이 그대로인 백로그에만** 해당한다.

      ── 이번에 새로 걸린 함정 셋 ──────────────────────────────
      · **「오류 발생 — 색인 생성 요청을 제출하는 중에 문제가 발생했습니다.
        나중에 다시 시도해 주세요」는 할당량이 아니다.** 171위에서 한 번 떴는데
        **그대로 다시 누르니 들어갔다.** 이걸 한도로 읽고 그만두면 남은 걸
        통째로 못 넣는다. 한 번은 재시도해 보고 판단한다.
      · **검사창은 `triple_click`으로 넣는다.** 그냥 클릭 뒤 타이핑하면
        글자는 들어가는데 **Enter가 안 먹어 「개요」에 머문다**(입력창이
        여러 개 겹쳐 있고 활성 것이 그때그때 다르다). 09-05에 적어 둔
        "ctrl+a로 안 지워진다"와 같은 자리다.
      · **크롬 기본 계정이 `jgga3111`이라 「이 속성에 액세스할 수 없습니다」가
        뜬다.** 주소에 `?authuser=jgga1234567@gmail.com`을 붙이면 `/u/1/`로
        들어간다. 0절의 계정 분리가 여기서 걸린다.
      · 요청 한 건에 **40~50초**가 걸린다. 누르면 구글이 「실제 URL 테스트」를
        먼저 돌리기 때문이다. 이게 09-07에 적은 "한 건 75초"의 정체다.
- [x] **미색인 576건의 실상을 갈라 봤다 (2026-09-08).** "전부 해결"은 안 된다.

      | 사유 | 건수 | 실상 |
      |---|---|---|
      | `NOINDEX` 태그 | 46 | **우리가 일부러 한 것** — 얇은 페이지 방침 |
      | 크롤링됨 – 색인 안 됨 | 2 | 전수 확인, **결함 아님** |
      | 발견됨 – 색인 안 됨 | 528 | 구글이 **크롤링조차 안 한 것** |

      · 크롤링됨 2건은 `WLF00002234`(169위·본문 599자)와 `/income/48`
        (소득기준 허브, 3건)이다. 둘 다 기준을 넘고 결함이 없다. 구글이
        아직 안 실었을 뿐이라 **고칠 것이 없다.**
      · 528은 우리 페이지 결함이 아니라 **구글이 우선순위를 낮게 매긴 것**이다.
        수동 요청으로 다 밀어 넣으면 한 건 40~50초 × 528 ≈ **7시간**이고,
        넣어도 색인은 보장되지 않는다. "URL을 더 넣으면 대기열만 길어진다"는
        09-07 결론이 그대로 유효하다. **값어치 있는 데까지만 넣는다.**
- [x] **09-09 색인 작업 — 구글 26건 요청 · 빙 사이트맵 재제출.**
      구글은 40건을 검사해 26건 요청, **14건은 이미 색인**돼 건너뛰었다.
      **다음은 237위 `WLF00002372`(기초생활지원 차상위계층지원)부터** — 같은 날
      오후에 여기까지 넣었고 지금 다음은 262위다(아래 09-09 2차).
      · 우선 4건 먼저 — 09-08 배포분 `/guide/k-pass` · `/guide/tax-credit-amount` ·
        `/guide/youth-savings`(새 글 셋) + `/guide/emergency`(금액표 추가).
        **새 글 셋은 전부 미색인**이었고 emergency는 이미 색인돼 있어 재요청했다.
      · 백로그 201~236위에서 22건 요청. 이 구간도 **절반 가까이 이미 색인**이라
        (14/40) 수동 요청의 값어치가 계속 줄고 있다.
      · **빙 사이트맵을 재제출했다.** 빙 804 대 라이브 807로 벌어져 있었다
        (09-08 배포로 새 글 셋이 늘었다). `⋮ → Re-submit` 뒤 Status가
        `Success` → **`Processing`**, Last submit이 9/6 → 9/8로 바뀌는 것을
        값으로 확인했다. `URLs discovered`는 빙이 다시 긁은 뒤에 바뀐다.

      ── 이번에 새로 걸린 함정 셋 ──────────────────────────────
      · **빈 검사창에는 `triple_click`이 안 먹는다.** 09-08에 "검사창은
        triple_click으로 넣는다"고 적었는데 반쪽이었다. 값이 **있을 때**는
        맞지만, 빈 칸에 triple_click하면 포커스가 안 잡혀 타이핑이 사라진다.
        → **빈 칸이면 `left_click`, 값이 있으면 `triple_click`.**
      · **창 크기가 바뀌면 GSC가 모바일 레이아웃으로 갈아탄다.** 세션 도중
        프레임이 1351 → 618로 줄었는데, 그 순간 검사창이 화면 밖(x=-127)으로
        나가고 상단 **돋보기 아이콘** 뒤로 숨었다. 좌표가 음수로 나오면
        "안 보인다"가 아니라 **레이아웃이 바뀐 것**이다 — 돋보기를 먼저 누른다.
        (09-04에 적은 "프레임 폭은 고정값이 아니다"의 연장이다.)
      · **「오류 발생」이 이날 네 번 떴고 네 번 다 재시도로 들어갔다**
        (206·213·214·235위). 09-08에 한 번 겪고 적어 둔 규칙이 그대로 맞았다.
        이제는 **오류를 보면 무조건 한 번 재시도**한다.
      · 화면 URL 대조가 또 일을 했다. 닫기가 안 눌려 대화상자가 남아 있으면
        검사창에 못 들어가는데, 화면에는 **앞 URL의 판정이 그대로** 떠 있다.
        `u.endsWith(넣은 id)`를 매번 확인해 두 번 걸러 냈다.
      · **좌표 클릭을 그만두고 자바스크립트로 돌렸다** (09-09 2차, 아래 별항).
- [x] **09-09 2차 — 빙 100건 · 구글 15건 요청, 그리고 「할당량 초과」를 처음
      봤다.** 아침에 남겨 뒀던 빙 601~700위를 오후에 넣고(할당량이 차 있었다),
      구글은 237위부터 훑어 **25건 검사 · 15건 요청 · 10건 이미 색인**.
      ~~다음은 262위 `WLF00000098`부터~~ — **09-10에 그만뒀다.**
      조회수 백로그를 이어서 넣지 않는다(바로 아래 「색인 요청 기준을 바꿨다」).

      ── 좌표 클릭을 버렸다 ────────────────────────────────────
      여태 스크린샷 좌표로 눌렀는데(프레임 폭이 바뀐다, 모바일로 갈아탄다,
      캡처가 깨진다…) 이번에 **화면을 안 보고 자바스크립트로만** 돌렸다.
      한 건에 40~90초는 그대로지만 좌표 사고가 0이었다.
      · **`computer`의 `key Return`이 검사창에서 안 먹는다.** 글자는 들어가는데
        엔터만 씹혀 「개요」에 머문다. `KeyboardEvent('keydown'…keyCode:13)`을
        입력창에 **직접 쏘면** 들어간다. 09-08의 "triple_click으로 넣는다"는
        입력 얘기였고, **보내는 것은 별개 문제**였다.
      · **검사창이 두 개 겹쳐 있고 앞의 것은 `disabled`다.** 폭만 보고 고르면
        죽은 칸을 잡는데, 에러도 안 나고 글자가 산 칸에 **덧붙는다**
        (`…WLF00003547https://…`). `!disabled && !readOnly`를 같이 본다.
      · **GSC는 앞서 검사한 패널을 DOM에 그대로 쌓아 둔다.** 버튼을 첫 번째로
        잡으면 **옛 화면의 버튼**을 누른다. 보이는 것 중 **마지막**을 쓴다.
      · **그리고 이게 「오류 발생」 3연속의 진짜 원인이었다.** 할당량인 줄
        알았는데 아니었다 — 고쳐서 다시 넣으니 **세 건 다 한 번에 들어갔다.**
        09-08·09-09에 적어 둔 "오류는 할당량이 아니다"가 또 맞았고, 이번엔
        **재시도가 아니라 내 선택자가 문제**였다. 오류를 보면 재시도하되,
        연달아 나면 **누르고 있는 버튼이 맞는지**부터 본다.
      · 재시도할 때 버튼 라벨이 **「색인 생성 요청다시 요청」**으로 바뀐다.
        정확히 일치로 찾으면 두 번째부터 못 찾는다 — 앞부분만 본다.
      · **도구 호출이 45초에 끊긴다.** `await`로 기다리면 45초짜리 작업도
        중간에 잘린다. `await` 없이 IIFE로 띄우면 **페이지는 계속 돈다** —
        결과를 `window.__log`에 쌓고 짧은 호출로 꺼내 본다. 75건을 한 번에
        걸어 두고 40초마다 개수만 읽는 식으로 돌렸다.

      ── 「할당량 초과」는 「오류 발생」과 다른 화면이다 ────────────
      262위에서 **「할당량 초과 — 일일 할당량을 초과하여 이 요청을 처리할 수
      없습니다. 내일 다시 제출해 주세요.」**가 떴다. 재시도해도 안 들어간다.
      **이 문구를 보면 그날은 끝이다.** 「오류 발생 — …나중에 다시 시도해
      주세요」와 글이 다르니 **문구로 갈라 읽는다.**
      · 오늘 들어간 것은 15건이다. 실측 목록에 09-09를 둘로 나눠 적는다
        (오전 26건 · 오후 15건 = 41건).
      · 246~261위 16건 중 **10건이 이미 색인**이었다(62%). 09-07에 35/80,
        09-08에 15/40, 09-09 오전에 14/40이었으니 **비율이 계속 오른다.**
        250위 아래는 사이트맵만으로 붙고 있다는 뜻이다.
- [x] **화면이 밋밋했던 원인은 카드가 아니라 지면이었다 (2026-09-09).**
      사용자가 "너무 밋밋하다"고 해서 경쟁 사이트(해피나눔)를 실측 대조했다.

      | | 복지클릭(전) | 해피나눔 |
      |---|---|---|
      | 지면 | **순백 `#fff`** | `rgb(250,250,249)` |
      | 카드 | 흰색·라운드 12·그림자 없음 | **똑같음** |
      | 홈 img+svg | **1개** | 24개 |
      | 목록 img+svg | **0개** | — |

      · **카드는 이미 저쪽과 같았다.** 그림자도 저쪽이 안 쓴다. 차이는
        **지면이 카드와 같은 흰색**이라 807쪽 전부에서 카드가 테두리 선
        하나로만 구분됐다는 것이다. 그래서 면을 세 층으로 갈랐다 —
        `card #fff` / `ground #faf9f7` / `sunken #f0ede7`(옛 `bg-slate-50`
        49곳). 색 온도도 파랑 쪽에서 중립으로 옮겼다(ink·muted·line).
      · **주색 파랑은 그대로 뒀다.** 러닝온과 형제로 보이게 맞춘 결정을
        통째로 뒤집지 않고 지면만 손봤다. 되돌리려면 ground를 `#fff`,
        sunken을 `#f8fafc`로 두 줄만 바꾸면 된다(globals.css 머리말).
      · **아이콘 세트 35종을 그렸다**(`components/AxisIcon.tsx`) — 대상 6 ·
        생애주기 7 · 주제 15 · 혜택 7. `AxisGrid` 한 곳만 고치면 허브 네
        곳(`/target`·`/life`·`/theme`·`/benefit`)이 전부 걸린다.
        09-06에 이모지를 걷어내며 *"아이콘 세트를 직접 그릴 형편이 아니니
        아예 없앴다"*고 적어 둔 자리를 이번에 채운 것이다.
      · 이모지가 실패한 이유가 "제각각"이었으므로 그 반대만 지켰다 —
        24×24 · 선 1.6 · **면을 칠하지 않음** · 색은 `currentColor` ·
        도형은 원·둥근 사각·선 셋으로만. 없는 슬러그는 물음표 같은 대체
        그림을 넣지 않고 **아예 안 그린다.**
      · 대비 실측: 본문 대 지면 **16.56**, muted 대 지면 **6.17**(기준 4.5).
        `text-slate-*` 158곳은 손대지 않았다 — 거의 중립이라 그대로 읽힌다.
      · **「마감 임박 D-day」는 안 했다.** 저쪽 첫 화면의 핵심 장치인데
        900건의 `applyEnd`가 **9999-12-31 555건 · 없음(중앙부처) 330건**이라
        실제 날짜는 **15건뿐**이고, D-30 안쪽은 **1건**이다(2026-09-30, D-21).
        나머지는 D-113 이상이라 화면이 안 된다. docs/03 §7의 "달력에 채울
        것이 없다"가 지금 데이터로도 그대로 맞다(§8에 다시 적었다).
        · 09-09에 처음 적을 때 16이라고 썼는데 **다시 세니 15**였다.
          이런 수는 화면에 안 나가더라도 판단의 근거가 되므로 **세는 명령을
          같이 남긴다** — `grep -o 'applyEnd": *[^,}]*' src/data/services.ts`.
      · **스톡 사진도 안 넣었다.** 저쪽 히어로는 스톡 삽화인데 라이선스가
        걸리고, 애드센스 심사 중에 출처 불분명한 이미지를 늘릴 이유가 없다.
        삽화가 필요하면 OG 카드처럼 우리가 그리는 SVG로 간다.
- [x] **축을 겹쳐서 좁힐 수 있게 했다 — `/find` (2026-09-09).** 우리는 축이
      경쟁 사이트보다 많은데(주제 15·대상 6·생애주기 7·혜택 7·지역 15)
      **축마다 페이지가 따로 있을 뿐 겹칠 수가 없었다.** "청년이면서 저소득"을
      찾으려면 `/life/youth`에 들어가 눈으로 골라야 했다. 해피나눔은 홈에서
      생애주기·대상·지역을 한 번에 고르게 한다(docs/03 §7 탐색축 비교).
      · **만들기 전에 조합이 성립하는지 셌다.** 대부분 비면 필터가 고장난
        것처럼 보인다. 생애주기 7 × 대상 6 = 42개 중 **39개가 3건 이상**이고
        0건은 2개뿐이었다(임신·출산×보훈, 임신·출산×다문화).
      · **결과 화면은 `noindex`다.** 조합마다 URL이 생기므로 색인시키면
        "발견됨 – 색인 안 됨" 528건에 스스로 더 얹는다. `/search`가 택한 길과
        같고, 사이트맵은 807 그대로다. 대신 결과 밑에 **색인되는 축 페이지**
        (`/life/*`·`/target/*`·`/region/*`) 링크를 함께 둔다.
      · **거르는 일은 서버가 한다.** `AxisFinder`(클라이언트)는 축 목록만 알고
        `@/data/services`는 손대지 않는다 — 900건을 브라우저로 내리면 2.9MB다
        (`lib/search.ts` 머리말의 함정과 같은 자리). 그 대가로 고르는 동안
        건수를 미리 못 보여준다. 그건 받아들인다.
      · **엉뚱한 슬러그는 404가 아니라 무시한다.** 주소를 손으로 고쳐 온
        사람에게 빈 화면을 주느니 나머지 조건으로 좁혀 보여주는 쪽이 낫다.
      · ⚠ **`/find`의 상자는 지금 걸린 조건을 물고 시작해야 한다.** 처음엔
        안 그래서, 청년·저소득 결과(67건)를 보는데 상자는 전부 "전체"였고
        단추는 "전체 목록 보기"였다 — 지역만 더하려 누르면 앞서 고른 둘이
        풀린다. 넘기는 값은 주소의 날것이 아니라 **걸러 낸 값**이어야 한다
        (화면에 적용된 것과 상자에 켜진 것이 늘 같아야 한다).
      · 실측 대조: 청년×저소득 67 · 노년×저소득 86 · 임신출산×보훈 0(빈 화면
        문구) · 청년×저소득×서울 40 · 경기만 430(120건 자름 안내). 전부
        미리 계산한 값과 일치.
      · **`audit-sitemap`의 「일부러 제출하지 않는 것」 표에 `/find`가 빠져
        있었다.** 그 표는 데이터만 읽고 `src/app`을 안 훑어서, 사이트맵에
        안 넣는 페이지를 새로 만들면 조용히 옛 수로 남는다(3과 2는 손으로
        센 수다). 807이라는 합계는 멀쩡한데 **왜 목록에 없는지를 설명해
        주지 못하는 상태**였다. 줄을 하나 더하고 그 이유를 주석에 적었다.
        **사이트맵에 안 넣는 페이지를 만들면 이 표도 같이 고친다.**
- [x] **문장으로 물어보는 자리를 만들었다 — `/ask` (2026-09-09).** 첫 화면에서
      찾는 길이 셋이었는데(헤더 검색·여덟 칸·`/find`) **셋 다 먼저 무언가를
      알아야** 쓸 수 있었다 — 검색은 사업 이름을, 나머지 둘은 "생애주기"·
      "대상"이 무엇인지를. 처음 온 사람이 품고 오는 것은 축이 아니라 문장이라
      ("서울 사는 30대인데 월세 지원 있나요"), 아무것도 모르는 사람이 첫
      화면에서 쓸 수 있는 길이 하나도 없던 셈이다.
      · **히어로를 반으로 줄이고 그 자리를 내줬다.** 375px 실측 **341px →
        187px**(데스크톱 179px). 줄인 것은 두 줄짜리 제목을 한 줄로, 4xl을
        3xl로, 그리고 설명 문단을 통째로 뺀 것이다 — 그 문단이 하던 말을
        **바로 밑 수치 셋이 이미 더 정확하게** 하고 있었다(900건·570건).
        수치와 출처 줄은 한 치수만 줄이고 남겼다. 히어로에서 사실을 말하는
        줄은 거기뿐이다.
      · **히어로 안에 넣지 않았다.** 히어로에 큰 검색창을 뒀다가 걷어낸 적이
        있다 — 375px에서 헤더 검색과 나란히 보여 같은 모양의 둥근 상자가
        둘이었다. 그래서 **제목이 붙은 상자**로 만들어 히어로 밖에 뒀다.
      · **AI가 아니다. 모델을 부르지 않는다.** 질문의 낱말을 표와 대조할
        뿐이라(`lib/askParse.ts`), 화면에 "AI가 답해 드립니다"라고 적지
        않는다. 대신 **읽은 것을 전부 펼쳐 보여준다** — 무엇을 조건으로
        바꿨는지("30대" → 청년), 어느 낱말을 못 찾았는지, 무엇을 바꿔
        찾았는지("병원비" → 의료비로 찾음). 알아들은 척하고 조용히 좁히면
        안 나온 것이 없는 것인지 못 읽은 것인지 구분할 방법이 사라진다.
      · **자격 판정은 안 한다**(3절). 소득 금액이 보이면 판정하지 않고
        `/check`로 보낸다. 나이로 생애주기를 고를 때는 "나이 기준은 사업마다
        다릅니다"를 화면에 함께 적는다 — 청년 상한이 34세인 사업도 39세인
        사업도 있어서, 우리가 그은 선은 자격선이 아니다.
      · **`noindex`다.** 질문마다 URL이 생긴다 — `/search`·`/find`와 같은 길.
        `audit-sitemap`의 「결과 화면」 줄을 2 → 3으로 같이 고쳤다(바로 위
        항목에 적어 둔 규칙이 첫 적용을 받은 자리다). 사이트맵은 807 그대로.
      · 서버 컴포넌트다. 묻는 상자도 평범한 `<form method="get">`이라
        **클라이언트 번들이 0바이트 는다.**

      ── 이번에 얻은 규칙 넷 (전부 화면을 보고 고친 것) ──────────────
      · **필드가 있다고 축으로 쓸 수 있는 건 아니다.** 처음엔 혜택(지급형태)도
        조건으로 걸었는데, 「기초생활수급자 전기요금 감면」을 넣으니 24건이
        나오면서 **정작 「전기요금 복지할인」이 빠졌다.** 원본이 그 사업을
        「감면」이 아니라 **「현금지급」**으로 분류해 놨기 때문이다. 원본이 매긴
        값은 우리가 못 고치고, 걸러 내는 데 쓰면 **가장 맞는 답이 조용히
        사라진다.** 그래서 혜택은 **거르지 않고 점수만 준다.** 본문에는
        "요금감면을 지원합니다"가 그대로 있어 낱말로는 걸린다.
        · 같은 이유로 **주제(`themes`)는 아예 조건으로 안 쓴다.** 900건 중
          329건에만 있고 **전부 중앙부처**라, "주거"를 걸면 지자체 570건이
          통째로 사라지는데 화면에는 그냥 적게 나온 것처럼 보인다.
          5절의 「중앙과 지자체는 주는 필드가 다르다」가 그대로 걸린다.
      · **낱말로 걸러도 안 되고 안 걸러도 안 된다 — 가른다.** 안 걸린 것을
        그냥 두니 "월세"로 찾은 목록 다섯째부터 장애인연금이 나왔고, 걸러
        내니 "혼자 아이 키우는데"가 한부모 117건에서 4건으로 줄면서 정작
        「한부모가족 아동양육비」가 사라졌다. 뒤엣것의 원인은 낱말이 나빠서가
        아니라 **관공서 문장이 「아이」를 안 쓰기 때문**이다(아동·자녀라고
        쓴다). 그래서 낱말까지 걸린 것을 먼저 내고, 조건만 맞는 것을 **그
        아래 줄을 긋고** 따로 붙인다. 어느 쪽도 버리지 않는다.
      · **사람 말과 원문 말이 다른 자리는 세어 보고 잇는다.** 「병원비」는
        900건에 **0건**인데 「의료비」는 58건이다. 넣는 기준 둘 — ㉠ 사람 말이
        0건일 것 ㉡ 원문 말은 여러 건 있을 것. 실측으로 여덟 쌍만 넣었다
        (병원비→의료비 · 약값→약제비 0/9 · 전기세→전기요금 0/10 ·
        가스비→가스요금 0/3 · 수도세→수도요금 0/11 · 집세→임차료 0/9 ·
        밥값/끼니→급식 0/20). **탈락**: 치료비 19 · 진료비 24 · 등록금 12 ·
        학원비 7 · 차비 4 · 간병비 9 — 사람 말이 이미 원문에 있다.
        `lib/aliases.ts`가 별칭을 넣을 때 쓰는 방식과 같다.
      · **"어디에도 없습니다"라고 쓰기 전에 어디를 셌는지 본다.** 「전기요금
        감면」에서 "「전기요금」은 수록 900건 어디에도 없습니다"가 나갔는데
        **틀린 말이었다** — 900건에는 있고 조건으로 좁힌 24건에 없었을
        뿐이다. 세는 범위를 문장에 그대로 적는다(3절). 7절의 「검사 쪽을
        먼저 의심한다」와 같은 자리다.
- [ ] **네이버 서치어드바이저는 사용자가 직접 한다 (2026-09-10 확정).**
      **여기서 진행하지 않는다. 매번 다시 묻지도 않는다.** 계정을 새로 정해야
      하고(0절의 계정 셋에 네이버 줄이 없다) 콘솔에서 `naver-site-verification`
      토큰을 받아 와야 하는데, 그 두 가지가 사용자 손에 있다.
      · 코드 쪽 준비는 **이미 끝나 있다.** 사이트맵 날짜가 `YYYY-MM-DD`라
        네이버 파서에 맞고(4절), `robots.ts`가 Yeti를 안 막고 있으며,
        `www`는 apex로 308이다.
      · 사용자가 토큰을 주면 `src/app/layout.tsx`의 `verification.other`에
        한 줄 넣는 것으로 끝난다. **그때만 손댄다.**

- [x] **색인 요청 기준을 바꿨다 — 「좋은 글 위주로만」 (2026-09-10 사용자 확정).**
      여태 조회수 순서를 따라 내려가며 백로그를 한도까지 채웠다. 그만둔다.
      · 넣는다 — 우리가 직접 쓴 안내 글, **내용이 실제로 바뀐 페이지**,
        본문이 두툼한 상위 상세.
      · 안 넣는다 — **넣어 봐야 색인이 안 되고 「발견됨 – 색인 안 됨」으로
        쌓이기만 할 것.** 할당량이 남았다고 백로그로 채우지 않는다.
      · 근거는 이미 이 문서에 있었다. 「발견됨 – 색인 안 됨」이 528건이고,
        순위가 내려갈수록 **이미 색인돼 건너뛰는 비율이 계속 올랐다**
        (09-07 44% → 09-08 38% → 09-09 오전 35% → 09-09 오후 **62%**).
        한 건에 40~90초를 들일 자리가 아니다.
      · **"다음은 몇 위부터"를 이어서 넣던 방식을 그만둔다.** 262위
        `WLF00000098`에서 멈춘 백로그는 그대로 두고 다시 손대지 않는다.
- [x] **09-10 색인 작업 — 구글 3건 요청 · 빙 100건 제출. 새 기준의 첫 적용.**

      ── 구글: 안내 글 20편을 전수 검사했다 ─────────────────────────
      조회수 백로그 대신 **우리가 직접 쓴 글**만 봤다. 20편 중 **18편이 이미
      색인**이었고 요청은 2건뿐이다(`/guide/online` · `/guide/popular`).
      홈 `/`은 어제 배포로 내용이 바뀌어(히어로 축소 + `/ask`) **재요청**했다.
      **합계 3건 요청 · 18건 건너뜀 · 할당량 안 걸림.**
      · **09-07~09-09에 요청한 여덟 편이 전부 색인됐다** — calendar ·
        tax-credit · pay-dates · unemployment · emergency · k-pass ·
        tax-credit-amount · youth-savings. 뒤의 셋은 **어제 요청해 하루 만에**
        들어갔다. 좋은 글은 요청하면 빨리 붙는다.
      · 그런데 **한 번도 요청 안 한 아홉 편도 이미 색인돼 있었다**(apply ·
        documents · income-line · region · mistakes · terms · life-stage ·
        baby-money · livelihood + `/guide` 인덱스). 사이트맵만으로 붙는다.
        → **글은 색인이 잘 된다. 문제는 글이 아니라 얇은 상세다.**
        이것이 새 기준을 뒷받침하는 실측이다.

      ── 빙: 상세 아닌 100건을 처음 넣었다 ────────────────────────
      **09-03~09-09에 넣은 700건이 전부 상세 페이지였다.** 안내 글 20 ·
      축 허브 68 · 정적 12 = **100건이 한 번도 안 갔다.** 오늘 그 100건을
      넣었다 — 할당량 100 → 0, 목록 총계 **700 → 800**.
      · **상세 백로그 7건(701~707위)은 새 기준대로 뺐다.** 조회수 맨 아래라
        넣어도 쌓이기만 한다. 남은 것은 `/income` 허브 7개뿐이고,
        이것도 급하지 않다.
      · 빙 할당량 문구가 이날은 `Quota will reset in 24 hrs`였다(09-08 3분,
        09-09 2시간). **문구는 그때그때 다르니 값(숫자)을 읽는다** — 제출 전
        100, 제출 후 0을 확인했다.

      ── 이번에 걸린 함정 둘 ──────────────────────────────────────
      · **판정 문구가 「URL이 Google에 등록되어 있음」이다.** "등록되어
        있습니다"로 찾다가 첫 건이 90초 timeout으로 죽었다. 「있지 않음」이
        붙는지로 갈라 읽는다 — `/URL이 Google에 등록되어[^\n|]*/`를 뽑아
        `있지 않음` 포함 여부를 본다. **화면 문구를 짐작해서 적지 말고
        한 건을 실제로 띄워 보고 그대로 옮긴다.**
      · **브라우저 창이 좁으면 GSC가 모바일 레이아웃으로 갈아탄다.**
        이날 창이 728px여서 검사창의 `width`가 **0**이 됐다(돋보기 뒤로 숨음).
        09-09에 적어 둔 그 함정이다. 좌표를 안 쓰더라도 **`resize_window`로
        1440px을 만들어 두고 시작한다** — 그러면 숨는 일이 없다.
        (끝나면 `preset: "desktop"`으로 되돌린다.)
      · 이미 색인된 페이지도 **내용이 바뀌었으면 재요청한다**(홈이 그 경우다).
        화면에 「페이지가 변경되었나요? 색인 생성 요청」이 따로 뜬다.
- [ ] 색인 요청 한도는 **고정된 수가 아니다.** 굴러가는 24시간 창이라
      전날 얼마나 썼는지에 따라 그날 들어가는 수가 달라진다. 실측: 09-03 3건,
      09-04 11건, 09-05 11건, 09-06 16건, **09-07 48건(할당량 안 걸림)**,
      09-08 25건, 09-09 오전 26 + 오후 15 = **41건(오후에 「할당량 초과」)**.
      "하루 11건"으로 외우면 09-06처럼 다섯 건을 남기고 그만두게 된다.
      · **09-07에 한도가 자릿수째로 달라졌다.** 48건을 넣도록 "할당량 초과"가
        한 번도 안 떴다. 앞선 나흘(3·11·11·16)과 같은 규모가 아니다. 그래서
        "끝까지 넣어 본다"는 규칙이 **끝이 없는 작업**이 됐다 — 한 건에 75초씩
        드니 48건에 한 시간이 걸렸다. 이제는 **넣을 값어치가 있는 데까지**만
        넣는다. 조회수 200위 아래는 사이트맵만으로도 붙고 있어 수동 요청의
        값어치가 거의 없다(아래 "이미 색인됨" 항목들 참고).
      · **요청 전에 URL 검사부터 한다.** 09-04에 상위 20건을 훑었더니 절반이
        이미 색인돼 있었다. 이미 색인된 건에 요청을 쓰면 그날 치가 사라진다.
      · 09-04 요청 11건: 이동통신요금감면·기초연금·유아학비·정신건강바우처·
        부모급여·초중고교육비·긴급복지생계·교육급여·기저귀조제분유·
        청소년특별지원·근로자녀장려금.
      · 09-05 요청 11건: 자산형성지원(WLF00000100)·생계급여·디딤씨앗통장·
        초중고교육정보화·자립준비청년자립수당·여성청소년생리용품·노인일자리·
        청년월세지원(WLF00004004)·장애인활동지원·장제급여·노인맞춤돌봄.
        12번째 WLF00003265 장애수당에서 할당량이 걸렸다. 뽑는 명령은 docs/02 3절.
      · 09-06 요청 16건(조회수 42~83위): 장애수당·긴급돌봄·20세이하운전면허학원비·
        스포츠강좌이용권·평생교육이용권·방과후학교자유수강권·학생교육비꿈드리미·
        백내장수술비·농식품바우처·독거노인장애인응급안전안심·의료급여틀니임플란트·
        개인채무조정·임신사전건강관리·경기도어린이청소년교통비·가사간병방문·
        긴급복지연료비전기요금.
        **17번째 WLF00000917 긴급복지 주거지원에서 할당량이 걸렸다.**
      · **09-07 요청 48건.** 새 글 세 편(`/guide/calendar`·`/guide/tax-credit`·
        `/guide/pay-dates`)을 먼저 넣고, WLF00000917부터 조회수 84~166위를
        훑어 45건을 더 넣었다. 같은 구간에서 **35건은 이미 색인돼 있어
        건너뛰었다** — 요청한 것보다 건너뛴 것이 더 많은 구간이 나오기 시작했다.
        **다음은 조회수 167위 WLF00005409(장애인 버스요금 지원)부터**다.
      · **다만 다음 회차는 조회수 순서보다 먼저 넣을 것이 다섯 있다.**
        전부 **내용이 바뀌었는데 사이트맵 lastmod가 안 움직인** 것들이라,
        수동 요청을 안 하면 구글이 늦게 온다. 상세가 전부 `SERVICES_UPDATED`를
        쓰는 대가다(800개를 오늘로 미는 것보다 낫다).

        | URL | 무엇이 바뀌었나 |
        |---|---|
        | ~~`/service/WLF00005440`~~ | 제목이 「…(모두의카드·K-패스)」로 — 09-08 완료 |
        | ~~`/service/WLF00000092`~~ | 제목이 「…(기저귀바우처)」로 — 09-08 완료 |
        | ~~`/service/WLF00006266`~~ | "6월 예정" 지남 띠 — 09-08 완료 |
        | ~~`/service/WLF00000055`~~ | 제목이 「통합문화이용권(문화누리카드)」로 — 09-08 완료 |
        | ~~`/guide/unemployment`~~ | 새 글 — 09-08 완료 (09-10 색인 확인) |
        | ~~`/guide/pay-dates`~~ | 15일이 들어감 — 09-08 완료 (09-10 색인 확인) |
        | ~~`/guide/k-pass`~~ | 09-09 요청 완료 (09-10 색인 확인) |
        | ~~`/guide/tax-credit-amount`~~ | 09-09 요청 완료 (09-10 색인 확인) |
        | ~~`/guide/youth-savings`~~ | 09-09 요청 완료 (09-10 색인 확인) |
        | ~~`/guide/emergency`~~ | 09-09 요청 완료 (09-10 색인 확인) |

        **이 표는 전부 끝났다.** ⚠ 위 여섯 줄은 09-08에 넣고도 취소선을 안 그어
        둬서, **09-10에 "아직 안 넣은 것"으로 읽고 오늘 목록에 또 올릴 뻔했다.**
        같은 자리가 두 번 읽히는 표는 넣은 날 바로 지운다.
        조회수 백로그(167위 `WLF00005409`~)는 09-10에 **그만뒀다** —
        아래 「색인 요청 기준을 바꿨다」.
      · **빙은 이번엔 재제출이 필요 없다.** URL 수가 804 그대로다(추가 0 ·
        삭제 0 · lastmod 0). 빙 URL 제출은 예정대로 **501~600위**를 넣는다.
      · **09-07에 한 건이 「색인 생성 요청이 거부됨」으로 돌아왔다** —
        WLF00005448 다문화가족 자녀 교육활동비. "실시간 테스트 중에 URL에서
        색인 생성 문제가 감지되었습니다." 사이트가 아니라 **내 후보 목록이
        틀렸다.** 그 페이지는 noindex이고 사이트맵에도 없다.
        원인은 docs/02 3절의 뽑는 명령이 **상투 문구를 길이에서 안 뺀
        옛 규칙**이라서다(그 명령은 713건, 실제 색인 대상은 707건).
        **후보를 뽑을 때는 `types/welfare.ts`의 `bodyText`와 같은 규칙을
        쓴다** — `isCriteriaBoilerplate`인 selectionCriteria는 빼고 센다.
        뽑은 뒤 라이브 사이트맵과 대조하면 이 실수는 한 번에 걸린다.
      · 09-06에 검사만 하고 건너뛴 것(이미 색인됨) 15건: 국민취업지원제도·
        의료급여본인부담면제·의료급여·암검진·연장형보육료·주거안정월세대출·
        긴급복지의료·행복주택·버팀목전세·재난적의료비·대중교통비환급·
        차상위본인부담경감·청년월세(5691)·발달재활·고위험임산부 외.
        **조회수 42~85위를 훑어 절반 넘게 이미 들어와 있었다.** 사이트맵만으로
        색인이 붙고 있다는 뜻이고, 수동 요청의 값어치는 그만큼 줄어든다.
      · **판정과 클릭을 한 번에 끝내는 법.** 좌표로 「색인 생성 요청」을 누르면
        이미 색인된 페이지에서는 버튼 위치가 달라 헛클릭이 난다. 본문에서
        "등록되어 있지 않음"을 확인한 뒤에만 누르도록 자바스크립트 한 줄로
        묶는다 — `버튼.click()`은 실제로 요청이 들어간다(09-06 확인).
      · 09-05에 검사만 하고 건너뛴 것(이미 색인됨): 한부모아동양육비·
        자활성공지원금·의료급여임신출산·통합문화이용권·과학문화바우처·
        일상돌봄·양곡할인·자활근로. 조회수 19~46위를 훑어 절반이 이미
        들어와 있었다 — 사이트맵만으로 색인되고 있다는 뜻이다.
      · 스크린샷 좌표를 믿지 말 것. 09-04에 GSC 캡처가 깨져 화면이 축소된 것처럼
        보였다(실제 뷰포트는 1910px). `k = 캡처 프레임 폭 / innerWidth`로 DOM
        사각형을 환산해 누른다. **프레임 폭은 고정값이 아니다** — 09-05에는
        1287로 시작해 세션 도중 1314로 바뀌었다. 좌표가 한 번 빗나가면 상수를
        의심하지 말고 다시 잰다. 클릭 뒤에는 본문 텍스트로 "색인 생성 요청됨"을
        확인한다.
      · **검사창은 `ctrl+a`로 안 지워진다.** 09-05에 ctrl+a가 글자 `a`로
        들어가 `ahttps://...`가 됐고, 엔터를 눌러도 앞 URL 결과가 그대로
        떠 있었다. 그 화면을 새 URL의 결과로 읽으면 **엉뚱한 판정 위에
        "다시 요청"을 누르게 된다.** `triple_click`으로 줄을 통째로 잡고
        덮어쓴다. 그리고 판정 전에 **화면에 찍힌 URL이 방금 넣은 것인지**
        본문에서 정규식으로 확인한다.
      · 요청 뒤 대화상자는 **닫기로 닫는다.** Return을 누르면 포커스가 남은
        "다시 요청"이 눌려 할당량이 한 건 날아간다.
- [ ] 빙은 **URL 제출이 하루 100건**이고 구글 한도와 완전히 별개다.
      · 09-03 상위 1~100위, 09-04 101~200위, 09-05 201~300위,
        09-06 301~400위, 09-07 401~500위, **09-08 501~600위** 제출
        (매번 잔여 할당량 0으로 확인). 09-08 제출 뒤 목록 총계가
        `1 — 25 of 600`. **09-09 오후에 601~700위 100건 제출** —
        「성공: 100개의 URL을 제출하였습니다」, 총계 `1 — 25 700의`,
        할당량 100 → 0. **09-10에 상세 아닌 100건을 넣어 총계 800이 됐다**(아래).
      · **초기화 문구를 값으로 읽고 다시 와 보면 된다.** 09-09 아침에
        `2 hrs`라 그날 몫으로 남겼는데, 오후에 다시 열어 보니 100으로
        차 있었다. 기다릴 만한 문구면 하루를 버리지 않는다.
      · **09-09에는 초기화가 2시간이었다.** 할당량 0으로 시작한 것은 09-08과
        같은데 화면 문구가 `Quota will reset in **2 hrs**`였다(09-08은 3분).
        **초기화까지 얼마인지는 그때그때 다르다** — 문구를 값으로 읽고
        기다릴 만하면 기다리고, 아니면 그날 몫으로 남긴다.
        09-09분(601~700위)은 **같은 날 오후에 넣었다**(위 항목).
      · **목록은 docs/02 3절 명령을 그대로 쓰지 말 것.** 그 명령은 상투 문구를
        본문 길이에서 안 빼는 옛 규칙이라 색인 대상이 아닌 URL이 섞인다
        (713 대 707). `types/welfare.ts`의 `bodyText`와 같게 세고, 뽑은 뒤
        **라이브 사이트맵에 전부 있는지 대조한다**(09-07에 100건 전부 확인).
      · 제출 전에 **`Quota left for today`를 값으로 읽는다.** 09-07엔 100으로
        시작해 제출 뒤 0이 됐다. 넣기 전에 이미 0이면 그날은 끝난 것이다.
      · 빙은 배포로 URL이 늘어도 **사이트맵을 스스로 다시 읽지 않는다.**
        9/1자 556개 → (재제출) 669개 → 804개에서 또 멈춰 있었다(라이브는 806).
        **수집·배포 뒤에는 빙 사이트맵을 반드시 다시 제출한다.**
        09-06 재제출 뒤 806개로 라이브와 일치. Status는 `Processing`으로 바뀌었다.
      · **같은 날 두 번째 재제출(09-06).** 그날 두 번째 배포로 라이브가 803이
        됐는데(상투 문구 정리로 6건 빠지고 새 글 1편 추가) 빙은 806에 멈춰
        있었다. `⋮ → Re-submit` 뒤 **Last submit이 9/6으로, Status가
        `Processing`으로** 바뀌는 것을 값으로 확인했다.
        `URLs discovered`는 빙이 다시 긁은 뒤에 바뀐다 — 누른 직후에는 옛 수가
        그대로 806으로 남아 있으니 그걸 실패로 읽지 말 것.
      · 상단 통계가 `Known sitemaps 2`인데 표에는 행이 하나뿐이다(필터는 전부
        All). 예전 `bokjimap.co.kr` 사이트가 계정에 남은 것과 관련일 수 있다.
        사이트 삭제는 되돌릴 수 없어 사용자만 한다.
      · 재제출은 사이트맵 행 오른쪽 **`⋮` → Re-submit**이다. 상단의
        "Submit sitemap" 단추는 새 사이트맵을 등록하는 자리라 눌러도
        패널이 안 열릴 때가 있다. 누른 뒤 **Status가 `Processing`으로
        바뀌는지** 값으로 확인한다(09-05 확인).
      · 제출창은 리액트 제어 필드라 키 입력이 안 들어간다.
        `ta.focus(); ta.select(); document.execCommand("insertText", ...)`로
        넣는다(09-05엔 마우스 클릭 없이 이것만으로 들어갔다). 그리고 **넣은
        값을 다시 읽어 줄 수·첫 줄·끝 줄을 확인한다.** 툴의 "Typed ..." 성공
        반환은 근거가 못 된다.
      · 제출 패널의 Submit 단추는 `.ms-Panel.is-open` 안에 있다. 본문 전체에서
        `button`을 훑으면 안 잡힌다 — 패널을 먼저 찾고 그 안에서 고른다.
      · 빙에서는 `javascript_tool`이 쿼리스트링 때문에 차단될 때가 있다.
        `location.href` 같은 걸 반환하지 말고 필요한 값만 돌려받는다.

---

## 7. 작업 원칙

우선순위: **정확성 > 검증 > 최소 변경 > 명확성 > 유지보수성**

- 파일·API·스키마가 있다고 가정하지 말고 먼저 읽어서 확인한다.
- 요청된 작업에만 변경을 국한하고, 관련 없는 리팩토링은 하지 않는다.
- 검증(빌드·린트·타입·실제 화면) 없이 "됐다"고 말하지 않는다.
- 막히면 멈추고 무엇이 막혔는지, 무엇이 검증됐는지 명확히 보고한다.

### 검증 루틴 (데이터·기능 변경 후)

1. `npx tsc --noEmit` + `npx eslint src --max-warnings=0` + `npm run build`
2. 데이터 정합성 스크립트 (id 중복, 날짜 논리, 필수 필드 누락)
3. `node scripts/audit-sitemap.mjs` — **몇 개가 나갈 것인가**(소스의 규칙)
4. `node scripts/audit-indexability.mjs` — **그게 색인될 수 있는가**(빌드 결과물)
   · 사이트맵에 있는데 noindex, 자기를 안 가리키는 canonical, **제목·설명 중복**
   · 3번은 소스를, 4번은 렌더된 HTML을 본다. 규칙이 맞아도 HTML이 틀릴 수 있다.
5. 실제 화면 확인 (모바일 가로 스크롤 포함)

### 렌더된 HTML을 문자열로 검사할 때 (두 번 걸린 함정)

**검사 스크립트가 틀려서 멀쩡한 사이트를 "깨졌다"고 보고하는 일이 반복됐다.**
문장이 안 잡히면 사이트를 고치기 전에 **검사 쪽을 먼저 의심한다.**

- **React는 보간값 둘레에 `<!-- -->`를 넣는다.** `{SERVICES_UPDATED}에 받아`가
  HTML에서는 `<!-- -->2026-09-04<!-- -->에 받아`다. 그래서 `grep -F`가 실패하고,
  태그를 지우면 그 자리가 공백이 되어 `2026-09-04 에 받아`처럼 **없던 띄어쓰기가
  생긴다.** 태그를 지운 뒤 `.replace(/\s+/g," ")`로 접고, 정규식에도 ` ?`를 둔다.
  (09-06에 "5개 조문이 전부"와 "출처 줄"에서 각각 한 번씩 걸렸다.)
- **`&#x27;`·`&quot;`·`&amp;`는 버그가 아니다.** React가 `'`·`"`를 이스케이프한
  것이고 브라우저는 제대로 그린다. 깨진 글자를 찾을 때는 HTML이 아니라
  **innerText**를 보거나, 이름 엔티티(`&rarr;` `&rsquo;`)만 골라 센다.
- **bash `&&` 사슬은 `grep -c`가 0을 세면 끊긴다**(종료코드 1). 뒤 명령이
  조용히 안 돌아 **옛 파일을 그대로 읽고** 오판한다. 검사 스크립트는 사슬 대신
  줄을 나눠 쓴다.
