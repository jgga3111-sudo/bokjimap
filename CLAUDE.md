@AGENTS.md

# CLAUDE.md — 복지클릭(bokjiclick) 프로젝트 가이드

Claude Code가 이 프로젝트에서 작업할 때 세션 시작 시 자동으로 읽는 지침이다.
Next.js 16 학습데이터 불일치 경고는 `AGENTS.md`(위 import) 참조.

---

## 0. 러닝온과 절대 혼동하지 말 것 (최우선)

이 컴퓨터에는 **같은 사람이 만든 Next.js 사이트가 두 개** 있다. 스택이 거의
같아서 경로·설정·계정을 헷갈리기 쉽다. 아래 표를 먼저 확인하고 작업한다.

| 항목 | 러닝온 (건드리지 말 것) | 복지클릭 (이 프로젝트) |
|------|------------------------|---------------------|
| 폴더 | `C:\Users\jgga_\클로드코드\marathon-hub` | `C:\Users\jgga_\클로드코드\welfare-hub` |
| 도메인 | `runningon.co.kr` | `bokjiclick.co.kr` |
| GitHub | `jgga3111-sudo/runningon` | `jgga3111-sudo/bokjimap` |
| dev 포트 | 3000 | **3001** |
| 주제 | 마라톤·러닝 대회 | 복지·지원금 정보 |
| 계정 | — | 깃·버셀 `jgga3111`, 서치콘솔·빙·사이트 연락처 `jgga1234567` |
| 애드센스 | 승인됨 (`runningon93`) | **같은 계정에 사이트로 추가** — 1인 1계정이라 새로 못 만든다 |
| 인스타 | `@runningon_office` | 아직 없음 |
| Supabase | 사용 중 | **계정 기능에만 쓴다(09-11 결정, 아직 꺼 둠)** — 러닝온과 **다른 프로젝트** |
| 쿠팡 파트너스 | 사용 중(러닝화) | 아직 없음 |

### 사이트는 **셋**이다 — 복지클릭 · 러닝온 · 데이원코리아 (2026-09-11 사용자 당부)

같은 사람이 운영하는 사이트가 **셋**이고, 외부 서비스 화면에서 서로 붙어
보인다. 셋을 늘 **다른 사이트로 인식한다.** 지금 보는 화면이 셋 중 어느
것인지 **이름·도메인으로 먼저 확인하고** 손댄다.

| 사이트 | 도메인 | 겹쳐 보이는 자리 | 구분하는 법 |
|---|---|---|---|
| **복지클릭**(이 프로젝트) | `bokjiclick.co.kr` | — | Supabase 프로젝트 `bokjiclick`(`mngvwlhvnocpnrusmqho`) · Resend 키 `bokjiclick` · 보내는 사람 「복지클릭」 |
| 러닝온 | `runningon.co.kr` | 애드센스 계정 · 가비아 DNS 목록 · **Supabase SMTP 화면**(Gmail 「러닝온」) | 폴더 `marathon-hub` · 포트 3000 · Supabase는 다른 계정 |
| 데이원코리아 | `dayonekorea.co.kr` | **Resend 계정(`jgga1234567`)** · 가비아 DNS 목록 | Resend 키 `dayonekorea` · 보내는 사람 「Day One Korea」 |

- **남의 사이트 설정은 읽기만 한다.** 러닝온·데이원코리아의 도메인·키·DNS·
  메일 기록은 복지클릭 작업 중에 바꾸지 않는다(09-11 가비아에서도 복지클릭
  줄의 「설정」만 눌렀다).
- 09-11에 실제로 헷갈릴 뻔한 자리 둘 — 사용자가 **러닝온 Supabase의 SMTP**를
  보고 복지클릭 것으로 읽었고, Resend에는 **데이원코리아 도메인이 먼저** 있었다.

### 계정 셋은 **역할로** 기억한다 (2026-09-09 사용자 확정)

이름이 셋 다 비슷해서(`jgga3111` · `jgga1234567` · `runningon93`) 한 줄로
외우면 반드시 헷갈린다. **09-09에 실제로 애드센스 계정을 뒤바꿔 적었다가
되돌렸다.** 이름이 아니라 **하는 일**로 붙여 둔다.

| 하는 일 | 계정 | 어디서 쓰나 |
|---|---|---|
| **코드를 올린다** | `jgga3111@gmail.com` | 깃 커밋 작성자 · GitHub · Vercel · Supabase(09-11 추가) |
| **검색엔진에 말한다** | `jgga1234567@gmail.com` | 구글 서치콘솔 · 빙 웹마스터 · 사이트 연락처 |
| **광고비를 받는다** | `runningon93@gmail.com` | 애드센스 `pub-3333691556845206` |

- **애드센스만 러닝온과 같은 계정이다.** 1인 1계정이라 새로 못 만들어서,
  러닝온으로 이미 승인받은 그 계정에 복지클릭을 **사이트로 추가**했다.
  나머지 둘은 복지클릭 전용이다.
- **Resend(인증 메일 발송)도 예외다 — `jgga1234567` 계정을 쓴다**(2026-09-11
  사용자 결정). 역할로는 「코드를 올린다」(jgga3111)에 들어갈 자리인데, 이미
  그 계정에 **`dayonekorea.co.kr`**(다른 사이트, 복지클릭·러닝온 아님)이 인증돼
  있었고 사용자가 그 계정에 복지클릭을 **더하기로** 정했다. Resend 화면에서
  도메인이 둘 보이는 것은 정상이다 — **dayonekorea 쪽 도메인·키·메일 기록은
  건드리지 않는다.** API 키는 도메인별로 권한을 좁혀(`Sending access` +
  `bokjiclick.co.kr`만) 만든다.
- 구글 화면은 주소에 **`?authuser=<이메일>`**을 붙인다. 크롬 기본 계정이
  `jgga3111`이라 그냥 열면 서치콘솔에서 "이 속성에 액세스할 수 없습니다"가
  뜬다. **계정 번호(`/u/N/`)는 로그인 순서 따라 바뀌니 외우지 않는다.**
- ⚠ **이 표를 말 한마디로 고치지 않는다.** 사람도 헷갈리는 자리다.
  기록과 다른 말을 들으면 **먼저 되물어 확인하고** 고친다.

**금지 사항**
- 러닝온 폴더의 파일을 이 프로젝트로 **복사하지 않는다.** 참고만 하고 새로 쓴다.
  (러닝온 CLAUDE.md에는 인스타 계정·`PROMO_SECRET`·쿠팡 API 한도가 박혀 있어
  그대로 옮기면 두 사이트 설정이 섞인다.)
- 이 프로젝트에서 러닝온 저장소에 커밋·푸시하지 않는다. 반대도 마찬가지.
- 러닝온 데이터(`races.json`, `courses.ts`, `shoes.ts`)를 참조하지 않는다.

**러닝온에서 가져와도 되는 것**: 설계 패턴과 교훈뿐이다(4절 참조). 코드는
읽고 이해한 뒤 이 프로젝트에 맞게 새로 작성한다.

---

## 1. 프로젝트 개요

- **이름:** 복지클릭 (bokjiclick) — 전국 복지·지원금 정보 사이트
- **주소:** https://bokjiclick.co.kr (2026-09-01 연결 완료)

  ⚠ 처음 이름은 **복지맵**이었다. 배포 직후 `bokjimap.com`이 같은 주제로
  이미 운영 중(2026-02 개설, 애드센스 게재)인 것을 발견해 이름을 바꿨다.
  가비아 **도메인 이름 변경**으로 추가 비용 없이 옮겼다(등록 후 7일 이내만
  가능한 기능이라 운이 좋았다). 교훈은 docs/03-경쟁.md 6절에 적어 두었다.
  코드·문서에 `bokjimap`이 남아 있으면 잔재이니 고친다.
- **기술 스택:** Next.js 16.3.3 (App Router, Turbopack) · React 19.2.8 ·
  TypeScript · Tailwind CSS 4
- **저장소:** GitHub `jgga3111-sudo/bokjimap` (main 브랜치 — 저장소 이름은
  도메인을 바꾼 뒤에도 그대로 두었다. 리네임하면 Vercel 연결이 끊긴다)
- **호스팅:** Vercel (프로젝트 `bokjimap`)

### 명령어

| 용도 | 명령 |
|------|------|
| 개발 서버 | `npm run dev` (포트 **3001**) |
| 빌드 | `npm run build` |
| 린트 | `npx eslint src --max-warnings=0` |
| 타입 검사 | `npx tsc --noEmit` |

---

## 2. 로컬 환경

- **OS:** Windows 11 / **셸:** PowerShell(주) + Bash(POSIX 스크립트용)
- **작업 디렉토리:** `C:\Users\jgga_\클로드코드\welfare-hub`
- **경로 주의:** 폴더명 `클로드코드`는 한글이다. 직접 타이핑하지 말고 복사해서 쓸 것.
- **Node가 PATH에 없다.** `C:\Program Files\nodejs`에 설치돼 있으나 세션 PATH에는
  안 잡힌다. Bash에서 `npx: command not found`가 나면 PowerShell로 아래처럼 실행한다.
  ```powershell
  $env:Path = "C:\Program Files\nodejs;" + $env:Path
  ```
- **git 사용자는 저장소 로컬 설정으로 고정**되어 있다(`fccls <jgga3111@gmail.com>`).
  전역 설정은 비어 있으니 건드리지 않는다. 다른 이메일로 커밋하면 Vercel이
  Blocked 처리한 전례가 러닝온에 있다.

---

## 3. 데이터 원칙 (이 프로젝트의 핵심)

지원금 정보는 사람의 신청·금전에 직접 영향을 준다. 정확성이 최우선이다.
이 영역은 AI 양산 블로그가 틀린 정보를 도배하는 곳이라, **정확성 자체가 이
사이트의 경쟁력**이다.

- **확인 못 하면 null.** 추측하지 않는다. 빈칸이 부실해 보여도, 틀린 값으로
  채워 사람을 헛걸음시키거나 신청 기회를 놓치게 하는 것보다 낫다.
- **작년 공고를 올해로 옮기지 않는다.** 지원금은 매년 금액·요건이 바뀐다.
  기준연도를 반드시 대조한다.
- **여러 출처가 엇갈리면 공식(지자체 공고·복지로)을 따른다.** 어떻게 엇갈렸는지 기록한다.
- **금액·소득기준·연령제한은 원문 표현을 그대로 옮긴다.** 임의로 반올림하거나
  요약하지 않는다.
- **`verifiedAt`(정보 확인일)을 항목마다 남긴다.**
- **마감일이 지난 공고는 숨기지 말고 "마감"으로 표기**한다. 내년 재공고를 찾는
  검색 수요가 있다.
- 법령 해석이나 수급 자격 판정을 하지 않는다. 안내와 링크까지가 이 사이트의 역할이다.

---

## 4. 러닝온에서 승계할 설계 패턴

러닝온 사이트맵 실측(1,189 URL) 분석에서 나온 것들이다. **코드 복사가 아니라
패턴만 가져온다.**

- **수집형 구조.** 러닝온은 1,189 URL 중 95%가 "실존 개체 1개 = URL 1개"였다.
  글을 쓰는 게 아니라 개체를 모아 페이지를 편다.
- **축 조합으로 URL 증폭 + 하한선.** 러닝온은 지역×시기×종목 조합 랜딩을 만들되
  `MIN_RACES = 3` 미만 조합은 만들지 않았다. 복지맵도 `MIN_SERVICES` 하한을 둔다.
  얇은 페이지 양산은 저품질 신호다.
- **sitemap의 lastmod를 나눠 쓴다.** 전 URL이 같은 날짜로 나가면 구글이 통째로
  무시한다. 매일 바뀌는 화면만 오늘 날짜, 나머지는 실제 수정일.
- **네이버 사이트맵 파서는 날짜만(YYYY-MM-DD) 읽는다.** ISO 밀리초를 넣으면 500 에러.
- **얇은 페이지는 스스로 noindex하거나 지운다.** 러닝온은 본문 160자짜리 도구
  페이지를 noindex 처리했고, 얇은 제휴 페이지 241개는 아예 삭제했다.
  사유: *"애드센스는 색인 여부와 무관하게 사이트 전체를 검토한다."*

---

## 5. 데이터 소스

| 소스 | 링크 | 상태 |
|---|---|---|
| 중앙부처복지서비스 API | data.go.kr/data/15090532 | 자동승인 |
| 지자체복지서비스 API | data.go.kr/data/15108347 | 자동승인 |
| 복지서비스정보 (파일) | data.go.kr/data/15083323 | 367행, 연 1회 갱신 |

- 이용허락범위 **제한 없음** = 상업적 이용 가능(애드센스 게재 OK)
- 개발계정 **1,000건/일** 한도. 정적 빌드로 소화한다.
- **미측정**: 지자체복지서비스 실제 건수·필드 목록. 인증키 발급 후 첫 작업.
  이 수치가 나오기 전에는 URL 총량을 확정하지 않는다.

### 중앙과 지자체는 주는 필드가 다르다 (헛다리 방지)

빈칸을 세다 보면 **큰 구멍처럼 보이는데 구멍이 아닌 것**이 있다. 09-07에
"신청 방법이 비어 있는 사업이 전체 조회수의 90%"라는 수치를 보고 결함인 줄
알았다. 아니었다 — **중앙부처 330건 전부**이고, 중앙 API는 그 필드를 아예
안 보낸다. 대신 `applmetList`(신청 절차)를 보내고 그쪽은 330건 전부 차 있다.

| 필드 | 중앙 330 | 지자체 570 |
|---|---|---|
| `applyMethod`·`applyMethods` (신청 방법) | **전부 없음** | 대부분 있음 |
| `applySteps`·`outline`·`baseYear` | 전부 있음 | **전부 없음** |
| `updatedAt` (원문 최종수정일) | **전부 없음** | 있음 |

빈칸을 셀 때는 **먼저 provider로 갈라 본다.** 한쪽이 100%면 결함이 아니라
구조다. `guide/apply/page.tsx` 머리말에 같은 함정이 이미 적혀 있었다.

### 법령 원문을 받는 법 (2026-09-07 확인)

지급일·요율처럼 **공공데이터에 없는 값은 조문에 있다.** 그런데 국가법령정보센터
`law.go.kr/법령/<이름>`은 **프레임 껍데기**라 WebFetch로 열면 제목만 오고 조문이
안 온다(09-07에 두 번 헛다리를 짚었다). 화면 링크로는 그 주소가 맞으니 **읽는
주소와 거는 주소를 구분한다.**

전문은 공개 API로 받는다. 인증키 없이 `OC=test`로 열린다.

```bash
curl -s -A "<브라우저 UA>" \
  "https://www.law.go.kr/DRF/lawService.do?OC=test&target=law&type=JSON&LM=$(urlencode '고용보험법')"
```

- `법령.기본정보.시행일자`가 **지금 시행 중인 판**이다. 이걸 같이 적어 둔다.
- `법령.조문.조문단위[].조문내용 / 항[].항내용 / 호[].호내용`에 본문이 있다.
- `법령.별표.별표단위[].별표내용`은 **괘선 문자로 그린 표**다. 고용보험법 별표1
  (소정급여일수)이 여기 있다. 눈으로 옮겨 적는다.
- **여러 법을 한꺼번에 받아 정규식으로 훑는 쪽이 훨씬 촘촘하다.** `매월\s*\d{1,2}일`
  하나로 09-07에 다섯 건을 새로 찾았다(장애수당·장애아동수당·보훈 셋).
  한 조씩 눈으로 찾던 09-06보다 나았다.
- **준용 규정을 놓치지 말 것.** 독립유공자 보훈급여금 지급일은 그 법 시행령에
  없고, 제9조제3항이 국가유공자법 시행령 제29조를 **준용**한다고만 적혀 있다.
  "조문 없음"으로 판정하기 전에 준용을 찾아본다.
- 조문이 있어도 **수록 사업이 없으면 싣지 않는다**(5·18 학습보조비 4/15·10/15).

---

## 6. 배포

**Vercel Pro 사용 (2026-08-31 사용자 확정).**

사유: Vercel Fair Use Guidelines(2026-07-29)상 **Hobby 플랜은 비상업적 개인
용도 한정**이고, 상업적 사용 예시에 *"The inclusion of advertisements, including
but not limited to online advertising platforms like Google AdSense"*가 명시돼
있다. 애드센스를 달 사이트는 Hobby로 운영할 수 없다.

Pro는 **팀 단위 과금($20/월)**이라 같은 팀에 있는 러닝온·복지맵이 함께 커버된다.
프로젝트마다 따로 결제하지 않는다.

### 배포 규칙
- 배포는 `git push origin main`으로만 한다. Vercel CLI로 직접 배포하지 않는다.
- **커밋 작성자는 반드시 `fccls <jgga3111@gmail.com>`.** 다른 이메일로 커밋하면
  Vercel이 Blocked 처리해 배포가 멈춘다(러닝온에서 세 커밋이 막힌 전례).
  이 저장소는 `git config --local`로 고정해 두었다. 전역 설정은 건드리지 않는다.
- push 후 "배포 완료"라고 말하기 전에 **실제 사이트에서 반영 여부를 확인**한다.
  커스텀 도메인 연결 후에는 `curl -sL`로 커스텀 도메인을 대상으로 확인한다
  (리다이렉트를 안 따라가면 껍데기만 보고 오판한다).

### 완료 (2026-09-01)
- [x] Vercel 프로젝트 생성 및 GitHub 연결
- [x] `bokjiclick.co.kr` 연결 — apex가 Production, `www`는 apex로 308
- [x] 구글 서치콘솔·빙 웹마스터 등록 및 사이트맵 제출

### 남은 일
- [x] **수집 완료 (2026-09-05) — 상세 5,219건 전량**(중앙 461 · 지자체 4,758).
      마지막 976건(중앙 61 / 지자체 915)을 실패 0으로 받았다.
      · **수록은 900건 그대로다.** 마지막 976건은 전부 조회수 3,738위 아래라
        900 컷 바깥이고, `build-data.mjs 900`을 다시 돌려도 바뀌는 것은
        생성 날짜 두 줄뿐이었다. 그 두 줄을 커밋하면 **사이트맵 lastmod가
        900개 페이지에서 오늘로 밀린다** — 내용은 그대로인데 전부 고친 것처럼
        보이는 거짓 신호라 되돌렸다. 4절의 "lastmod를 나눠 쓴다"가 여기 걸린다.
      · 수록을 900에서 올릴지는 **서치콘솔 색인 보고서를 보고** 정한다.
        그때 필요한 원문은 이제 전부 로컬에 있다(`data-research/detail-raw/`).
      · **09-06에 확대의 값어치를 재 봤다. 낮다.** 조회수 집중도가 극단적이다 —
        상위 10건이 전체 조회수의 52.4%, 상위 50건 79.8%, **상위 100건 86.1%**,
        상위 300건 93.6%. 900을 1,500으로 올려도 새로 담기는 것은 3,738위
        아래뿐이라 트래픽 기여가 거의 없고 얇은 페이지만 600개 는다.
        **늘리는 것보다 상위 100건을 깊게 파는 쪽이 같은 노력으로 훨씬 크다.**
      · 수집이 늘면 **글의 숫자가 조용히 바뀐다.** 돌린 뒤 `/guide` 열두 편의
        수치가 본문·metadata 양쪽에서 계산값인지 확인한다(2026-09-02에 실제로
        metadata만 상수로 남아 191건이라고 나가고 있었다).
- [ ] 수집 완료 후 `MIN_BODY_LENGTH` 기준선 재확인 (types/welfare.ts 주석 참고)
      · noindex 303 → 215 → 187 → **193건**(색인대상 707건 / 사이트맵 803개,
        09-06 실측). 목록에 링크된 채 크롤링만 당하고 버려지는 몫이다. 링크를
        끊지 말고 본문을 채운다 — 수집이 늘 때마다 저절로 넘어왔다(88건, 28건).
        09-06에 6건 **늘어난** 것은 데이터가 얇아져서가 아니라 상투 문구를
        본문 길이에서 뺐기 때문이다(바로 아래 항목).
      · **09-07에 구글이 "'NOINDEX' 태그에 의해 제외되었습니다"를 메일로 보냈다.
        고칠 것 없다 — 우리가 일부러 한 것이다.** 걸린 46건을 전수 대조했다.

        | | 건수 | 판정 |
        |---|---|---|
        | 지금도 noindex · 사이트맵에도 없음 | 38 | 의도대로 ✓ |
        | 지금은 noindex 아님 · 사이트맵에 있음 | 8 | 이미 고쳐졌고 구글이 안 다시 긁었을 뿐 |

        뒤의 8건(장애입양아동 의료비지원·산재근로자 보험급여 등)은 수집이
        늘면서 본문이 300자를 넘어 색인 대상으로 넘어온 것들이다. **구글
        보고서는 마지막 크롤링 시점의 상태**라 옛 판정이 남아 있다.
        조회수 669~888위라 수동 요청을 쓸 자리가 아니고, 사이트맵으로
        다시 긁히면 저절로 풀린다.
        · **이 메일이 다시 오면 46건이 아니라 "사이트맵에 있으면서 noindex인
          건"이 몇인지를 센다.** 그 수가 0이 아니면 그때가 진짜 결함이다
          (`audit-indexability.mjs`가 보는 것과 같은 조건).
- [x] **상투 문구 선정기준 149건 정리 (2026-09-06).** 원본 API가 선정기준 자리에
      "지원대상의 내용을 참고해주시기 바랍니다."만 보내는 사업이다. 그 149건이
      가져가는 복지로 조회수가 **전체의 30.8%** — 가장 많이 여는 페이지의 한 칸이
      아무 말도 안 하고 있었다. 절과 목차 칩을 빼고 지원 대상 밑 한 줄로 바꿨다.
      "선정 기준을 두지 않는 사업"이라고는 쓰지 않는다. 그건 제도 해석이다.
      · 판정은 **40자 이하**로 제한한다. 한부모가족처럼 그 문장 뒤에 실제 내용이
        이어지는 108자짜리가 있는데, 그건 지우면 안 되는 본문이다.
      · **규칙이 두 군데 있다** — `types/welfare.ts`의 `isCriteriaBoilerplate`와
        `scripts/audit-sitemap.mjs`의 사본. 한쪽만 고치면 소스는 713이라 하고
        빌드 결과는 707이라 하는 상태가 된다(09-06에 실제로 그랬다).
- [x] **지난 신청 기간에 띠 달기 9건 (2026-09-06)** — `src/lib/applyPeriod.ts`.
      조회수 1위 청년내일저축계좌 요약문이 "(2026년 모집기간: '26.5.4.~'26.5.20.)"
      으로 끝나는데 9월에도 끝났다는 말이 없었다. `applyEnd`는 570건 중 554건이
      `9999-12-31`이라 **날짜가 필드가 아니라 문장 안에** 있다.
      · **기준일을 마감일로 읽지 않는 것이 핵심이다.** 범위 표현이어야 하고, 그
        앞 40자에 신청·모집·접수·공모·응모 + 기간/기한이 붙어 있어야 한다.
        이 조건이 없으면 대상포진의 "2026. 1. 1. 이후 출생"과 희망두배의
        "기준기간: 2025.6.1.~2026.5.31."이 걸려 들어온다.
      · 지났는지는 **브라우저가 판정한다.** 빌드에서 정하면 빌드한 날이 정적
        HTML에 굳어, 배포를 안 하는 동안 조용히 틀려 간다(`ThisMonth`와 같은 함정).
- [x] **"지급일"이 통째로 빠져 있었다 (2026-09-06).** 데이터랩에서 "근로장려금
      지급일" **한 낱말**이 "청년월세·청년월세지원·청년월세 신청" 셋을 합친 것의
      3배였는데(26년 8월 51.1 대 11.8), `grep 지급일 src`은 0건이었다.
      · 복지로 기초연금 상세를 열어 봤더니 소득인정액 계산식까지 빼곡한데
        **지급일 칸이 없다.** 조회수 상위 120건 본문에 "매월 N일"이 적힌 것도
        2건뿐이다. 지급일은 시행령·시행규칙에 있었다(`src/lib/payDates.ts`).
      · **데이터에 없는 것을 1차 출처에서 찾아 메우는 것** — 이게 복지로 원문을
        그대로 옮기는 것과 우리가 갈라지는 자리다(4절 docs/03 §4와 같은 결론).
      · 부모급여는 조회수 11위인데도 뺐다. 아동수당법에도 영유아보육법에도
        지급일 조문이 없다. 확인 못 한 것은 싣지 않는다(3절).
      · **09-07에 6건 → 11건으로 늘렸다.** 법령을 통째로 받아 `매월 N일`을
        기계로 훑었다(5절 「법령 원문을 받는 법」). **매월 15일이라는 무리가
        통째로 빠져 있었다** — 보훈 보상금·생활조정수당·참전명예수당.
        20일에는 장애수당(조회수 259,070)과 장애아동수당이 더 붙었다.
      · **글 제목에 숫자를 세우면 데이터가 늘 때 거기가 조용히 틀려진다.**
        "지원금은 20일과 25일에 들어옵니다"가 15일이 생기는 순간 틀린 말이
        됐다. 본문에도 "전부 같은 괄호를 달고 있습니다 — '…그 전날로 한다'"가
        있었는데 보훈 조문은 "…그 전날에 지급한다"로 문장이 다르다.
        **늘릴 때는 제목과 '전부/모두'라고 쓴 문장을 같이 본다.**
- [x] **통칭으로 검색하면 우리 페이지가 안 잡혔다 (2026-09-07)** —
      `src/lib/aliases.ts`. 조회수 294,386짜리 「통합문화이용권」을 사람들은
      **"문화누리카드"**로 찾는다. 데이터랩 월평균 45.6 대 0.14로 **320배**.
      본문엔 열 번 나오는데 제목·설명·h1에는 없었고, 헤더 색인이 이름만 보므로
      검색창에서는 **0건**이었다. 정작 `lib/calendar.ts`는 처음부터
      "문화누리카드(통합문화이용권)"로 적고 있었다.
      · **넣는 기준 둘.** ㉠ 원문이 함께 적어 놓은 이름일 것(원문에
        "통합문화이용권(문화누리카드)을 발급하여"가 그대로 있다) ㉡ 통칭
        검색량이 공식명을 넘을 것. 평생교육이용권은 공식명 13.8 > 통칭 7.9라
        넣지 않았다. **900건을 훑어 두 조건에 걸린 건 이 하나뿐이다.**
        목록이 길어지면 기준이 느슨해진 것이니 이 두 줄을 다시 읽는다.
      · 검색 두 갈래(`search.ts`·`searchFull.ts`)의 규칙을 **같이** 고쳐야
        한다. 한쪽만 고치면 드롭다운과 결과 페이지가 다른 답을 낸다.
      · **이 페이지는 내용이 바뀌었는데 사이트맵 lastmod가 안 움직인다.**
        상세는 전부 `SERVICES_UPDATED`를 쓰기 때문이다(800개를 오늘로 미는
        것보다 낫다). 대신 **다음 색인 요청 때 이 URL을 맨 앞에 넣는다.**
- [x] **별칭 기준을 한 번 넓혔다 — K-패스 (2026-09-07)** — `src/lib/aliases.ts`.
      조회수 58위(168,033) 국토교통부 사업의 제목이 「대중교통비 환급
      지원(모두의카드)」인데, 사람들이 치는 말은 **K패스**다. 데이터랩
      12개월 월평균 **K패스 12.7 대 모두의카드 3.7 — 3.5배**이고, 실업급여를
      1로 놓으면 K패스 2.4·문화누리카드 1.3이다(둘 다 글을 새로 쓸 만하다고
      판단했던 검색량보다 크다).
      · **문화누리카드보다 나빴다.** 그때는 0건이었는데 여기는 **틀린 1건**이
        나왔다 — "K패스"로 치면 강남구 교통비(203위), "K-패스"로 치면
        광주G-패스가 나오고 정작 본체는 안 나왔다. 본문에 그 낱말이 있는
        곁가지 지자체 사업이다. 0건이면 "여긴 없구나" 하고 다시 찾지만,
        틀린 1건은 "이 사이트엔 강남구 것만 있네" 하고 떠난다.
      · **기준 ㉠을 넓혔다.** 원래 "복지로 원문이 함께 적어 놓은 이름"이었는데
        복지로 본문에는 "K패스"가 한 글자도 없다. 병기하는 것은 소관부처다 —
        국토부 대광위에 「K-패스 사업」·「모두의카드」 페이지가 나란히 있고,
        정책브리핑은 *"그렇게 나온 것이 K-패스 '모두의 카드'다"*라고 쓴다.
        그래서 **"원문 또는 소관부처의 공식 표기"**로 읽는다. 넓힌 만큼
        근거는 **1차 출처여야 한다** — 블로그·위키는 근거가 못 된다.
      · **붙임표를 `norm`에서 지웠다**(`searchText.ts`). 공식은 「K-패스」,
        사람들은 「K패스」다. 붙임표 하나로 다른 낱말이 되면 공식 표기를
        그대로 실은 쪽이 오히려 안 잡힌다. 표기를 둘 적으면 제목이
        "(모두의카드·K-패스·K패스)"가 되어 읽기만 나빠진다.
      · 이름이 이미 괄호로 끝나면 **그 괄호 안에 잇는다**. 안 그러면
        "…(모두의카드)(K-패스)"가 된다.
      · 화면 이동경로도 별칭 이름으로 바꿨다. JSON-LD BreadcrumbList가 이미
        별칭을 내보내고 있어서 **구조화 데이터와 화면이 어긋나 있었다.**
      · **탈락시킨 후보를 같이 적어 둔다**(다시 후보로 올라오지 않게).
        청년미래적금 ✗ — 옛 이름 청년도약계좌(8.7)보다 **새 이름(24.7)이
        이미 2.8배** 더 검색된다. 노란우산공제 0.49 ✗ · 본인부담상한제
        0.18 ✗ · 늘봄학교 0.05 ✗ · 전세사기 지원 0.003 ✗ (실업급여=1 기준).
- [x] **별칭에 절대량 하한을 더했다 — 기저귀바우처 (2026-09-08)** —
      `src/lib/aliases.ts` 기준 ㉢. 검색량 큰 낱말 **43개를 라이브
      `/search?q=`에 실제로 쳐 보고** 0건인 것을 골랐다(이름을 눈으로
      훑지 않는다 — 다음에도 이 방법을 쓴다). 0건은 넷이었다.
      · **기저귀바우처만 넣었다.** 조회수 20위(1,026,518)인데 0건이었다.
        원문에 "기저귀 바우처 지원대상은 다음과 같습니다"가 그대로 있어
        **좁은 기준 ㉠도 통과**한다(K-패스처럼 넓힐 필요가 없었다).
        공식명("기저귀 조제분유" 0.003)과 자릿수가 셋 다르다.
      · **㉡에 구멍이 있었다.** 비율만 보니 둘 다 거의 0이어도 통과한다.
        「통신비감면」이 공식명보다 24배 많이 검색돼 ㉡을 통과하는데
        절대량은 실업급여의 **0.007배**다. 그래서 **㉢ 통칭 검색량이
        실업급여의 10분의 1 이상**을 더했다. 실업급여는 글을 한 편 새로
        쓸 값어치가 있다고 판단한 선이고, 별칭은 글보다 훨씬 싸다.
        실측: 문화누리카드 1.30 · K패스 2.43 · **기저귀바우처 0.113** ←
        여기까지. 아이사랑카드 0.023 · 마음투자 0.019 · 통신비감면 0.007 ✗
      · **43개가 전부 0건으로 나와 한 번 속았다.** 방금 브라우저에서 되는
        걸 확인한 문화누리카드까지 0이었다. `/search` 응답은 RSC가 아니라
        서버 렌더 HTML이고 React 주석이 숫자 사이에 낀다(`">17<!-- -->건`).
        **세는 쪽을 먼저 의심한다**(7절과 같은 함정, 오늘만 두 번째다).
      · 나머지 39개는 맨 위 결과가 제대로 나왔다 — 오늘 넣은 문화누리카드·
        K패스 포함. 검색은 대체로 건강하다.
- [x] **상위 100건을 다시 받아 봤다 — 본문은 한 글자도 안 바뀌었다 (2026-09-07).**
      "원본이 이미 고쳐졌으면 코드로 땜질할 이유가 없다"를 확인한 것이다.
      상위 100건은 **중앙 82 · 지자체 18**이라 중앙 일일한도(100)에 딱 든다.
      100건 성공·실패 0.
      · 38건이 달라 보였지만 **20건은 배열 순서, 18건은 조회수 카운터**뿐이었다.
        `lifeArray`·`trgterIndvdlArray` 같은 쉼표 목록의 **순서가 호출마다
        흔들린다**("저소득, 장애인" → "장애인, 저소득"). 내용은 같다.
      · **그래서 받은 원본을 되돌렸다.** 그대로 두면 다음 `build-data`가
        38개 페이지에 순서만 다른 diff를 만들고, `SERVICES_UPDATED`가 밀려
        **800개 lastmod가 전부 오늘로 간다.** 내용은 그대로인데 전부 고친
        것처럼 보이는 거짓 신호다(4절과 09-05에 되돌린 것과 같은 이유).
      · **재수집을 강제하려면 파일을 지워야 한다.** 스크립트가 "이미 받은
        servId는 건너뛴다"라서, 백업해 두고 지운 뒤 돌린다.
      · 결론: **상위 100건에 관해서는 우리 데이터가 최신이다.** 다음에 또
        의심스러우면 900건이 아니라 이 100건만 돌린다(한 시간이 아니라 몇 분).
- [x] **"2026년 6월 예정"이 9월까지 그대로 나가고 있었다 (2026-09-07)** —
      `statedPlan` in `src/lib/applyPeriod.ts`. 조회수 96위 청년미래적금
      (80,450) 요약문 끝이 그렇게 끝나는데, 위의 재수집으로 **원본이 아직
      안 고쳐진 것**을 확인했다. 기존 「지난 신청 기간」 띠는 `~`로 이어진
      **범위**만 봐서 단일 시점은 안 걸렸다.
      · **"예정"만 본다.** 처음엔 `예정|시행|출시|오픈`으로 훑어 6건을 잡았는데
        그중 다섯이 "2025년 10월부터 시행", "2016.07.01.시행" 같은 것이었다.
        **시행일이 지난 것은 정상이다** — 이미 시행 중이라는 뜻이지 문장이
        낡은 게 아니다. 띠를 달면 멀쩡한 사업이 문제 있어 보인다. 아직 안
        일어난 일을 가리키는 말은 "예정"뿐이다.
      · 끝날을 **그달 마지막 날**로 잡는다. "6월 예정"은 6월 15일에 안 지났다.
      · 900건 중 2건이 걸리고 **지금 뜨는 것은 1건**이다. 참전명예수당의
        "2026년 10월부터 월별 지급 예정"은 아직 미래라 안 뜨고, 11월이 되면
        **배포 없이 저절로 뜬다.** 판정을 브라우저에 맡긴 이유가 이것이다.
      · "아직 시작 안 됐습니다"라고 쓰지 않는다. 우리가 아는 것은 원문의
        시점이 지났다는 사실뿐이다(3절).
- [x] **원문 최종수정일을 확인일 옆으로 올렸다 (2026-09-07).** 여태 맨 아래
      「공식 안내」 상자에만 있었다. 상단에는 "2026-09-04 확인"만 있는데
      그건 **우리가 받아 온 날**이지 원문이 고쳐진 날이 아니다.
      · 수록 900건 중 **40건은 원문이 2025년 이전**이고 가장 오래된 것은
        **2022-06-29**(대구 현장체험학습비)이다. 4년 된 공고를 "9월에
        확인함"으로만 적어 두면 날짜로 거짓말을 하는 셈이 된다.
      · "오래됐습니다" 같은 **판정은 붙이지 않는다.** 며칠이 지나야 낡은
        것인지는 제도마다 다르다. 두 날짜를 나란히 두고 읽는 사람이 정한다.
      · 중앙부처 330건은 이 값이 아예 없어(API가 안 준다) 조각이 안 붙는다.
- [x] **수록 데이터에 없는 제도를 처음 실었다 — 실업급여 (2026-09-07)** —
      `/guide/unemployment`. 검색량이 문화누리카드급인데(월평균 35 대 45)
      이름에 "실업"이 든 사업이 **0건**이다. 고용보험 소관이라 복지로 API에
      애초에 안 들어온다.
      · 그래서 **집계 숫자가 한 줄도 없는 첫 글**이다. 근거가 전부 조문과
        정부 안내다. 이런 글을 또 쓸 때의 선은 이 글에 그어 두었다 —
        자격 판정 안 함(제58조 이직 사유는 아예 안 펼침), 곱한 값은
        **"조문에 없는 숫자입니다"라고 화면에 밝힘**(11만 3,500 × 60%),
        사람마다 다른 하한액은 **금액을 안 적음**.
      · **글을 새로 내면 `opengraph-image.tsx`를 같이 만든다.** 09-06에
        빠뜨려 두 편이 카톡에서 썸네일 없이 나갔던 그 함정에 또 걸릴 뻔했다.
- [x] **본문에 HTML 코드가 그대로 나가고 있었다 (2026-09-06).** `&rarr;`가
      화살표가 아니라 글자 그대로 찍혔다. 900건 중 **49건 · 148군데**이고,
      그 49건이 가져가는 조회수가 **전체의 28.3%**다 — 장애인연금 3위,
      주거급여 4위, 에너지바우처 5위, 기초연금 8위가 들어 있다.
      · 원인은 `build-data.mjs`가 **숫자 엔티티만 풀고 이름 엔티티는 아무도
        풀지 않아서**였다. `&#9312;`(①)는 2차 해독이 있었는데 `&rsquo;`
        `&times;` `&ldquo;` 같은 것은 통과해 버렸다. 기호 46종 표를 더했다
        (`SYMBOL_ENTITIES` / `decodeNamedSymbols`).
      · **`&amp; &lt; &gt; &quot; &apos;`는 그 표에 넣지 않는다.** 1차에서
        이미 풀었고 2차에서 또 풀면 원문에 없던 해독이 한 단계 더 일어난다.
        표에 있는 것은 전부 구조와 무관한 기호뿐이라 두 번 풀려도 안전하다.
      · **재생성할 때 받은 날짜를 인자로 준다** — `build-data.mjs 900 2026-09-04`.
        안 주면 `SERVICES_UPDATED`가 오늘로 밀리는데, 그러면 ① 상세 페이지가
        "오늘 받아 정리했다"고 **사실이 아닌 말**을 하고 ② 사이트맵 허브
        lastmod가 전부 오늘로 밀린다. 이번엔 09-04로 고정해 **라이브 사이트맵과
        803개 전부 동일**(날짜 변경 0건)임을 대조하고 배포했다.
      · 화면에 `&#x27;`이 보이는 건 버그가 아니다. React가 `'`를 이스케이프한
        것이고 브라우저는 제대로 그린다. **HTML이 아니라 innerText로 세야
        한다** — 09-06에 HTML만 보고 10쪽이 깨진 줄 알았다.
- [x] **크롤러 정책을 갈랐다 (2026-09-06)** — `src/app/robots.ts`.
      "크롤링을 막는다"는 이 사이트에서 두 갈래다. **검색엔진을 막으면
      사이트가 죽는다** — 검색이 유일한 유입 경로다.
      · 막은 것: AI 학습 24종(GPTBot·ClaudeBot·CCBot·Google-Extended·
        Bytespider·meta-externalagent…) + SEO 분석 11종(Ahrefs·Semrush·MJ12…).
        학습을 막아도 **구글 검색 순위에는 영향이 없다**(구글 명시).
      · 연 것: 검색엔진 전부, 카톡·페북 링크 미리보기(`facebookexternalhit`),
        **애드센스 크롤러(Mediapartners-Google)** — 막으면 광고가 안 붙는다.
        AI **검색**봇(OAI-SearchBot·ChatGPT-User·Claude-User·PerplexityBot)도
        열어 뒀다. 출처를 달고 사람을 보내 주는 쪽이라 막으면 손해다.
      · 판단 기준은 하나다 — **그 봇이 사람을 우리 사이트로 보내 주는가.**
        새 이름이 계속 생기니 이 기준으로 넣고 뺀다.
      · `FacebookBot`(학습)과 `facebookexternalhit`(미리보기)은 **다른 봇**이다.
        헷갈려 뒤엣것을 막으면 카톡·페북 공유 카드가 죽는다.
      · robots.txt는 **지키는 봇에게만** 통한다. 진짜로 요청을 끊으려면
        Vercel 방화벽(WAF)이 필요하다 — 대시보드 일이라 사용자가 한다. **미완.**
- [x] **CSP를 셋 더 걸었다 (2026-09-06)** — `base-uri 'self'` ·
      `form-action 'self'` · `object-src 'none'`. 넣기 전에 `<form>` 0건,
      `<iframe>·<object>·<embed>` 0건, `<base>` 0건을 확인했다.
      · `script-src`는 **여전히 안 넣는다.** 지금 넣으면 애드센스가 죽는다.
        nonce 미들웨어와 함께 손봐야 한다(next.config.ts 머리말).
      · `frame-src`도 건드리지 않는다. 광고 iframe을 직접 막는 자리다.
        CSP는 다른 출처 iframe **안으로 상속되지 않아** 위 셋은 광고와 무관하다.
- [x] **확인일을 상세 상단으로 올렸다 (2026-09-06).** 같은 값이 맨 아래
      「공식 안내」 상자에 있었는데, 375px에서 상위 상세는 5,400px이 넘어서
      **끝까지 내려간 사람만** 이게 언제 정보인지 알 수 있었다. 복지 정보는
      날짜가 없으면 맞는지 틀린지 판단할 수가 없다.
      · 첫 화면 수치 밑에도 출처 한 줄을 넣었다. 여태 푸터까지 내려가야
        나왔는데, 처음 온 사람이 "믿어도 되나"를 정하는 자리는 첫 화면이다.
      · 남아 있던 이모지 둘(🔍·📅)을 선 아이콘으로 바꿨다. **기기마다 다른
        그림이 나온다** — 헤더처럼 늘 떠 있는 자리가 사람마다 달라 보이면
        만들다 만 사이트로 읽힌다(Badge.tsx·display.ts가 이미 걷어낸 이유).
- [ ] **애드센스는 사용자가 직접 한다 (2026-09-10 확정).**
      **여기서 진행하지 않는다. 상태를 확인하겠다고 열지도, 매번 다시 묻지도
      않는다.** 사용자가 요청할 때만 손댄다. 계정이 러닝온과 같은
      `runningon93`이라 이 브라우저에 로그인돼 있지도 않다(바로 아래 pub 번호
      함정 참고). 아래 내용은 **참고 기록**이지 할 일 목록이 아니다.
- [ ] 애드센스 참고 — 사이트 등록·소유확인 완료, **승인 심사 「준비 중」**(09-07 확인,
      09-06과 같음. 상태 세부정보는 `—`, 최종 업데이트 09-05 09:49 KST 그대로)
      · **계정은 `runningon93@gmail.com`의 `pub-3333691556845206`이다**
        (2026-09-09 사용자 재확인).
        **애드센스는 1인 1계정**이라 새로 못 만든다. 그래서 러닝온으로 이미
        승인받은 그 계정에 복지클릭을 **사이트로 추가**한 것이다. 애드센스
        한 계정이 사이트를 여럿 갖는 건 정상이지만, 0절의 계정 분리와
        어긋나므로 **애드센스만은 예외임을 알고 있어야 한다.** jgga3111도
        jgga1234567도 이 계정에 접근 권한이 없다 —
        `?authuser=runningon93@gmail.com`.
        · ⚠ **`jgga1234567`에도 애드센스 계정이 따로 있다 — `pub-4973793162848744`.**
          2026-09-10에 `?authuser=runningon93@gmail.com`을 붙여 열었는데
          **파라미터가 안 먹고** 그 계정으로 들어갔다. 브라우저에 로그인된
          것이 `jgga1234567`뿐이면 애드센스는 **말없이 그 계정을 연다**
          (서치콘솔은 "액세스할 수 없습니다"라고 말해 주는데 여기는 안 그런다).
          화면이 「사이트 추가 · 필수」로 시작하는 **빈 온보딩**이라, 모르고 보면
          *"애드센스가 초기화됐다"*로 오판하기 딱 좋다. 실제로는 활성화도 안 된
          남의 빈 계정이다.
          → **애드센스를 열면 `pub-` 번호부터 확인한다.**
          `pub-3333691556845206`이 아니면 그 화면은 우리 것이 아니다.
          → **이 빈 계정에 사이트를 붙이거나 활성화하지 않는다.** 붙이면
          광고 코드가 엉뚱한 게시자 번호로 나간다.
        · ⚠ **09-09에 이 줄을 jgga3111로 고쳤다가 되돌렸다.** 세 계정이
          섞여 있어 사람도 헷갈리는 자리다. **셋을 한 줄로 외우지 말고
          역할로 기억한다** — 코드를 올리는 곳(jgga3111) · 검색엔진에
          말하는 곳(jgga1234567) · 광고비를 받는 곳(runningon93).
      · `ads.txt 상태`는 09-06 기준 **승인됨**이다(09-05 09:49 KST 갱신).
        "찾을 수 없음"은 결함이 아니라 재크롤링 지연이었다.
      · 계정에 **"주소가 확인되지 않아 지급이 보류 중"** 알림이 떠 있다.
        러닝온 쪽 사안이라 여기서 건드리지 않는다.
      · 2026-09-02 서치콘솔 속성 등록·소유확인 완료(URL 접두어, jgga1234567)
      · **계정 번호(`/u/N/`)를 외우지 말 것.** 09-03에 `/u/2/`였던 자리가
        09-04에는 runningon93 계정이 됐다. 순번은 로그인 순서 따라 바뀐다.
        주소에 `?authuser=jgga1234567@gmail.com`을 붙이면 구글이 알아서 찾아간다.
      · **09-07에 「페이지 색인 생성」 보고서가 열렸다**(속성 등록 09-02, 닷새째).
        **색인 생성됨 270 · 색인 안 됨 576.** 클릭은 6회(09-06 4회).
        미색인 576의 내역:

        | 사유 | 건수 |
        |---|---|
        | 발견됨 – 현재 색인이 생성되지 않음 | 528 |
        | 'NOINDEX' 태그에 의해 제외 | 46 |
        | 크롤링됨 – 현재 색인이 생성되지 않음 | 2 |

        **수록 900→1,500을 올리지 않는다.** 사이트맵 803개 중 270개만 색인됐고
        528개가 "발견됐지만 크롤링할 가치가 낮다"고 대기 중이다. 러닝온이
        똑같이 598건을 쌓아 두고 있었다(docs/02 1절). **이 상태에서 URL을
        600개 더 넣으면 대기열만 길어진다.** 09-06에 적은 "상위 100건을 깊게
        파는 쪽이 낫다"가 이 수치로 확인됐다.
- [x] **계산기를 처음 냈다 — 셋 (2026-09-08).** 여태 사이트에 계산하는 곳은
      `/check`(중위소득) 하나뿐이었다. 원문의 금액을 **산문으로만** 보여 주고
      한 걸음을 안 갔다.
      · `/guide/k-pass` — 원문이 "20%~53.3%", "기준금액(3~10만원)"이라고
        **범위만** 말한다. 표는 운영기관인 **한국교통안전공단**에 있었다.
      · `/guide/tax-credit-amount` — **밖에서 가져온 숫자가 하나도 없다.**
        산식이 수록 원문(23위 「근로·자녀장려금」)에 통째로 들어 있었다.
        `/guide/tax-credit`(언제)과 짝이다 — 이쪽은 얼마다.
      · `/guide/youth-savings` — 원문이 "납입액의 **일정비율**"이라고만 한다.
        비율(일반형 6%·우대형 12%)은 금융위 보도자료에 있었다.
      · `/guide/emergency`에 생계지원 금액표를 더했다(아래 별항).

      ── 이번에 얻은 규칙 넷 ──────────────────────────────────
      · **요약본을 믿지 말고 병합 셀까지 본다.** 공단 페이지를 요약해 읽었더니
        "어르신 50%"였는데 원문 표는 `<td colspan="3">30%</td>`로 청년·어르신·
        2자녀를 묶고 있었다. 요약이 병합을 못 읽고 값을 오른쪽으로 민 것이다.
        국토부 보도자료 제목이 "어르신 유형(**환급률 30%**) 신설"이라 대조하면
        걸린다. **그대로 실었으면 어르신에게 실제의 1.7배를 알려 줄 뻔했다.**
      · **교차 검증되는 자리를 찾는다.** K-패스 기준금액 표의 최솟값 3만원·
        최댓값 10만원이 복지로 원문의 "기준금액(3~10만원)"과 정확히 맞았다.
        서로 다른 두 출처가 같은 수를 가리키면 옮겨 적은 것이 맞다는 뜻이다.
      · **두 출처가 어긋나면 계산하지 않고 어긋난 사실을 싣는다.** 청년미래적금
        일반형 소득기준이 복지로 7,500만 대 금융위 6,000만이다. 가입 상한과
        기여금 상한이 다른 것으로 읽히지만 **그건 우리 해석이지 어느 문서에도
        없다.** 그 구간을 계산에서 빼고 「두 문서가 다릅니다」 절로 적었다.
      · **모르는 값을 그럴듯한 수로 채우지 않는다.** 청년미래적금 이자는 은행마다
        달라 계산에서 뺐고("계산 안 함"이라고 화면에 적음), K-패스 일반형은
        3,000원 미만 이용분을 모르면 **0이 아니라 "계산 안 함"**으로 둔다 —
        0은 "없다"는 뜻이라 빈칸과 다르고, 뭉개면 실제보다 적게 알려 준다.

      ── 판정은 여전히 하지 않는다 ────────────────────────────
      계산기는 되고 **판정소는 안 된다.** 고시·원문의 산식에 넣어 "이 값이
      나옵니다"는 계산이지만 "당신은 받을 수 있습니다"는 판정이다(3절).
      유형·지역·재산 구간은 **고르는 것이지 우리가 맞히는 것이 아니다.**
      곱한 값에는 전부 "저희가 계산한 값입니다"를 붙였다(실업급여 글의
      "조문에 없는 숫자입니다"와 같은 자리).
- [x] **긴급복지 글에 금액표를 넣었다 — 결정을 뒤집으며 주석을 고쳐 썼다 (09-08).**
      원래 이 글은 **금액을 일부러 안 실었다.** 주석에 이유가 적혀 있었다 —
      "여기 옮겨 적으면 고시가 바뀌는 순간 이 글만 옛 숫자로 남는다."
      · 그 걱정은 **손으로 적을 때만** 맞다. 새로 넣은 표는 수록 원문의
        `supportContent`를 빌드 때 파싱해 그린다. 재수집으로 금액이 바뀌면
        글도 같이 바뀌고, 상세 페이지와 **같은 한 곳**을 보므로 어긋날 수 없다.
        파싱이 실패하면 절 자체가 사라진다 — 틀린 표를 그리느니 없는 게 낫다.
      · **남의 결정을 뒤집을 때는 주석을 지우지 말고 고쳐 쓴다.** 원래 판단과
        무엇이 달라져서 바꿨는지를 같이 남긴다. 안 그러면 다음 사람이 같은
        고민을 처음부터 다시 한다.
- [x] **토스 미니앱 인기 100선을 수요 신호로 썼다 (09-08).** 사용자가 화면을
      찍어 줬다. 100개 중 **복지·지원금이 28개**였고 갈래가 뚜렷했다.

      | 갈래 | 토스 순위 | 개수 |
      |---|---|---|
      | 지원금 찾기·매칭 | 7·28·35·37·42·48·61·75·93 | 9 |
      | **근로장려금 계산** | 22·26·30·55·74(+10 반기) | **6** |
      | K패스 환급 계산 | 53·78·100 | 3 |
      | 기초생활 수급 **판정** | 38·66 | 2 |

      · **검색량과 겹치는 것이 강한 신호다.** K패스는 데이터랩에서 실업급여의
        2.4배였고 토스에도 셋이 있었다 — 두 곳이 같이 가리켰다.
      · **우리는 이미 가진 숫자를 안 쓰고 있었다.** 상위 100건 중 **72건**에
        구체 금액이 원문에 있고 그중 22건은 산식·비율·한도까지 있다. 상위
        20건 중 17건이다. 그런데 계산하는 곳은 `/check` 하나뿐이었다.
      · **인기가 있어도 안 하는 것**: 「수급 점검」·「판정소」류(38·66·74).
        3절이 못 박은 자리다. 급식·택배·운세·주식과 공공 대관(45)·
        어린이집(84)·환급금 조회(89)는 주제 밖이다.
      · 다음 후보로 재 볼 것: 국민연금 미리보기(32위) — 우리 데이터엔
        194위 출산크레딧 1건뿐이고, 계산이 가입이력에 달려 있어 조심스럽다.
- [x] **09-08 색인 작업 — 구글 25건 요청 · 빙 100건 제출.**
      구글은 조회수 **167~200위**를 훑어 25건 요청, **15건은 이미 색인돼
      건너뛰었다**(40건 검사). **다음은 201위 `WLF00002235`(서울형 긴급복지)부터.**
      · 우선 6건 먼저 넣었다 — `WLF00005440`(K-패스) · `WLF00000092`
        (기저귀바우처) · `WLF00006266`(청년미래적금) · `WLF00000055`
        (문화누리카드) · `/guide/unemployment` · `/guide/pay-dates`.
        **조회수 20위 기저귀바우처가 아예 미색인이었다**(발견됨-크롤링 안 됨).
      · **이미 색인된 페이지도 내용이 바뀌었으면 재요청한다.** 화면에
        「페이지가 변경되었나요? 색인 생성 요청」이 따로 뜬다. "이미 색인되면
        건너뛴다"는 규칙은 **내용이 그대로인 백로그에만** 해당한다.

      ── 이번에 새로 걸린 함정 셋 ──────────────────────────────
      · **「오류 발생 — 색인 생성 요청을 제출하는 중에 문제가 발생했습니다.
        나중에 다시 시도해 주세요」는 할당량이 아니다.** 171위에서 한 번 떴는데
        **그대로 다시 누르니 들어갔다.** 이걸 한도로 읽고 그만두면 남은 걸
        통째로 못 넣는다. 한 번은 재시도해 보고 판단한다.
      · **검사창은 `triple_click`으로 넣는다.** 그냥 클릭 뒤 타이핑하면
        글자는 들어가는데 **Enter가 안 먹어 「개요」에 머문다**(입력창이
        여러 개 겹쳐 있고 활성 것이 그때그때 다르다). 09-05에 적어 둔
        "ctrl+a로 안 지워진다"와 같은 자리다.
      · **크롬 기본 계정이 `jgga3111`이라 「이 속성에 액세스할 수 없습니다」가
        뜬다.** 주소에 `?authuser=jgga1234567@gmail.com`을 붙이면 `/u/1/`로
        들어간다. 0절의 계정 분리가 여기서 걸린다.
      · 요청 한 건에 **40~50초**가 걸린다. 누르면 구글이 「실제 URL 테스트」를
        먼저 돌리기 때문이다. 이게 09-07에 적은 "한 건 75초"의 정체다.
- [x] **미색인 576건의 실상을 갈라 봤다 (2026-09-08).** "전부 해결"은 안 된다.

      | 사유 | 건수 | 실상 |
      |---|---|---|
      | `NOINDEX` 태그 | 46 | **우리가 일부러 한 것** — 얇은 페이지 방침 |
      | 크롤링됨 – 색인 안 됨 | 2 | 전수 확인, **결함 아님** |
      | 발견됨 – 색인 안 됨 | 528 | 구글이 **크롤링조차 안 한 것** |

      · 크롤링됨 2건은 `WLF00002234`(169위·본문 599자)와 `/income/48`
        (소득기준 허브, 3건)이다. 둘 다 기준을 넘고 결함이 없다. 구글이
        아직 안 실었을 뿐이라 **고칠 것이 없다.**
      · 528은 우리 페이지 결함이 아니라 **구글이 우선순위를 낮게 매긴 것**이다.
        수동 요청으로 다 밀어 넣으면 한 건 40~50초 × 528 ≈ **7시간**이고,
        넣어도 색인은 보장되지 않는다. "URL을 더 넣으면 대기열만 길어진다"는
        09-07 결론이 그대로 유효하다. **값어치 있는 데까지만 넣는다.**
- [x] **09-09 색인 작업 — 구글 26건 요청 · 빙 사이트맵 재제출.**
      구글은 40건을 검사해 26건 요청, **14건은 이미 색인**돼 건너뛰었다.
      **다음은 237위 `WLF00002372`(기초생활지원 차상위계층지원)부터** — 같은 날
      오후에 여기까지 넣었고 지금 다음은 262위다(아래 09-09 2차).
      · 우선 4건 먼저 — 09-08 배포분 `/guide/k-pass` · `/guide/tax-credit-amount` ·
        `/guide/youth-savings`(새 글 셋) + `/guide/emergency`(금액표 추가).
        **새 글 셋은 전부 미색인**이었고 emergency는 이미 색인돼 있어 재요청했다.
      · 백로그 201~236위에서 22건 요청. 이 구간도 **절반 가까이 이미 색인**이라
        (14/40) 수동 요청의 값어치가 계속 줄고 있다.
      · **빙 사이트맵을 재제출했다.** 빙 804 대 라이브 807로 벌어져 있었다
        (09-08 배포로 새 글 셋이 늘었다). `⋮ → Re-submit` 뒤 Status가
        `Success` → **`Processing`**, Last submit이 9/6 → 9/8로 바뀌는 것을
        값으로 확인했다. `URLs discovered`는 빙이 다시 긁은 뒤에 바뀐다.

      ── 이번에 새로 걸린 함정 셋 ──────────────────────────────
      · **빈 검사창에는 `triple_click`이 안 먹는다.** 09-08에 "검사창은
        triple_click으로 넣는다"고 적었는데 반쪽이었다. 값이 **있을 때**는
        맞지만, 빈 칸에 triple_click하면 포커스가 안 잡혀 타이핑이 사라진다.
        → **빈 칸이면 `left_click`, 값이 있으면 `triple_click`.**
      · **창 크기가 바뀌면 GSC가 모바일 레이아웃으로 갈아탄다.** 세션 도중
        프레임이 1351 → 618로 줄었는데, 그 순간 검사창이 화면 밖(x=-127)으로
        나가고 상단 **돋보기 아이콘** 뒤로 숨었다. 좌표가 음수로 나오면
        "안 보인다"가 아니라 **레이아웃이 바뀐 것**이다 — 돋보기를 먼저 누른다.
        (09-04에 적은 "프레임 폭은 고정값이 아니다"의 연장이다.)
      · **「오류 발생」이 이날 네 번 떴고 네 번 다 재시도로 들어갔다**
        (206·213·214·235위). 09-08에 한 번 겪고 적어 둔 규칙이 그대로 맞았다.
        이제는 **오류를 보면 무조건 한 번 재시도**한다.
      · 화면 URL 대조가 또 일을 했다. 닫기가 안 눌려 대화상자가 남아 있으면
        검사창에 못 들어가는데, 화면에는 **앞 URL의 판정이 그대로** 떠 있다.
        `u.endsWith(넣은 id)`를 매번 확인해 두 번 걸러 냈다.
      · **좌표 클릭을 그만두고 자바스크립트로 돌렸다** (09-09 2차, 아래 별항).
- [x] **09-09 2차 — 빙 100건 · 구글 15건 요청, 그리고 「할당량 초과」를 처음
      봤다.** 아침에 남겨 뒀던 빙 601~700위를 오후에 넣고(할당량이 차 있었다),
      구글은 237위부터 훑어 **25건 검사 · 15건 요청 · 10건 이미 색인**.
      ~~다음은 262위 `WLF00000098`부터~~ — **09-10에 그만뒀다.**
      조회수 백로그를 이어서 넣지 않는다(바로 아래 「색인 요청 기준을 바꿨다」).

      ── 좌표 클릭을 버렸다 ────────────────────────────────────
      여태 스크린샷 좌표로 눌렀는데(프레임 폭이 바뀐다, 모바일로 갈아탄다,
      캡처가 깨진다…) 이번에 **화면을 안 보고 자바스크립트로만** 돌렸다.
      한 건에 40~90초는 그대로지만 좌표 사고가 0이었다.
      · **`computer`의 `key Return`이 검사창에서 안 먹는다.** 글자는 들어가는데
        엔터만 씹혀 「개요」에 머문다. `KeyboardEvent('keydown'…keyCode:13)`을
        입력창에 **직접 쏘면** 들어간다. 09-08의 "triple_click으로 넣는다"는
        입력 얘기였고, **보내는 것은 별개 문제**였다.
      · **검사창이 두 개 겹쳐 있고 앞의 것은 `disabled`다.** 폭만 보고 고르면
        죽은 칸을 잡는데, 에러도 안 나고 글자가 산 칸에 **덧붙는다**
        (`…WLF00003547https://…`). `!disabled && !readOnly`를 같이 본다.
      · **GSC는 앞서 검사한 패널을 DOM에 그대로 쌓아 둔다.** 버튼을 첫 번째로
        잡으면 **옛 화면의 버튼**을 누른다. 보이는 것 중 **마지막**을 쓴다.
      · **그리고 이게 「오류 발생」 3연속의 진짜 원인이었다.** 할당량인 줄
        알았는데 아니었다 — 고쳐서 다시 넣으니 **세 건 다 한 번에 들어갔다.**
        09-08·09-09에 적어 둔 "오류는 할당량이 아니다"가 또 맞았고, 이번엔
        **재시도가 아니라 내 선택자가 문제**였다. 오류를 보면 재시도하되,
        연달아 나면 **누르고 있는 버튼이 맞는지**부터 본다.
      · 재시도할 때 버튼 라벨이 **「색인 생성 요청다시 요청」**으로 바뀐다.
        정확히 일치로 찾으면 두 번째부터 못 찾는다 — 앞부분만 본다.
      · **도구 호출이 45초에 끊긴다.** `await`로 기다리면 45초짜리 작업도
        중간에 잘린다. `await` 없이 IIFE로 띄우면 **페이지는 계속 돈다** —
        결과를 `window.__log`에 쌓고 짧은 호출로 꺼내 본다. 75건을 한 번에
        걸어 두고 40초마다 개수만 읽는 식으로 돌렸다.

      ── 「할당량 초과」는 「오류 발생」과 다른 화면이다 ────────────
      262위에서 **「할당량 초과 — 일일 할당량을 초과하여 이 요청을 처리할 수
      없습니다. 내일 다시 제출해 주세요.」**가 떴다. 재시도해도 안 들어간다.
      **이 문구를 보면 그날은 끝이다.** 「오류 발생 — …나중에 다시 시도해
      주세요」와 글이 다르니 **문구로 갈라 읽는다.**
      · 오늘 들어간 것은 15건이다. 실측 목록에 09-09를 둘로 나눠 적는다
        (오전 26건 · 오후 15건 = 41건).
      · 246~261위 16건 중 **10건이 이미 색인**이었다(62%). 09-07에 35/80,
        09-08에 15/40, 09-09 오전에 14/40이었으니 **비율이 계속 오른다.**
        250위 아래는 사이트맵만으로 붙고 있다는 뜻이다.
- [x] **화면이 밋밋했던 원인은 카드가 아니라 지면이었다 (2026-09-09).**
      사용자가 "너무 밋밋하다"고 해서 경쟁 사이트(해피나눔)를 실측 대조했다.

      | | 복지클릭(전) | 해피나눔 |
      |---|---|---|
      | 지면 | **순백 `#fff`** | `rgb(250,250,249)` |
      | 카드 | 흰색·라운드 12·그림자 없음 | **똑같음** |
      | 홈 img+svg | **1개** | 24개 |
      | 목록 img+svg | **0개** | — |

      · **카드는 이미 저쪽과 같았다.** 그림자도 저쪽이 안 쓴다. 차이는
        **지면이 카드와 같은 흰색**이라 807쪽 전부에서 카드가 테두리 선
        하나로만 구분됐다는 것이다. 그래서 면을 세 층으로 갈랐다 —
        `card #fff` / `ground #faf9f7` / `sunken #f0ede7`(옛 `bg-slate-50`
        49곳). 색 온도도 파랑 쪽에서 중립으로 옮겼다(ink·muted·line).
      · **주색 파랑은 그대로 뒀다.** 러닝온과 형제로 보이게 맞춘 결정을
        통째로 뒤집지 않고 지면만 손봤다. 되돌리려면 ground를 `#fff`,
        sunken을 `#f8fafc`로 두 줄만 바꾸면 된다(globals.css 머리말).
      · **아이콘 세트 35종을 그렸다**(`components/AxisIcon.tsx`) — 대상 6 ·
        생애주기 7 · 주제 15 · 혜택 7. `AxisGrid` 한 곳만 고치면 허브 네
        곳(`/target`·`/life`·`/theme`·`/benefit`)이 전부 걸린다.
        09-06에 이모지를 걷어내며 *"아이콘 세트를 직접 그릴 형편이 아니니
        아예 없앴다"*고 적어 둔 자리를 이번에 채운 것이다.
      · 이모지가 실패한 이유가 "제각각"이었으므로 그 반대만 지켰다 —
        24×24 · 선 1.6 · **면을 칠하지 않음** · 색은 `currentColor` ·
        도형은 원·둥근 사각·선 셋으로만. 없는 슬러그는 물음표 같은 대체
        그림을 넣지 않고 **아예 안 그린다.**
      · 대비 실측: 본문 대 지면 **16.56**, muted 대 지면 **6.17**(기준 4.5).
        `text-slate-*` 158곳은 손대지 않았다 — 거의 중립이라 그대로 읽힌다.
      · **「마감 임박 D-day」는 안 했다.** 저쪽 첫 화면의 핵심 장치인데
        900건의 `applyEnd`가 **9999-12-31 555건 · 없음(중앙부처) 330건**이라
        실제 날짜는 **15건뿐**이고, D-30 안쪽은 **1건**이다(2026-09-30, D-21).
        나머지는 D-113 이상이라 화면이 안 된다. docs/03 §7의 "달력에 채울
        것이 없다"가 지금 데이터로도 그대로 맞다(§8에 다시 적었다).
        · 09-09에 처음 적을 때 16이라고 썼는데 **다시 세니 15**였다.
          이런 수는 화면에 안 나가더라도 판단의 근거가 되므로 **세는 명령을
          같이 남긴다** — `grep -o 'applyEnd": *[^,}]*' src/data/services.ts`.
      · **스톡 사진도 안 넣었다.** 저쪽 히어로는 스톡 삽화인데 라이선스가
        걸리고, 애드센스 심사 중에 출처 불분명한 이미지를 늘릴 이유가 없다.
        삽화가 필요하면 OG 카드처럼 우리가 그리는 SVG로 간다.
- [x] **축을 겹쳐서 좁힐 수 있게 했다 — `/find` (2026-09-09).** 우리는 축이
      경쟁 사이트보다 많은데(주제 15·대상 6·생애주기 7·혜택 7·지역 15)
      **축마다 페이지가 따로 있을 뿐 겹칠 수가 없었다.** "청년이면서 저소득"을
      찾으려면 `/life/youth`에 들어가 눈으로 골라야 했다. 해피나눔은 홈에서
      생애주기·대상·지역을 한 번에 고르게 한다(docs/03 §7 탐색축 비교).
      · **만들기 전에 조합이 성립하는지 셌다.** 대부분 비면 필터가 고장난
        것처럼 보인다. 생애주기 7 × 대상 6 = 42개 중 **39개가 3건 이상**이고
        0건은 2개뿐이었다(임신·출산×보훈, 임신·출산×다문화).
      · **결과 화면은 `noindex`다.** 조합마다 URL이 생기므로 색인시키면
        "발견됨 – 색인 안 됨" 528건에 스스로 더 얹는다. `/search`가 택한 길과
        같고, 사이트맵은 807 그대로다. 대신 결과 밑에 **색인되는 축 페이지**
        (`/life/*`·`/target/*`·`/region/*`) 링크를 함께 둔다.
      · **거르는 일은 서버가 한다.** `AxisFinder`(클라이언트)는 축 목록만 알고
        `@/data/services`는 손대지 않는다 — 900건을 브라우저로 내리면 2.9MB다
        (`lib/search.ts` 머리말의 함정과 같은 자리). 그 대가로 고르는 동안
        건수를 미리 못 보여준다. 그건 받아들인다.
      · **엉뚱한 슬러그는 404가 아니라 무시한다.** 주소를 손으로 고쳐 온
        사람에게 빈 화면을 주느니 나머지 조건으로 좁혀 보여주는 쪽이 낫다.
      · ⚠ **`/find`의 상자는 지금 걸린 조건을 물고 시작해야 한다.** 처음엔
        안 그래서, 청년·저소득 결과(67건)를 보는데 상자는 전부 "전체"였고
        단추는 "전체 목록 보기"였다 — 지역만 더하려 누르면 앞서 고른 둘이
        풀린다. 넘기는 값은 주소의 날것이 아니라 **걸러 낸 값**이어야 한다
        (화면에 적용된 것과 상자에 켜진 것이 늘 같아야 한다).
      · 실측 대조: 청년×저소득 67 · 노년×저소득 86 · 임신출산×보훈 0(빈 화면
        문구) · 청년×저소득×서울 40 · 경기만 430(120건 자름 안내). 전부
        미리 계산한 값과 일치.
      · **`audit-sitemap`의 「일부러 제출하지 않는 것」 표에 `/find`가 빠져
        있었다.** 그 표는 데이터만 읽고 `src/app`을 안 훑어서, 사이트맵에
        안 넣는 페이지를 새로 만들면 조용히 옛 수로 남는다(3과 2는 손으로
        센 수다). 807이라는 합계는 멀쩡한데 **왜 목록에 없는지를 설명해
        주지 못하는 상태**였다. 줄을 하나 더하고 그 이유를 주석에 적었다.
        **사이트맵에 안 넣는 페이지를 만들면 이 표도 같이 고친다.**
- [x] **문장으로 물어보는 자리를 만들었다 — `/ask` (2026-09-09).** 첫 화면에서
      찾는 길이 셋이었는데(헤더 검색·여덟 칸·`/find`) **셋 다 먼저 무언가를
      알아야** 쓸 수 있었다 — 검색은 사업 이름을, 나머지 둘은 "생애주기"·
      "대상"이 무엇인지를. 처음 온 사람이 품고 오는 것은 축이 아니라 문장이라
      ("서울 사는 30대인데 월세 지원 있나요"), 아무것도 모르는 사람이 첫
      화면에서 쓸 수 있는 길이 하나도 없던 셈이다.
      · **히어로를 반으로 줄이고 그 자리를 내줬다.** 375px 실측 **341px →
        187px**(데스크톱 179px). 줄인 것은 두 줄짜리 제목을 한 줄로, 4xl을
        3xl로, 그리고 설명 문단을 통째로 뺀 것이다 — 그 문단이 하던 말을
        **바로 밑 수치 셋이 이미 더 정확하게** 하고 있었다(900건·570건).
        수치와 출처 줄은 한 치수만 줄이고 남겼다. 히어로에서 사실을 말하는
        줄은 거기뿐이다.
      · **히어로 안에 넣지 않았다.** 히어로에 큰 검색창을 뒀다가 걷어낸 적이
        있다 — 375px에서 헤더 검색과 나란히 보여 같은 모양의 둥근 상자가
        둘이었다. 그래서 **제목이 붙은 상자**로 만들어 히어로 밖에 뒀다.
      · **AI가 아니다. 모델을 부르지 않는다.** 질문의 낱말을 표와 대조할
        뿐이라(`lib/askParse.ts`), 화면에 "AI가 답해 드립니다"라고 적지
        않는다. 대신 **읽은 것을 전부 펼쳐 보여준다** — 무엇을 조건으로
        바꿨는지("30대" → 청년), 어느 낱말을 못 찾았는지, 무엇을 바꿔
        찾았는지("병원비" → 의료비로 찾음). 알아들은 척하고 조용히 좁히면
        안 나온 것이 없는 것인지 못 읽은 것인지 구분할 방법이 사라진다.
      · **자격 판정은 안 한다**(3절). 소득 금액이 보이면 판정하지 않고
        `/check`로 보낸다. 나이로 생애주기를 고를 때는 "나이 기준은 사업마다
        다릅니다"를 화면에 함께 적는다 — 청년 상한이 34세인 사업도 39세인
        사업도 있어서, 우리가 그은 선은 자격선이 아니다.
      · **`noindex`다.** 질문마다 URL이 생긴다 — `/search`·`/find`와 같은 길.
        `audit-sitemap`의 「결과 화면」 줄을 2 → 3으로 같이 고쳤다(바로 위
        항목에 적어 둔 규칙이 첫 적용을 받은 자리다). 사이트맵은 807 그대로.
      · 서버 컴포넌트다. 묻는 상자도 평범한 `<form method="get">`이라
        **클라이언트 번들이 0바이트 는다.**

      ── 이번에 얻은 규칙 넷 (전부 화면을 보고 고친 것) ──────────────
      · **필드가 있다고 축으로 쓸 수 있는 건 아니다.** 처음엔 혜택(지급형태)도
        조건으로 걸었는데, 「기초생활수급자 전기요금 감면」을 넣으니 24건이
        나오면서 **정작 「전기요금 복지할인」이 빠졌다.** 원본이 그 사업을
        「감면」이 아니라 **「현금지급」**으로 분류해 놨기 때문이다. 원본이 매긴
        값은 우리가 못 고치고, 걸러 내는 데 쓰면 **가장 맞는 답이 조용히
        사라진다.** 그래서 혜택은 **거르지 않고 점수만 준다.** 본문에는
        "요금감면을 지원합니다"가 그대로 있어 낱말로는 걸린다.
        · 같은 이유로 **주제(`themes`)는 아예 조건으로 안 쓴다.** 900건 중
          329건에만 있고 **전부 중앙부처**라, "주거"를 걸면 지자체 570건이
          통째로 사라지는데 화면에는 그냥 적게 나온 것처럼 보인다.
          5절의 「중앙과 지자체는 주는 필드가 다르다」가 그대로 걸린다.
      · **낱말로 걸러도 안 되고 안 걸러도 안 된다 — 가른다.** 안 걸린 것을
        그냥 두니 "월세"로 찾은 목록 다섯째부터 장애인연금이 나왔고, 걸러
        내니 "혼자 아이 키우는데"가 한부모 117건에서 4건으로 줄면서 정작
        「한부모가족 아동양육비」가 사라졌다. 뒤엣것의 원인은 낱말이 나빠서가
        아니라 **관공서 문장이 「아이」를 안 쓰기 때문**이다(아동·자녀라고
        쓴다). 그래서 낱말까지 걸린 것을 먼저 내고, 조건만 맞는 것을 **그
        아래 줄을 긋고** 따로 붙인다. 어느 쪽도 버리지 않는다.
      · **사람 말과 원문 말이 다른 자리는 세어 보고 잇는다.** 「병원비」는
        900건에 **0건**인데 「의료비」는 58건이다. 넣는 기준 둘 — ㉠ 사람 말이
        0건일 것 ㉡ 원문 말은 여러 건 있을 것. 실측으로 여덟 쌍만 넣었다
        (병원비→의료비 · 약값→약제비 0/9 · 전기세→전기요금 0/10 ·
        가스비→가스요금 0/3 · 수도세→수도요금 0/11 · 집세→임차료 0/9 ·
        밥값/끼니→급식 0/20). **탈락**: 치료비 19 · 진료비 24 · 등록금 12 ·
        학원비 7 · 차비 4 · 간병비 9 — 사람 말이 이미 원문에 있다.
        `lib/aliases.ts`가 별칭을 넣을 때 쓰는 방식과 같다.
      · **"어디에도 없습니다"라고 쓰기 전에 어디를 셌는지 본다.** 「전기요금
        감면」에서 "「전기요금」은 수록 900건 어디에도 없습니다"가 나갔는데
        **틀린 말이었다** — 900건에는 있고 조건으로 좁힌 24건에 없었을
        뿐이다. 세는 범위를 문장에 그대로 적는다(3절). 7절의 「검사 쪽을
        먼저 의심한다」와 같은 자리다.
- [ ] **네이버 서치어드바이저 — 09-11 사용자가 진행하기로 했다. 토큰 넣음.**
      · 사용자가 HTML 태그 토큰을 줬다 → `layout.tsx` `verification.other`에
        `naver-site-verification` 한 줄. **`e91b613` 라이브 확인** — 홈·안내 글·상세 세 곳에 태그,
        사이트맵 808, robots.txt가 Yeti를 안 막음.
      · **09-11 사용자가 콘솔에서 「소유확인」을 끝냈다.** 사이트맵 제출
        (`https://bokjiclick.co.kr/sitemap.xml`)·주요 페이지 수집 요청은 사용자 콘솔 몫이다.
      · ⚠ **`searchadvisor.naver.com`은 앱 브라우저와 Claude in Chrome 둘 다
        「safety restrictions」로 열리지 않는다**(09-11). 그래서 사이트 등록·
        「소유확인」 단추·사이트맵 제출·수집 요청은 **사용자가 콘솔에서** 한다.
        내가 할 수 있는 것은 태그를 넣고 라이브에서 나오는지 확인하는 데까지다.
      · 어느 네이버 계정인지는 기록이 없다(0절 계정 표에 네이버 줄 없음) — 알려
        주면 계정 표에 적는다. 말 한마디로 고치지 않는 표라 되물어 확인한 뒤에.
- [ ] **네이버 서치어드바이저는 사용자가 직접 한다 (2026-09-10 확정).**
      **여기서 진행하지 않는다. 매번 다시 묻지도 않는다.** 계정을 새로 정해야
      하고(0절의 계정 셋에 네이버 줄이 없다) 콘솔에서 `naver-site-verification`
      토큰을 받아 와야 하는데, 그 두 가지가 사용자 손에 있다.
      · 코드 쪽 준비는 **이미 끝나 있다.** 사이트맵 날짜가 `YYYY-MM-DD`라
        네이버 파서에 맞고(4절), `robots.ts`가 Yeti를 안 막고 있으며,
        `www`는 apex로 308이다.
      · 사용자가 토큰을 주면 `src/app/layout.tsx`의 `verification.other`에
        한 줄 넣는 것으로 끝난다. **그때만 손댄다.**

- [x] **색인 요청 기준을 바꿨다 — 「좋은 글 위주로만」 (2026-09-10 사용자 확정).**
      여태 조회수 순서를 따라 내려가며 백로그를 한도까지 채웠다. 그만둔다.
      · 넣는다 — 우리가 직접 쓴 안내 글, **내용이 실제로 바뀐 페이지**,
        본문이 두툼한 상위 상세.
      · 안 넣는다 — **넣어 봐야 색인이 안 되고 「발견됨 – 색인 안 됨」으로
        쌓이기만 할 것.** 할당량이 남았다고 백로그로 채우지 않는다.
      · 근거는 이미 이 문서에 있었다. 「발견됨 – 색인 안 됨」이 528건이고,
        순위가 내려갈수록 **이미 색인돼 건너뛰는 비율이 계속 올랐다**
        (09-07 44% → 09-08 38% → 09-09 오전 35% → 09-09 오후 **62%**).
        한 건에 40~90초를 들일 자리가 아니다.
      · **"다음은 몇 위부터"를 이어서 넣던 방식을 그만둔다.** 262위
        `WLF00000098`에서 멈춘 백로그는 그대로 두고 다시 손대지 않는다.
- [x] **09-10 색인 작업 — 구글 3건 요청 · 빙 100건 제출. 새 기준의 첫 적용.**

      ── 구글: 안내 글 20편을 전수 검사했다 ─────────────────────────
      조회수 백로그 대신 **우리가 직접 쓴 글**만 봤다. 20편 중 **18편이 이미
      색인**이었고 요청은 2건뿐이다(`/guide/online` · `/guide/popular`).
      홈 `/`은 어제 배포로 내용이 바뀌어(히어로 축소 + `/ask`) **재요청**했다.
      **합계 3건 요청 · 18건 건너뜀 · 할당량 안 걸림.**
      · **09-07~09-09에 요청한 여덟 편이 전부 색인됐다** — calendar ·
        tax-credit · pay-dates · unemployment · emergency · k-pass ·
        tax-credit-amount · youth-savings. 뒤의 셋은 **어제 요청해 하루 만에**
        들어갔다. 좋은 글은 요청하면 빨리 붙는다.
      · 그런데 **한 번도 요청 안 한 아홉 편도 이미 색인돼 있었다**(apply ·
        documents · income-line · region · mistakes · terms · life-stage ·
        baby-money · livelihood + `/guide` 인덱스). 사이트맵만으로 붙는다.
        → **글은 색인이 잘 된다. 문제는 글이 아니라 얇은 상세다.**
        이것이 새 기준을 뒷받침하는 실측이다.

      ── 빙: 상세 아닌 100건을 처음 넣었다 ────────────────────────
      **09-03~09-09에 넣은 700건이 전부 상세 페이지였다.** 안내 글 20 ·
      축 허브 68 · 정적 12 = **100건이 한 번도 안 갔다.** 오늘 그 100건을
      넣었다 — 할당량 100 → 0, 목록 총계 **700 → 800**.
      · **상세 백로그 7건(701~707위)은 새 기준대로 뺐다.** 조회수 맨 아래라
        넣어도 쌓이기만 한다. 남은 것은 `/income` 허브 7개뿐이고,
        이것도 급하지 않다.
      · 빙 할당량 문구가 이날은 `Quota will reset in 24 hrs`였다(09-08 3분,
        09-09 2시간). **문구는 그때그때 다르니 값(숫자)을 읽는다** — 제출 전
        100, 제출 후 0을 확인했다.

      ── 이번에 걸린 함정 둘 ──────────────────────────────────────
      · **판정 문구가 「URL이 Google에 등록되어 있음」이다.** "등록되어
        있습니다"로 찾다가 첫 건이 90초 timeout으로 죽었다. 「있지 않음」이
        붙는지로 갈라 읽는다 — `/URL이 Google에 등록되어[^\n|]*/`를 뽑아
        `있지 않음` 포함 여부를 본다. **화면 문구를 짐작해서 적지 말고
        한 건을 실제로 띄워 보고 그대로 옮긴다.**
      · **브라우저 창이 좁으면 GSC가 모바일 레이아웃으로 갈아탄다.**
        이날 창이 728px여서 검사창의 `width`가 **0**이 됐다(돋보기 뒤로 숨음).
        09-09에 적어 둔 그 함정이다. 좌표를 안 쓰더라도 **`resize_window`로
        1440px을 만들어 두고 시작한다** — 그러면 숨는 일이 없다.
        (끝나면 `preset: "desktop"`으로 되돌린다.)
      · 이미 색인된 페이지도 **내용이 바뀌었으면 재요청한다**(홈이 그 경우다).
        화면에 「페이지가 변경되었나요? 색인 생성 요청」이 따로 뜬다.
- [x] **09-10 버그 점검 — 코드 리뷰로 10건 고침 (09-11 배포).**
      첫 화면 「많이 찾는 지원」 순위표(`PopularList`)는 **꾸미기라 따로 뺐다가
      09-11에 사용자가 정해 배포했다.** 375px 첫 화면 5,709px → 4,662px.
      정적 검사·감사 셋·라이브 807개 전부 200은 원래 멀쩡했다. 걸린 것은
      전부 **입력 끝자락**이었다.
      · **가장 무거운 것 — 자가진단 결과를 버림으로 저장했다**(`myIncome.ts`).
        50.7%로 보여 준 사람을 50으로 저장해 50% 이하 사업에 「충족」을 띄웠다.
        **틀리는 방향이 늘 "받는다" 쪽**이라 3절에 정면으로 걸린다.
        소수 첫째 자리로 저장한다.
      · 자가진단 입력이 점을 지워 187.5 → 1875(열 배) · 「8인 이상」이
        누를 때마다 늘어 21인부터 저장이 조용히 실패 · 금액 상한 없음.
      · 계산기 셋이 "1,500"·"1500만원"을 빈칸으로 봤다(IncomeCheck만 쉼표를
        걷고 있었다) · 자녀 수 1.5를 조용히 1로 셈했다.
      · `/ask` — 붙여 쓴 "노인틀니"가 틀니를 잃었다(0 → 7건) · "3세대"를
        3살로 읽었다 · 은/는을 낱말 **개수**로 골랐다(“주차장”는).
      · `/search`·`/find`·`/ask`가 루트의 `canonical: "/"`를 물려받아
        noindex와 엇갈린 신호를 냈다. 각자 자기 주소를 적었다.
      · **고치지 않은 것**: 지난 기간 띠의 "~ .5.29."는 **원문이 그렇게
        적혀 있다**(WLF00004717) — 원문 그대로 옮기는 게 맞다(3절).
      · ⚠ 이날 **Browser 창 캡처가 스크롤한 화면을 빈 지면으로** 찍었다.
        헤더까지 사라지는데 `elementFromPoint`로는 글이 있다 — 사이트가
        아니라 캡처 쪽 문제다. 스크롤 아래를 봐야 하면 위 형제 요소를
        잠깐 숨기고 맨 위에서 찍는다.
- [x] **09-11 라이브 점검 — 한 건 고침(09-11 배포), 나머지는 멀쩡.**
      · 전 페이지 안의 **내부 링크 1,011개**(사이트맵 밖 204개 포함) 전부 200 ·
        공유 썸네일 727개 전부 PNG · 본문에 `NaN`·`undefined`·HTML 기호 0건 ·
        모바일 가로 넘침 0 · 페이지 종류 9개 콘솔 오류 0.
      · **고친 것 — `/find` 뒤로 가기**. 청년·저소득(67건)에서 뒤로 가면
        결과는 청년 305건인데 상자에 「저소득」이 켜진 채 남았다. 09-09에
        "적용된 것과 켜진 것이 늘 같아야 한다"고 적어 둔 규칙이 **사이트 안
        이동**에서는 안 지켜진 것이다 — 상자가 첫 값으로만 상태를 잡는다.
        `key`에 적용 조건을 넣어 해결. 도구의 `navigate`는 페이지를 통째로
        새로 열어서 이 버그를 못 잡는다 — **링크를 실제로 누르고
        `history.back()`으로** 재야 한다.
      · **못 확인한 것** — 헤더 검색창에서 한글 조합 중 Enter를 누르면
        (`isComposing`을 안 봄) 크롬에서 두 번 처리될 수 있다. 같은 주소로
        두 번 가는 것이라 보이는 피해는 거의 없고, 이 브라우저로는 한글
        입력기를 흉내 낼 수 없어 **재현 없이 고치지 않았다.**
- [x] **관심 지원 저장을 만들었다 — ☆ (2026-09-11).** `lib/saved.ts`.
      상세 제목 아래 ☆ 단추, 첫 화면 「관심 지원」 칸(있을 때만), `/saved`
      전체 목록(noindex). **로그인 없이도 쓴다**(사용자 결정) — 이 브라우저
      저장소에만 두고, 로그인하면 계정과 맞춘다(아래 항목).
      · 최근 본 지원(`recent.ts`)과 따로 둔 이유 — 저절로 쌓이는 것과
        **고른 것**은 다르다. 최근 것은 여섯에서 밀려나지만 고른 것은
        지울 때까지 남아야 한다.
      · 개인정보처리방침 2조의 「브라우저에 남는 것」이 둘 → 셋이 됐다.
- [ ] **이메일 가입·인증·중복 방지 — 코드는 다 짰고 스위치는 꺼 둠 (2026-09-11).**
      사용자 결정: 누구나 저장 + 가입하면 계정으로 · **Supabase 새 프로젝트** ·
      이메일+비밀번호, 가입 시 인증 메일 · 메일은 **Resend**.

      ── 설계에서 지킨 것 ──────────────────────────────────────────
      · **스위치 하나(`NEXT_PUBLIC_AUTH_ON`)** 로 가입 화면·헤더 링크·방침의
        회원 조항이 **같이** 켜진다. 방침이 "회원가입을 받지 않는다"고 말하는
        동안 가입을 받는 순간이 생기면 안 된다. 꺼져 있으면 계정 화면은
        404이고 사이트는 전과 같다.
      · **브라우저는 Supabase에 직접 붙지 않는다.** 전부 서버 액션
        (`lib/auth/actions.ts`)을 거친다 → 키가 번들에 안 실리고, CSP를 안
        열어도 되고, 토큰 쿠키를 `httpOnly`로 둔다.
      · **정적 페이지 807개를 지켰다.** 헤더가 서버에서 로그인을 확인하면
        전 페이지가 동적이 된다. 헤더는 값 없는 표식 쿠키(`bc_member=1`)만
        보고, `/account`가 서버에서 다시 확인한다. proxy도 `/account`·
        `/reset` 두 곳에만 건다.
      · **중복 가입 막기** — Supabase는 이미 인증된 주소로 가입하면 오류
        대신 신원(identities) 0개짜리 가짜 계정을 준다(남의 가입 여부를
        못 캐게). 그걸로 가려 "이미 가입된 이메일"을 띄운다. 비밀번호 찾기·
        인증 메일 재발송은 반대로 **가입 여부를 말하지 않는다.**
      · **동기화 순서** — 페이지를 열면 계정 목록을 먼저 받아 브라우저를
        맞추고(처음 만난 브라우저면 먼저 합침), 그 뒤 ☆ 변경을 계정에 민다.
        순서가 뒤집히면 다른 기기에서 뺀 것이 되살아난다(`SavedSync`).
        **로그아웃·탈퇴하면 이 브라우저의 목록도 지운다** — 공용 PC.
      · 탈퇴는 즉시 삭제(비밀 키로 계정 삭제 → 관심 지원은 cascade).
        만 14세 미만은 받지 않는다(법정대리인 동의 절차가 없으므로).

      ── 09-11 코드 리뷰로 고친 여섯 (켜기 전에 잡았다) ──────────────
      · **열린 리다이렉트** — `safeNext`가 앞 두 글자만 봐서
        `/login?next=/%09/evil.com`이 통과했다(탭이 풀려 `/\t/evil.com` →
        브라우저가 탭을 지워 `//evil.com`). 제어문자·`\` 거절 + URL로 풀어
        출처 대조. 공격 주소 11가지로 시험해 전부 막히는 것을 확인.
      · **읽기 실패를 빈 목록으로 읽었다** → 장애 한 번에 브라우저 ☆가 지워지고
        「합쳤다」 표시까지 남았다. 실패는 null, null이면 아무것도 안 바꾼다.
      · **첫 동기화 중에 누른 ☆가 사라졌다** · **다른 기기에서 뺀 것이
        되살아났다** — 목록을 통째로 덮어쓰던 방식을 버리고 **넣기/빼기
        차이만** 보낸다(`pushSavedOps`). 받은 목록 위에 못 보낸 차이를 얹는다.
      · **로그아웃이 모든 기기에 걸렸다**(Supabase 기본값 global) → 다른 기기에
        앞사람 목록이 남아 다음 사람 계정에 합쳐질 수 있었다. `scope: "local"`
        + 로그인이 풀린 게 **분명할 때만** 브라우저 목록을 지운다(네트워크
        오류를 로그아웃으로 읽지 않게 `whoAmI`가 셋으로 가른다).
      · **처음 합칠 때 순서가 섞였다**(한꺼번에 넣은 줄의 시각이 같음) →
        한 줄씩 1초 간격으로 시각을 매긴다. 계정당 200건 상한도 서버에서 건다.
      · 곁다리로 둘 더 — 탈퇴 확인란 오타에도 목록이 지워지던 것, `/saved`
        안내문이 계정이 켜져도 "이 브라우저에만"이라고 하던 것.
      · ⚠ **편집 도구에 `\u0000` 같은 이스케이프를 적으면 진짜 제어문자로
        들어간다.** 그래서 `config.ts`가 한때 **바이너리 파일**로 읽혔다(grep
        「Binary file matches」). 동작은 맞았지만 git diff가 안 보인다.
        이스케이프가 든 줄은 **node 스크립트로 파일에 쓰고**, 쓴 뒤 제어문자
        개수가 0인지 센다.

      ── 켜기 전 준비 (**사용자가 할 일** — 계정·키는 내가 만들지 않는다) ─
      1. ~~Supabase 새 프로젝트~~ — **09-11 만들었다.**
         · 프로젝트 `bokjiclick` · ref `mngvwlhvnocpnrusmqho` ·
           주소 `https://mngvwlhvnocpnrusmqho.supabase.co` ·
           지역 **Northeast Asia (Seoul) `ap-northeast-2`** · 무료(NANO).
         · 조직 **`jgga3111-sudo's Org`** — 사용자가 GitHub `jgga3111-sudo`로
           로그인해 둔 곳이다. 이 조직에는 프로젝트가 **이것 하나뿐**이고,
           러닝온 Supabase는 다른 계정에 있어 섞이지 않는다. 0절 계정 표의
           「코드를 올린다」 줄에 Supabase를 더했다.
         · 만들 때 고른 것: Data API 켬 · **새 테이블 자동 공개 끔**(Supabase
           권장 — 그래서 SQL에 `grant`를 직접 적었다) · **자동 RLS 켬**.
           DB 비밀번호는 「Generate a password」로 만들었고 **우리는 쓰지 않는다**
           (앱은 API 키로 붙는다). 필요하면 대시보드에서 재설정한다.
         · ⚠ 만들기 단추를 「Supabase Select 2026」 광고 상자가 덮고 있어
           첫 클릭이 광고에 먹혔다. **덮개를 닫고 누른다.**
         · ⚠ **Site URL이 기본값 `http://localhost:3000`이었다**(09-11 확인).
           3000은 **러닝온 개발 포트**라, 그대로 두면 인증 메일 링크가 엉뚱한
           곳으로 간다. 4번에서 반드시 바꾼다. Confirm email·새 가입 허용은
           기본으로 켜져 있었고 익명 로그인은 꺼져 있었다(그대로 둔다).
      2. ~~SQL Editor에서 `supabase/migrations/0001_saved_services.sql` 실행.~~
         — **09-11 끝.** 실행 뒤 `pg_class`·`pg_policies`·`role_table_grants`로
         RLS 켜짐 · 정책 셋 · cascade 외래키를 값으로 확인했다.
         · ⚠ **자동 공개를 껐는데도 authenticated에 truncate·trigger·references가
           붙어 있었다.** truncate는 RLS를 안 거친다. 파일을 `revoke all … from
           anon, authenticated` 뒤 grant로 고쳐 다시 돌렸고, 지금은
           authenticated = DELETE·INSERT·SELECT, anon = 없음이다.
           (service_role에 남은 셋은 그대로 둔다 — 우리는 이 표를 비밀 키로
           만지지 않고, 탈퇴 cascade는 표 주인 권한으로 돈다.)
         · 「Potential issue detected — destructive operations」 경고는
           `drop policy if exists`·`revoke` 때문에 뜬다. 새 표라 지울 것이 없다.
         (코드 리뷰에서 권한·RLS에 구멍이 없음을 확인했다 — 로그인한 사람만
         자기 줄을 읽기·넣기·지우기, 로그인 안 한 사람은 아무것도 못 한다.)
      3. ~~Authentication → Email: **Confirm email 켬**, 비밀번호 최소 8자.~~
         — **09-11 끝.** 최소 길이 6 → **8**, Password requirements를 기본(조건
         없음) → **Letters and digits**로 바꿨다. 가입 화면의 `checkPassword`
         (8자 이상·영문과 숫자)와 같은 규칙을 서버에도 건 것이다 — 화면만 막으면
         API를 직접 부르는 사람은 6자짜리로 가입할 수 있다. Confirm email 켜짐 ·
         익명 로그인 꺼짐은 기본값 그대로 확인했다.
      4. ~~URL Configuration: Site URL `https://bokjiclick.co.kr`, Redirect URLs
         `https://bokjiclick.co.kr/**`·`http://localhost:3001/**`.~~ — **09-11 끝.**
         저장 알림("Successfully updated site URL" · "Successfully added 2 URLs")과
         목록 합계 2를 값으로 확인했다.
         · ⚠ **Browser 창이 접힌 채 새 화면을 열면 대시보드가 아예 안 그려진다**
           (본문 글자 0, 앱이 붙지 않음). 이미 떠 있던 화면은 접혀도 값을 읽고
           바꿀 수 있었다 — 그래서 URL 설정은 됐고 SQL 편집기는 못 열었다.
           새 화면으로 옮길 때는 창을 펼쳐 둔다.
      5. **09-11 끝** — SMTP 저장 뒤 「Confirm sign up」·「Reset password」 두 개를
         `supabase/email-templates.md`의 한국어 제목·본문으로 바꿨다(저장 알림
         "Successfully updated email template" 두 번, 다시 열어 token_hash 링크 확인).
         제목 칸 이름은 `MAILER_SUBJECTS_CONFIRMATION`·`MAILER_SUBJECTS_RECOVERY`,
         본문은 Monaco 편집기다.
         ⚠ **7번(SMTP) 뒤에 한다.** 09-11에 열어 보니 템플릿 화면이 「Set up
         custom SMTP to edit templates」로 잠겨 있었다 — 기본 메일 서버로는
         제목·본문을 못 고친다. 붙일 한국어 제목·본문은
         **`supabase/email-templates.md`**에 있다.
         메일 템플릿 두 개의 링크를 **token_hash 모양**으로 바꾼다 —
         Confirm signup: `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email`
         Reset password: `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery&next=/reset`
         기본 템플릿(`?code=`)은 **가입한 그 브라우저에서만** 통해서, 휴대폰으로
         가입하고 PC 메일로 열면 실패한다(`auth/confirm/route.ts` 머리말).
      6. Resend 가입 → 도메인 `bokjiclick.co.kr` 추가 → **가비아 DNS에 Resend가
         보여 주는 레코드 추가** → Verified → API 키 발급.
         · **09-11 도메인 추가함** — 계정 `jgga1234567`(0절 예외), 지역
           **Tokyo `ap-northeast-1`**, 도메인 id `5781fb03-7e18-4d47-853a-a55a0e187aaa`.
           방침 6조의 국외 이전 국가를 「미국」→「일본(발송 서버, 도쿄)·미국(본사)」로
           고쳤다(지역을 고르면 방침도 같이 본다).
         · 가비아에 넣을 레코드 — 이름은 **호스트 부분만** 적는다(가비아가
           `.bokjiclick.co.kr`을 붙인다). 09-11 조회로 이 이름들에 기존 레코드 없음.

           | 타입 | 호스트 | 값 |
           |---|---|---|
           | TXT | `resend._domainkey` | `p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQCok3QUX5Nz33HJO0A4VpQu4ZLKVWzrFuPxj21gLYLKQeZ63QyWkzQAS6CTtlBOQyxsCVkhPQ0NiVSeY2bR/w/NlfOYlKrVCvg9Uu9F5DAIlM/kjEMGWUsme61lslYrFSsC1EPN8CW3biW+gv5dtXT7RuijFNVU5aM9mFLDEWmNUQIDAQAB` |
           | CNAME | `rsend` | `rsend-apne1.forge.rmta.net` |
           | CNAME | `send` | `send.forge.rmta.net` |
           | TXT | `_dmarc` | `v=DMARC1; p=none;` (선택) |

           **09-11 12:55 가비아에 넣었다**(레코드 2 → 6개). 가비아 네임서버와
           8.8.8.8 둘 다 네 개를 바로 돌려줬고, DKIM 218자 전부 일치. 기존
           A `@`·CNAME `www`(Vercel)는 그대로다. CNAME 값은 기존 www처럼 끝에
           점을 붙였다(`…rmta.net.`).
           **Resend 인증 완료(09-11)** — 「Verify DNS Records」 뒤 `send`가 먼저
           verified, DKIM·`rsend`는 1분쯤 pending이다가 verified. 도메인 상태
           verified. 사이트(`bokjiclick.co.kr`·`www`)는 전후 모두 200.
           · ⚠ **가비아 DNS 편집 함정 셋.** ① 편집은 「레코드 수정」을 누르면
             뜨는 **팝업 안의 줄**에서 한다 — 같은 이름의 숨은 칸이 또 있어
             엉뚱한 칸을 채우기 쉽다. ② **타입을 바꾸면 그 줄을 새로 그려**
             먼저 넣은 호스트·값이 지워진다. 타입 먼저, 그다음 값. ③ 스크립트로
             값을 넣으면 줄 「확인」이 안 받는다 — **칸에 커서만 옮기고 실제
             키보드로 친다.** 줄마다 「확인」 → 끝에 「저장」 한 번이고, 저장하면
             페이지가 새로 열린다.
           **MX `@`(inbound…amazonaws.com)는 넣지 않는다** — 메일 *받기*용이다.
           우리는 보내기만 하고, 루트에 MX를 걸면 나중에 쓸 메일 서비스와 부딪친다.
         · ⚠ 화면의 값은 가운데가 `[…]`로 잘려 나온다. 복사해 옮기면 잘린 값이
           들어간다 — 위 표는 페이지 원문에서 뽑은 전체 값이다.
         · 추적은 **꺼져 있다**(`click_track=false`·`open_track=false`, 저장된 도메인
           값으로 확인). 추가 화면에서는 「Enable click tracking」이 **체크된 채
           회색**으로 보여 켜진 줄 알았는데, 추적 하위도메인이 없으면 적용되지
           않는 칸이었다. 켜면 인증 링크가 추적 주소를 한 번 거치므로 켜지 않는다.
           화면의 체크 표시보다 **저장된 값**을 본다.
      7. Supabase → Authentication → SMTP: `smtp.resend.com` · 465 · 사용자
         `resend` · 비밀번호 = Resend API 키 · 보내는 주소
         `noreply@bokjiclick.co.kr`. (기본 메일은 시간당 몇 통이라 운영 불가.)
         · API 키는 **이미 있다** — Resend 키 이름 `bokjiclick`, Sending access,
           도메인 `bokjiclick.co.kr`만(09-11 사용자가 만듦, 상세 화면으로 확인).
           같은 계정의 `dayonekorea` 키와 따로다. **새로 만들지 않는다.**
           키 값은 만들 때 한 번만 보이므로, 잃어버렸으면 이 키를 지우고 같은
           조건으로 다시 만든다.
         · 09-11 복지클릭 프로젝트의 SMTP 칸을 비밀번호만 빼고 채워 뒀다
           (보내는 이름 `복지클릭`, 최소 간격 60초). 비밀번호·저장은 사용자 몫.
         · **09-11 저장 끝**(사용자가 키 입력·저장). 확인은 템플릿 화면으로 했다 —
           「Set up custom SMTP to edit templates」 잠금이 풀리고 편집기가 열렸다.
           ⚠ 저장 뒤에도 SMTP 화면의 「Save changes」·「Cancel」이 켜진 채 남아
           "안 눌렸다"로 오판할 뻔했다. 단추 모양이 아니라 **잠금이 풀렸는지**를 본다.
           스위치를 켜면 인증 메일 한도가 **시간당 30통**으로 바뀐다는 안내가 뜬다.
         · ⚠ **러닝온 Supabase에도 SMTP 화면이 있다**(Gmail `runningon93`,
           보내는 이름 「러닝온」). 09-11에 사용자가 그 화면을 보고 "이미 저장돼
           있다"고 읽었다. SMTP 화면을 볼 때는 **탭 제목의 `bokjiclick`과 주소의
           `mngvwlhvnocpnrusmqho`**부터 확인한다.
      7-1. **실제 발송 시험(09-11, 사용자 요청 — `jgga1234567@gmail.com`)**.
         대시보드 Users → Add user → **Send invitation**(비밀번호를 우리가 안
         만드는 유일한 길) → 1분 뒤 그 가입자에 **Send password recovery**.
         Resend 기록으로 확인한 결과:

         | 제목 | 보낸 사람 | 상태 |
         |---|---|---|
         | [복지클릭] 비밀번호 재설정 링크 | `"복지클릭" <noreply@bokjiclick.co.kr>` | delivered |
         | You've been invited | `"복지클릭" <noreply@bokjiclick.co.kr>` | delivered |
         | (비교) 비밀번호 재설정 링크 | `Day One Korea <noreply@dayonekorea.co.kr>` | 09-10분 |

         사용자가 받은 재설정 메일 본문을 붙여 줬다 — 「복지클릭 / 비밀번호를 새로
         정하려면… / 새 비밀번호 정하기 / 요청하지 않으셨다면…」, 템플릿 그대로
         한국어로 도착했다. **Gmail 받은편지함**(스팸함 아님, 사용자 확인).
         → SMTP·Resend·DKIM이 한 줄로 돈다. **데이원코리아와 보낸 주소·이름·
         키가 전부 갈라져 있다.** 초대 템플릿은 영어 기본 그대로다(우리 흐름에
         초대가 없어 손대지 않았다).
         · ⚠ **시험 가입자 하나가 남아 있다** — `jgga1234567@gmail.com`(초대
           상태, id `5519689c-…`). 켜는 날 가입 중복 시험에 걸리므로 그 전에
           지울지 사용자에게 묻는다(삭제는 되돌릴 수 없어 사용자가 정한다).
           → 09-11 사용자가 "지워줘"라고 했다. **영구 삭제는 내가 누르지 않는다**
           — Users에서 그 줄을 열어 「Delete user」 단추 앞까지 띄워 두고, 누르는
           것은 사용자에게 넘겼다. 지워졌는지는 Users 목록이 비었는지로 확인한다.
         · Resend `/emails` RSC에서 `created_at`을 뽑으면 **메일마다의 시각이
           아니라 페이지 공통 값**이 잡힌다(네 통이 전부 같은 값). 시각이 필요하면
           목록 화면의 글자를 읽는다.
      7-2. **코드 배포(09-11 커밋 `47abdfa`)** — 관심 지원 ☆와 가입 코드(스위치 꺼짐)를
         올렸다. 라이브 확인: /saved 200·noindex, /signup·/login 404, 상세 ☆ 1개,
         사이트맵 807, 헤더 로그인 링크 0. 첫 시도는 auto 모드 권한 검사가 긴
         한 줄 명령을 막았다 — **커밋과 push를 짧은 명령 두 개로 나누면 통과한다.**
      8. 키 셋(`SUPABASE_URL`·`SUPABASE_PUBLISHABLE_KEY`·`SUPABASE_SECRET_KEY`)을
         Vercel 환경변수와 `.env.local`에 넣는다(이름은 `.env.example`).
         · **09-11 끝.** `.env.local`의 두 키는 사용자가 붙였다. 값은 안 보고 길이·접두어
           (`sb_publishable_`·`sb_secret_`)만 확인했고, 각 키로 우리 프로젝트를 한 번씩
           불러 **둘 다 200**을 받았다(가입자 0명 — 시험 가입자 삭제도 이걸로 확인됨).
         · Vercel에는 세 개를 **Secret · Production**으로 넣었다. 이름 셋과 URL 값은 내가
           채워 두고, 키 값 둘은 사용자가 붙여 넣고 저장했다. Secret은 저장 뒤 값을 다시
           볼 수 없으니, 값이 맞는지는 켜는 날 끝까지 도는 시험(10번)이 확인한다.
           `NEXT_PUBLIC_AUTH_ON`은 넣지 않았다. 환경변수는 넣은 뒤 **첫 배포부터** 들어간다.
         · ⚠ **앱 브라우저(Browser 창)에서는 Supabase 「Copy API key」가 안 먹는다**
           (클립보드가 막혀 있다). 메모장으로 `.env.local`을 열어 거기서 복사하게 했다.
      ── 켜는 날 (**내가 할 일**) ─────────────────────────────────
      9. 방침 9조가 "중요한 변경은 **최소 7일 전**에 안내"라고 약속한다 —
         켜기 7일 전에 바뀌는 내용을 먼저 알린다. 켜는 날 `SITE.policyEffectiveDate`
         를 그날로 올린다.
      10. Vercel에 `NEXT_PUBLIC_AUTH_ON=1` → 재배포 → 실제 메일로 가입·중복·
          재발송·로그인·비밀번호 찾기·동기화·탈퇴를 끝까지 한 번 돈다.
- [ ] **09-11 종합 점검 — 보안·경쟁 비교·성장 (사용자 요청, 보고까지만, 배포는 사용자).**
      세 갈래를 동시에 조사했다. 보안 취약점(npm audit) 0건, 뚫리는 구멍 없음.
      **로컬에서 고친 것 여덟(전부 검사 통과, 미배포)**:
      · 보안 다섯 — ① 비밀번호 바꾸면 다른 기기 로그아웃(`signOut({scope:"others"})`)
        ② `/search` 검색어 120자 상한(`/ask`와 같게) ③ `Cross-Origin-Opener-Policy:
        same-origin-allow-popups` 헤더(광고 새 창은 그대로) ④ 방침 2조를 `AUTH_ON`
        분기로 — 켜면 "관심 지원은 로그인 시 계정에 저장"과 넷째 항목(동기화 표식)
        ⑤ AuthForms 방침 링크 `rel=noopener`.
      · **정확성 하나 — 상세 「온라인 신청 안 됨 / 인터넷 신청은 받지 않습니다」를
        「복지로 온라인 신청 — 연동 없음」으로.** 원문 필드 `onapPsbltYn`은 복지로
        연동 여부일 뿐인데 "인터넷으로 못 한다"로 세게 말했다(K-패스는 korea-pass.kr
        에서 신청). 3절 위반. 중앙 330건 전부 이 칸이 있다. `/guide/tax-credit`은
        원래부터 "복지로에서는 안 됨"이라고 바르게 쓰고 있었다.
      · SEO 둘 — ⑦ **807쪽 전부 og:url이 홈**이었다(layout의 `openGraph.url`을
        하위가 상속). 지웠다 — 페이지마다 적는 것보다 안 내보내는 쪽이 틀릴 수
        없다. ⑧ `max-image-preview:large`(디스커버 요건, 비용 0).
        ⚠ `generateMetadata`의 `robots: 조건 ? {index:false} : undefined`는 **undefined도
        덮어쓴다** — 상세·허브 4곳에서 layout의 robots가 안 물려졌다. `ROBOTS_INDEX`
        (site.ts)를 넘기게 고쳤다. 빌드 HTML로 확인하기 전엔 몰랐다.
      · ⚠ 이 저장소는 **CRLF**다. node로 패치할 때 `\r?\n`을 넣어야 찾는다(09-11에
        두 번 못 찾았다).
      **사용자 결정 남은 것**: Vercel Firewall 속도 제한(로그인·가입·재설정 POST) ·
      Supabase 「Secure password change」 · 탈퇴에 비밀번호 재확인 · 네이버
      서치어드바이저(네이버 유입 0 실측) · 검수 기준 페이지 · 허브 좁히기 칩 ·
      Article JSON-LD · 콘텐츠 캘린더(데이터랩 실측: 국민취업 0.76·국민연금
      예상수령액 0.72·육아휴직 급여 0.69·기초연금 수급자격 0.49, 실업급여=1;
      대부분 1월 정점·에너지바우처 12월 35×). PSI는 429로 미측정 — 다음에 잰다.
      경쟁 비교 결론: 축·계산기·/ask·/find는 앞서고, 뒤처진 자리는 상세의 "그래서
      어디서 신청하나"와 검수 기준을 화면에 안 말하는 것. 복지로는 못 열었다.
      하지 말 것 재확인: 나이·소득 숫자 필터, ✅/❌ 판정 아이콘, D-day, 댓글,
      시군구 페이지, 스톡 사진, CSP script-src.
- [x] **09-11 종합 점검 후속 — 개선 여섯 가지 (사용자 "모두 진행", 배포).**
      **2차 `0f239c7` 라이브 확인** — /standards 200 · 사이트맵 808 · K-패스 「신청 전
      체크」·「처리 절차(원문)」·창구 안내 · 「한눈에 보기」 0 · 청년 허브 새 제목과 칩
      「저소득 67」 · 새 홈 제목 · 안내 글 Article · 방침 「네 가지」. 배포 감지 15초.
      ~~⚠ 남은 후속: 빙 사이트맵 재제출 · 구글 색인 요청~~ — **09-12에 끝냈다**(아래
      「09-12 색인 작업」). 빙 재제출 + URL 2건, 구글 14건 요청.
      1차(`cd938df`)로 정확성·보안·og:url을 먼저 올리고 라이브 확인했다(og:url 0 ·
      max-image-preview · 새 문구 · COOP 헤더 · 사이트맵 807). 2차로 아래를 올렸다.
      · **상세 「신청 전 체크」** — 옛 「한눈에 보기」(분류 태그·행정 정보)와 「소득
        기준」 배너를 합쳤다. 줄마다 값이 있을 때만 그린다: 자격 조건(원문 첫 문장
        120자, 끊기면 「전문 보기」) · 지역 · 신청 기간(본문 문장에서 뽑은 것만, 9건) ·
        시행 기간(끝날이 실제 날짜일 때만, 15건) · 접수 방식(지자체만) · 소득 기준선
        (+자가진단 비교) · 담당 · 기준연도. **"없음"이라고 적지 않는다.**
        · 줄 이름을 「담당 부처」로 했다가 되돌렸다 — 지자체 값은 「옥천군 미래전략국
          성장정책과」 같은 과 이름이다. 「부처」는 중앙 기관을 가리키는 말이다.
        · 문장이 마침표로 끝났는데 "…"를 또 붙여 「지원합니다.…」가 됐다(화면 확인).
      · **중앙부처 「신청 방법」 → 「처리 절차(원문)」** + 위에 창구 안내 한 줄. 원문
        `applmetList`는 기관 쪽 처리 단계인데 1·2·3으로 세워 두니 신청자가 할 일로
        읽혔다. 목차 칩과 절 제목이 같은 함수(`applyTitle`)를 본다. 빌드 HTML로
        **중앙 330건 전부 붙고 지자체 570건에는 0건**임을 셌다.
      · **검수 기준 페이지 `/standards`** — 3절·5절을 읽는 사람 쪽으로 옮겼다. 숫자는
        렌더 시점 집계. 색인 대상이라 사이트맵 **807 → 808**, 푸터·홈 출처 줄에 링크.
      · **허브 「이 안에서 더 좁히기」 칩** — 생애주기·대상·지역 허브에서 `/find`로.
        건수는 `lib/narrow.ts`가 **/find 필터를 글자 그대로 옮겨** 센다(한쪽을 고치면
        다른 쪽도). 실측 청년×저소득 67 — 기록과 같다. 주제·혜택 허브는 /find가 그
        축을 안 받아서 칩 대신 "그 조건은 이어지지 않는다"고 적은 링크만 단다.
        · 지역 칩 건수는 전국 공통(중앙)을 포함해 세서 서울 122·경기 139처럼 비슷해
          보인다. /find와 같은 범위라 그대로 두고, 칩 줄에 그 사실을 적었다.
      · **허브·홈 제목·설명** — 허브 12~19자 → 30~40자(건수 + 실제로 많은 혜택,
        `lib/hubMeta.ts`), 홈 19자 → 37자·설명 37자 → 98자. 곁말은 전부 데이터에서 센다.
        · 곁말을 가운뎃점으로 이으니 「현금·바우처·서비스·돌봄」이 넷으로 읽혔다
          (「서비스·돌봄」이 한 이름). **쉼표로 잇는다.**
      · **지난번 조건** — /find에서 본 조건(슬러그·건수만)을 localStorage에 한 건,
        첫 화면에 "지난번: … → 다시 보기". 방침 2조 저장 항목이 셋 → 넷(켜면 다섯).
      · **안내 글 Article JSON-LD** — 글마다 붙이지 않고 모든 글 끝의 `GuideNav`에서
        내보낸다(빠뜨릴 수 없게). datePublished는 git 첫 커밋일(`lib/guidePublished.ts`,
        19편), dateModified는 guides.ts의 updated.
      · **안 한 것**: 안내 글 제목에 연도 붙이기 — 제목이 이미 30~48자이고 글 대부분이
        해에 묶이지 않는다. 신선도는 Article의 dateModified가 말한다.
      ── 이번에 걸린 함정 넷 ─────────────────────────────────────
      · **작업 에이전트 넷이 사용량 한도로 한꺼번에 죽었다.** 죽기 전에 파일 일부를
        남겼다(상세 페이지는 옛 부품만 지우고 새 부품을 안 끼운 채 → 타입 오류 3).
        버리지 않고 읽어 보고 이어 붙였다. 병렬로 맡길 때는 **끊겨도 반쯤 된 상태가
        남는다**는 걸 전제로, 재개 전에 `git status`와 tsc부터 본다.
      · **긴 heredoc 명령이 중간에 잘렸다**(165줄에서 "matching quote" 오류). 세 조각으로
        나누니 들어갔다. 패치 스크립트는 파일별로 작게 나눈다.
      · 데이터 파일에서 배열을 `indexOf("[")`로 찾으니 `WelfareService[]` 타입 표기를
        잡았다 — 그래서 "지자체 표본"이 중앙부처였다. `= [`로 찾는다.
      · auto 모드 권한 검사가 커밋+push를 한 줄로 이으면 막는다. **두 명령으로 나눈다.**
      **사용자 결정 남은 것**(변동 없음): Vercel Firewall 속도 제한 · Supabase
      「Secure password change」(**09-11 켬** — 아래 「보안 설정 둘」) · 네이버 서치어드바이저 토큰(09-11 넣음) · PSI 재측정(429였음).
- [x] **09-11 보안 설정 둘 — Supabase 「Secure password change」 켬 · Vercel 방화벽 속도 제한.**
      · **Supabase 「Secure password change」 켬** — 복지클릭 프로젝트(`mngvwlhvnocpnrusmqho`)
        Auth → Sign In / Providers → Email. 저장 알림 "Successfully updated settings",
        새로고침 뒤 다시 열어 켜진 것을 값으로 확인했다. 「Require current password when
        updating」은 **꺼 둔다** — 비밀번호 찾기 링크로 들어온 사람은 옛 비밀번호를 모른다.
        켜 두면 로그인한 지 24시간 지난 세션의 비밀번호 변경이 `reauthentication_needed`로
        막히는데, `lib/auth/actions.ts`가 이미 "로그아웃한 뒤 비밀번호 찾기 메일로" 안내한다.
      · **Vercel 방화벽 속도 제한** — 규칙 `bokjiclick auth rate limit`: Method = POST
        **그리고** 경로가 /login·/signup·/forgot·/reset 중 하나 → IP당 60초에 10회, 넘으면 429.
        GET까지 세면 헤더 링크의 미리 불러오기(prefetch)가 한도를 먹어서, 빨리 둘러보던
        사람이 로그인을 누를 때 429를 받을 수 있다. 그래서 POST만 센다. 게시 뒤 라이브에
        POST를 연달아 보내 **10번째부터 429**가 나는 것을 확인했다(홈·상세 GET은 200 그대로).
        「Add Rule」을 누르니 **요금 안내**가 떴다 — 통과한 요청 100만 건당 $0.50, 막은 요청은
        무료. 결제가 붙는 일이라 사용자에게 물었고 **동의받아(09-11)** 저장했다.
      · ⚠ **가려진(hidden) 크롬 탭**에서는 스크린샷이 안 되고, Chrome이 타이머를 늦춰
        `setTimeout`을 쓰는 스크립트가 도구의 45초 제한을 넘긴다. 넘겨도 페이지 안에서는
        계속 돌아서, 늦게 끝나며 뒤에 한 조작과 겹칠 수 있다 — **다시 조작하기 전에 상태부터
        읽는다.** Radix 메뉴는 `PointerEvent('pointerdown')`로 열린다.
      · 조건 줄을 하나 더할 때 **지우기 단추를 범위 없이 찾으면 첫 줄 것을 누른다**(첫 시도에
        경로 목록이 날아갔다). 첫 줄을 먼저 Method로 바꾸고 AND로 둘째 줄을 더하면 헷갈릴 게 없다.
      · **09-12 Supabase Advisor 경고 2건을 없앴다 — `public.rls_auto_enable()` 실행 권한 회수.**
        프로젝트 만들 때 「자동 RLS 켬」을 고르며 생긴 함수인데 `proacl`이 **비어 있었다** —
        비었다는 것은 기본값, 즉 **PUBLIC에게 실행 권한이 있다**는 뜻이다. 로그인 없는 키로
        `/rest/v1/rpc/rls_auto_enable`을 쏘니 400 `cannot display a value of type event_trigger`가
        왔다 — 거부가 아니라 **함수까지 들어갔다는 뜻**이라 경고가 빈말이 아니었다.
        `revoke execute on function public.rls_auto_enable() from public, anon, authenticated;`
        뒤 `acl {postgres=X/postgres}` · `has_function_privilege` 둘 다 false · 같은 REST 호출이
        **401 `42501 permission denied`** · Advisor 0/0/0. 이벤트 트리거는 시스템이 표 주인
        권한으로 돌리므로 **새 표 자동 RLS는 그대로**다. 되돌리려면 `grant execute … to anon, authenticated;`.
        · ⚠ **auto 모드 권한 검사가 `revoke`가 든 스크립트 실행을 막는다.** 우회하지 않고 편집기에
          문장만 넣고 Run은 사용자가 눌렀다. Supabase가 「파괴적 명령일 수 있다」 상자를 한 번 더
          띄우므로 **"Success"를 못 보면 그 상자에서 멈춘 것**이다 — 처음에 실제로 그랬고,
          화면을 믿지 말고 **권한 값을 다시 읽어** 안 들어간 것을 잡아냈다.
        · ⚠ **Supabase 대시보드는 탭이 뒤에 있으면 화면을 아예 안 그린다**(글자 0). 앱 브라우저
          창이든 크롬이든 **앞으로 꺼내 놔야** SQL 편집기를 쓸 수 있다. Vercel은 가려져도 그려진다.
        · 마무리 점검(09-11 밤~09-12): tsc·eslint 0 · 빌드 0 · audit 둘 통과 · 라이브 사이트맵
          808개 전부 200 · Vercel 오류 0 · 함수 오류율 0%.
      · 키 셋(`SUPABASE_*`)은 같은 날 `.env.local`과 Vercel(Secret · Production)에 넣었다 —
        위 「이메일 가입」 항목의 8번.
- [x] **09-12 색인 작업 — 빙 사이트맵 재제출 + URL 2건 · 구글 14건 요청.**
      09-11 배포(`0f239c7`)의 남은 후속이다. 새 기준(「좋은 글 위주로만」)대로
      **새 페이지 · 내용이 실제로 바뀐 페이지**만 넣었고 조회수 백로그는 안 건드렸다.
      · **빙** — 라이브 808 대 빙 807로 벌어져 있었다. `⋮ → Re-submit` 뒤 Last submit
        9/8 → **9/12**, Status `Success` → **`Processing`**을 값으로 확인했다.
        `URLs discovered`는 빙이 다시 긁은 뒤에 바뀐다(807 그대로 — 실패 아님).
        URL 제출은 **`/standards`와 홈 둘만** 넣었다. 목록 총계 800 → **801**
        (홈은 09-10에 이미 넣어 둔 것이라 안 늘었다), 할당량 100 → **98**.
        · 사이트맵 808 = **상세 707 + 나머지 101**이고 빙에 들어간 800 = 상세 700 +
          나머지 100이다. 차이 8은 **일부러 뺀 상세 백로그 7건 + 새로 생긴 `/standards` 1건**.
          이렇게 갈라 세면 「무엇이 아직 안 갔나」를 목록을 뒤지지 않고 알 수 있다.
      · **구글 14건 요청** — `/standards`(새 페이지) · 홈 · 조회수 상위 상세 10건 ·
        허브 둘(`/life/youth`·`/target/low-income`). 상세는 09-11에 「신청 전 체크」와
        「처리 절차(원문)」가 들어가 **내용이 실제로 바뀐** 것들이다.
      · **상위 10건 중 셋이 아직 미색인이었다** — 이동통신요금감면(7위)·기초연금(8위)·
        유아학비(10위). `/target/low-income` 허브와 `/standards`도 미색인.
        **조회수가 높다고 색인된 것이 아니다** — 요청 전에 검사부터 하는 이유가 이것이다.
        (요청 14건 중 미색인 5 · 이미 색인돼 내용 변경으로 재요청 9.)
      · ⚠ **「할당량 초과」가 떴는데 재시도로 들어갔다.** 09-09에 "이 문구를 보면 그날은
        끝"이라고 적어 뒀는데 **오늘은 아니었다** — 유아학비에서 뜬 것을 1분 뒤 같은 URL로
        다시 넣으니 「색인 생성 요청됨」이 나왔고, 그 뒤 두 건도 연달아 들어갔다.
        → **「할당량 초과」도 한 번은 재시도해 본다.** 두 번째도 같으면 그때 그날 몫으로 남긴다.
      · 한 건에 **약 80초**. 크롬 탭이 **가려져 있어도**(`visibilityState: hidden`) 스크립트는
        제 속도로 돌았다 — Supabase와 달리 GSC는 뒤에 있어도 화면을 그린다.
        `computer`의 스크린샷은 여전히 30초에 끊기므로 **좌표를 안 쓰고 자바스크립트로만** 돌렸다
        (09-09에 정한 방식 그대로, 좌표 사고 0).
- [x] **09-13 이번 달 신청 달력 카드 · 「캘린더에 담기」 · 인기 순위 10개 (`bcbf85a`, 배포).**
      사용자 요청 넷 — 네이버 캘린더 알림 · 알림 서비스 · 첫 화면 이번 달 마감 달력 · 인기 1~10위.
      · **인기 순위는 이미 있었다**(「사람들이 가장 많이 찾는 지원」, 8개). 10개로만 바꿨다.
      · **수록 데이터의 마감일로는 달력을 못 만든다.** 900건 중 `applyEnd`가 실제 날짜인 것 16건
        (없음 330 · `9999-12-31` 554), 이번 달 마감 **1건**. 그래서 첫 화면 카드는 공식 출처에서
        손으로 확인한 `lib/calendar.ts` 14항목을 쓴다. 9월 6건 · 가장 많은 달 12월 7건.
      · 사용자 결정(09-12): **알림은 「캘린더에 담기만」** · **모양은 목록 카드**(날짜 칸 그리드는
        기간이 여러 달에 걸친 항목이 절반이라 칸이 거의 다 칠해져 급한 것이 안 보인다).
      · **네이버에는 「링크로 일정 추가」 주소가 없다.** 공식 캘린더 API는 OAuth 로그인이 필요하고
        받는 본문이 iCalendar 문자열이다 → `.ics`가 네이버에서도 정식 경로. `/calendar/<key>.ics`
        12개를 정적 생성하고 **3일 전 알림(VALARM)**을 넣었다 — 이용자 캘린더가 울리므로 우리는
        이메일을 안 받는다(방침 수정·7일 공지 불필요). 구글은 링크 한 번(`action=TEMPLATE`).
      · `calendar.ts`에 `key`·`start`·`end`를 더했다. **`period`가 명확히 말한 날짜만** 옮기고
        「9월 말까지」·노인일자리(다음 해 모집일 미발표)는 null → 단추 없음. 근로장려금 넷은 period에
        연도가 없어 2026으로 읽었다 — **해가 바뀌면 이 넷을 다시 확인한다.**
      · 첫 화면 카드: 끝나는 날 가까운 순 4개 + 「남은 N가지 더 보기」, 지난 기간은 「기간 지남」·단추
        없음·맨 뒤. 오늘 날짜는 **현지 시각**으로 만든다(UTC면 한국 새벽에 어제가 되어 달이 틀린다).
        설명(note) 줄은 뺐다 — 넣으니 카드가 714px, 빼서 594px. 375px 첫 화면 4,662 → **5,343px**.
      · `.ics` 실측: CRLF · 75옥텟 접기(한글 글자 중간에서 안 자름) · DTSTAMP는 확인일로 고정(빌드마다
        파일이 안 바뀌게). 링크만 만드는 `calendarLinks.ts`와 파일을 짓는 `ics.ts`를 갈랐다 — 첫 화면
        카드가 클라이언트라 `ics.ts`를 물면 브라우저 번들로 딸려 간다.
      · 라이브: 배포 감지 90초 · 사이트맵 808 전부 200 · `.ics` 12개 200 `text/calendar` · 날짜 미확정
        둘과 없는 주소 404 · /guide/calendar 담기 단추 44(47칸 중) · 홈 순위 10줄 · 콘솔 오류 0.
      ── 이번에 걸린 함정 셋 ──────────────────────────────────────
      · ⚠ **Bash 도구의 heredoc(따옴표 친 `EOF`여도)에서 역슬래시가 한 겹 벗겨진다.** `ics.ts`의
        이스케이프 함수가 망가져 tsc가 「Unterminated regular expression」을 냈고, 감사 스크립트의
        정규식 공백 기호가 글자 s가 되어 **조용히 0개를 셌다**(오류 없이 틀린 수). 역슬래시가 드는
        코드는 node에서 `String.fromCharCode(92)`로 짓거나, split처럼 역슬래시 없는 코드로 쓴다.
      · **저장소가 전부 CRLF는 아니다.** `page.tsx`는 CRLF인데 `calendar.ts`·`ThisMonth.tsx`는 디스크에서
        LF였다. 패치 스크립트는 **파일마다** 줄바꿈을 읽어 맞춘다(`includes(CR+LF)`로 판정).
      · 구글 요청 도중 **가려진 크롬 탭의 타이머가 1분 단위로 늦춰졌다**(어제는 멀쩡). 2건에 6분.
        기다리는 함수를 MessageChannel 반복으로 바꿔 끼우니 한 건 80~120초로 돌아왔다. Blob 워커는
        서치콘솔의 Trusted Types 정책에 막혀 못 쓴다.
- [x] **09-13 색인 작업 — 구글 20건 · 빙 50건 (사용자 지정 수).** 전부 **09-11~09-13에 내용이 바뀐** 것.
      · **구글 20건 전부 「색인 생성 요청됨」**(오류·할당량 0). 홈 · `/guide/calendar` · 조회수 11~28위
        상세 18건(09-11에 「신청 전 체크」·「처리 절차(원문)」가 들어간 중앙부처 사업).
      · **20건 중 13건이 미색인이었다** — 부모급여(11위)·정신건강 심리상담 바우처(12위)·초중고 교육비·
        긴급복지 생계·교육급여·기저귀·청소년특별지원·근로·자녀장려금(23위)·희망저축계좌·생계급여·
        디딤씨앗통장·교육정보화·자립수당. **상당수가 09-04~09-08에 이미 요청했던 것**이다(기저귀는
        09-08). 요청하고도 닷새~아흐레째 안 붙은 상위 상세가 많다 — 요청을 되풀이할 문제가 아니라
        **구글이 이 상세들을 복지로 원문과 겹친다고 보는지**를 따져 볼 신호다. 안내 글은 요청하면
        하루 만에 붙었다(09-10 기록)는 것과 대조된다. 다음에 색인 보고서로 확인한다.
      · **빙 50건** — 홈 · 안내 글 20(목록 포함) · 생애주기 8 · 대상 7 · 지역 14. 「Success: 50 URLs
        submitted」, 할당량 98 → 48. 전부 09-10에 넣었던 주소라 목록 총계는 801 그대로다(다시 넣어도
        할당량은 쓰고 총계는 안 는다). 09-11에 허브 제목·좁히기 칩·Article JSON-LD가 바뀐 몫이다.
- [x] **09-13 복지 정보 변경 점검 · 마감 처리 · 세종 10건 수록 (`1b8f7e5`, 배포).**
      사용자 요청 — "오류나 변경된 것 확인 · 새로운 정보 확인 · 마감된 것은 마감 처리 따로".

      ── 점검하는 법 (다음에도 이 순서) ──────────────────────────────
      1. `node --env-file=.env.local scripts/check-updates.mjs <날짜>` — 목록을 새로 받아
         (6콜) `data-research/check/<날짜>/`에 두고 `parsed.json`과 대조. **parsed.json을
         통째로 바꾸지 않는다** — 조회수만 달라져도 900위 경계가 들고 나 색인된 상세가 404가 된다.
      2. 다시 받을 상세(목록이 바뀐 것 + 상위 100건)를 `detail-before/`에 백업 → `detail-raw/`에서
         지움 → `fetch-detail.mjs`(지운 것만 받는다) → `scripts/check-details.mjs <날짜>`로 대조.
         중앙 84건이면 중앙 한도 100 안이다.
      3. **내용이 같은 것은 옛 원본으로 되돌린다**(순서·조회수만 다름). 바뀐 것만 남긴다.
      4. 목록 필드(사업명·부서·최종수정일)는 parsed.json에서 **그 필드만** 고치고 `inqNum`은 둔다.
      5. `data-research/rechecks.json`에 날짜별 rechecked·changed·added → `build-data.mjs <수록> 2026-09-04`.
         받은 날(`SERVICES_UPDATED`)은 올리지 않는다 — 항목별 날짜가 따로 있다(아래).

      ── 결과 ──────────────────────────────────────────────────────
      · 목록: 중앙 461 그대로 · 지자체 4,758 → 4,818. **세종특별자치시 0 → 53건**(08-31엔 지자체 사업이
        아예 없던 지역) · 없어진 3건은 수록 밖.
      · 상세 109건 재수집: **99건 내용 같음 · 10건 실제 변경.** 금액·자격에 닿는 것 —
        광명시 전월세 대출이자 월세 환산율 **5.5% → 4.75%**·신청 「방문·우편」→「홈페이지 온라인·방문」,
        여성농업인 복지바우처 자격 「2023.1.1 이전 등록」→**「2025.1.1 이전」**, 167위 사업명
        「지역사회 통합건강증진사업」→「통합건강증진사업」·기준연도 2025→2026, 전화·담당과·근거법 몇 건.
      · 사이트맵 lastmod가 움직인 기존 상세 **8건뿐**(나머지 2건은 원래 noindex).

      ── 항목별 날짜 셋 (`types/welfare.ts`) ─────────────────────────
      전에는 확인일이 `SERVICES_UPDATED` 하나라, 올리면 안 본 791건까지 "09-13 확인"이 되고 안 올리면
      본 109건이 "09-04 확인"으로 남았다. 그래서 `checkedAt`(다시 받아 대조한 날) · `changedAt`(내용이
      달라진 날, lastmod용) · `addedAt`(나중에 새로 수록한 날)을 둔다. 상세 문장은 네 경우로 갈린다 —
      "09-04에 받아 정리" / "+09-13에 다시 받아 대조했고 내용은 그대로" / "+바뀐 내용을 반영" /
      새로 넣은 것은 "09-13에 받아 정리"(다시 대조한 적이 없으므로 덧붙이지 않는다).
      허브 lastmod도 **그 목록 안 changedAt의 최댓값**으로 바꿨다(23개가 09-13, 해당 없는 허브는 그대로).

      ── 마감 처리 ──────────────────────────────────────────────────
      · 전에는 상세 띠(`PastPeriodNotice`)에서만 보였다 — **조회수 1위 청년내일저축계좌('26.5.20. 모집 끝)가
        첫 화면 순위표 맨 위에 표시 없이** 있었다.
      · `src/data/closing.ts`(build-data가 생성) — 끝날이 적힌 사업만 담은 작은 표. 갈래 셋:
        `period` 본문의 신청 기간(띠와 같은 `statedApplyPeriod`) · `stated` **원문이 마감이라고 적은 것** ·
        `program` 사업 시행 종료일(9999는 뺌). 판정은 **브라우저가 오늘로**(`useLocalToday`) — 빌드한 날로
        굳히지 않는다. 09-13 기준 **11건**(신청 기간 9 · 원문 표기 2). 청년희망적금(9/30 종료)은 10/1에 배포 없이 붙는다.
      · 「마감」 딱지: 카드 · 첫 화면 순위표 · 허브 「이름으로 훑어보기」 · 상세 제목 옆 · 검색 · 질문 ·
        관심 지원 · 최근 본 지원 · 자가진단 결과. 목록에서 **빼거나 순서를 바꾸지 않는다**(3절 — 숨기지 말고
        마감으로 표기). 따로 모아 보는 자리는 `/service` 맨 아래 「기간이 지난 지원」.
      · 「마감됐다」고 단정하지 않는 원칙은 그대로 — 딱지는 짧게, 마우스를 올리면 "원문에 적힌 신청
        기간(…)이 지났습니다" / "원문에 「신청 마감」이라고 적혀 있습니다".
      · **`stated` 규칙은 좁게 뒀다** — `(신청|접수|모집) 마감`만 보고, 앞에 `소진`·`시`가 붙거나 뒤에
        `가능`·`될`이 오면 거른다(「예산소진시 마감」은 아직 안 끝난 것). 「사업종료」는 안 본다 —
        상병수당의 「'24.12월 사업종료」는 일부 지역 1단계 얘기다. 걸린 것: 고양 청년둥지론(사업명
        「- 신청 마감」) · 청년 주택임차보증금 이자(「<2025년도 신청 마감>」).

      ── 세종 10건 수록 (사용자 결정, 900 → 910) ────────────────────
      · 조회수가 900위(6,447)를 넘는 새 사업 10건이 **전부 세종**이었다. 1위 「세종 청년 미래적금」(86,409,
        약 88위). 10번째가 6,448이라 경계에 1 차이.
      · **기존 900건은 한 건도 안 빠진다**를 먼저 셌다 — 901위 6,446 < 넣을 최소 6,448이라 동점이 없다.
        새 10건만 parsed.json에 더하고(나머지 53건은 안 넣음) `build-data.mjs 910`.
      · 사이트맵 808 → **817**(상세 8 + `/region/sejong`). 청년 미래적금·노인일자리는 원문 본문이 짧아 noindex다.
      · 렌더된 HTML에 옛 「900건」 0곳 — 숫자는 전부 계산값이었다. 홈 제목이 「910건」으로 바뀌었다.

      ── 이번에 걸린 함정 다섯 ──────────────────────────────────────
      · **점검 폴더에 백업한 `services-before.ts`를 tsc가 읽어** 오류 수백 줄이 났다. 백업은 `.ts`로 두지 않는다.
        점검 원본 덤프는 `.gitignore`로 빼고 `report.json`·`recheck-ids.json`만 올린다.
      · **접힌 `<details>` 안은 `innerText`가 빈 문자열이다.** 허브 이름 목록에 딱지가 "없다"고 읽었는데
        실제로는 붙어 있었다 — `textContent`로 센다(7절 「검사 쪽을 먼저 의심한다」).
      · 반대로 **이름 끝의 "마감"을 딱지로 잘못 센 덕에 진짜 마감(고양 둥지론)을 찾았다.** 틀린 수가 나오면
        버리지 말고 무엇이 걸렸는지 본다.
      · 브라우저 콘솔 기록은 **개발 서버를 다시 띄워도 남는다.** 편집 도중(훅 호출을 먼저, import를 나중에 넣은
        사이) 난 오류가 새 서버에서도 보였다 — 같이 찍힌 웹소켓 번호가 멈춘 서버 것인지로 가르고, 목록이
        오류 경계 없이 그려지는지로 확인했다.
      · 창이 접힌 브라우저에서 잰 가로 폭은 **innerWidth 0**이라 넘침이 거짓으로 나온다. 375px로 정하고 잰다.

      ── 남은 일 ──────────────────────────────────────────────────
      · 새 URL 9개(세종 상세 8·/region/sejong)와 내용이 바뀐 상세 8개는 **다음 색인 요청 때 넣는다**(좋은 글 기준).
        빙은 사이트맵 재제출(808 → 817).
      · 세종 나머지 43건과 다른 지역 새 사업 10건은 조회수가 기준 아래라 안 넣었다. 다음 점검 때 다시 본다.
- [x] **09-13 마감 D-day — D-10부터 줄어들고 다음 날 「마감」 (`3808c45`, 배포).**
      사용자 질문 "마감기한이 있으면 하루 지날 때마다 D-2 → D-1로 줄어들게 가능해?" → 사용자 결정
      **「D-10일부터 표시하고 마감으로」.** 09-09의 「D-day는 안 한다」를 뒤집은 것이다 — 그때 이유(날짜 있는
      사업이 거의 없음)는 그대로지만, 마감 표(`closing.ts`)가 생겼고 **열흘 창**이 이상한 숫자를 막는다.
      · 09-13 실측: 끝날이 있는 것 22건(수록 + 신청 달력) 중 30일 안 3 · 90일 안 7 · 수록 910건의 1.4%.
        날짜가 D-2부터 **D-26772**(2099년 시행 기간 형식값)까지 퍼져 있어, 창 없이 걸면 이상한 사이트가 된다.
      · `lib/dday.ts` — 남은 날수를 **UTC 자정끼리 빼서** 센다(현지 시각이 섞이면 하루 어긋남). 0이면 「D-day」.
        `ClosedBadge` → **`DeadlineBadge`**로 이름을 바꾸고 `deadlineState(id, today)` 한 함수가 soon/closed를 가른다 —
        같은 표·같은 오늘을 봐서 **D-day 다음 날이 곧바로 「마감」**, 비는 날이 없다.
      · 붙는 자리: 「마감」과 같은 아홉 곳 + 상세 붉은 띠 `DeadlineNotice`("D-6 — 원문에 적힌 사업 기간(…)이
        6일 뒤에 끝납니다. **예산이 떨어지면 그 전에 끝날 수 있으니** 공식 안내에서…") + 첫 화면 이번 달 카드와
        `/guide/calendar`의 `DdayChip`(신청 달력 항목). 색은 마감(호박)과 갈라 **붉은색** — 할 일이 반대다.
      · 판정은 브라우저가 오늘로(`useLocalToday`) — **배포 없이 하루마다 줄어든다.** 09-13 라이브에 뜨는 것은
        첫 화면 카드의 근로장려금 반기신청 **D-2**(9/15) 하나. 청년희망적금(9/30)은 9/20에 D-10으로 나타난다.
      · 검증: 계산 함수를 날짜별로 돌려 9/19 없음 → 9/20 D-10 → 9/29 D-1 → 9/30 D-day → 10/1 마감, 연말·윤년 날수 확인.
        화면은 **`Date`를 12/25로 바꿔 끼우고 `window.next.router.push`로 사이트 안 이동**해 봤다 — 서울 디딤돌소득
        (12/31) 딱지·띠 D-6, 12/21 D-10, 12/31 D-day(「오늘 끝납니다」), 1/1 마감+지남 띠.
        · ⚠ 주소를 새로 열면 바꿔 끼운 `Date`가 풀린다. **사이트 안 이동**(`next.router.push`)이나 목록 필터를 눌러
          다시 그리게 해야 가짜 날짜로 그려진다.
      ── 이번에 걸린 함정 둘 ──────────────────────────────────────
      · ⚠ **주석의 날짜를 `src` 전체에서 문자열로 바꾸다가 `src/data/services.ts`의 원문 값
        `"applyStart": "2025-09-14"`까지 바꿨다.** `git diff`로 잡아 되돌렸다. **일괄 치환은 `src/data/`를 빼고**
        돌리고, 돌린 뒤 데이터 파일 diff가 0인지 본다(데이터 원문은 3절 — 원문 그대로).
      · 상세 띠에 「본문에 적힌 사업 기간」이라고 썼는데, 사업 기간은 본문이 아니라 **원문 시행 기간 칸**에서 온
        값이다. 신청 기간은 「본문에 적힌」, 사업 기간은 「원문에 적힌」으로 갈랐다.
- [x] **09-13 애드센스 거절 대응 — 「가치가 별로 없는 콘텐츠」 (`b48dd5a`, 배포).**
      애드센스 메일 「사이트에 광고를 게재하기 전에 해결해야 할 문제」 → 사이트 페이지 사유는 **「가치가 별로 없는
      콘텐츠」**(사용자 확인). 사용자 결정: **「보강 + 하위 noindex」** · 점검 끝나면 바로 배포.
      ── 진단 (09-13 실측) ─────────────────────────────────────────
      · 기술 요건은 전부 통과였다 — ads.txt·광고 코드·크롤러 허용·필수 페이지 7개·깨진 링크 0·과장/사칭 문구 0.
      · **원인은 구성이다.** 사이트맵 817 중 715가 상세이고 상세 본문의 49~78%가 복지로 원문 그대로. 우리가 붙인 글은
        대부분 전 페이지 공통 틀이고, 표본 11쪽에서 **그 사업만을 위해 쓴 문장 0개**. 색인 상세 715 중 521쪽은 사업별
        고유 정보가 하나도 없었다. 안내 글 19편 링크 밖 글자 합 약 3.3만 자 대 상세 원문 약 50만 자.
      · 서치콘솔 URL 검사 23건: 홈·안내 글·허브·/about·/standards는 **요청 없이도 전부 색인**, 상세만 절반가량
        「발견됨 – 색인 안 됨」(7·8·10·11·12위 포함, 두 번 요청해도 안 붙음). 색인 보고서 자체는 9/4 갱신분에 멈춰 있었다.
      · **광고 코드가 내용 없는 화면에도 실려 있었다** — layout `<head>`라서 /search·/saved·/ask·/find·404·
        꺼 둔 /login(본문 0자)·noindex 상세 195쪽 전부.
      · 개인정보처리방침 4조가 "광고를 사용하지 않으며 … 도입할 예정"이라고 적고 있었다(코드는 09-05부터 실려 있음).
      ── 고친 것 ─────────────────────────────────────────────────
      · **광고 코드는 색인 페이지에만** — `components/AdSenseScript.tsx`. layout에서 빼고 페이지가 직접 그린다.
        React 19가 `async` 스크립트를 head로 올려 HTML 원문에 전과 같이 나간다(빌드 HTML·라이브로 확인).
        넣는 곳: 홈·안내 글(`GuideNav`)·색인 상세·허브(건수 조건 같게)·소개·검수 기준·FAQ·출처·자가진단.
        안 넣는 곳: noindex 전부·404·계정·약관·방침·문의. **새 페이지를 만들면 이 부품을 넣을지 같이 정한다.**
      · **상세 색인 기준을 좁혔다** — `lib/indexable.ts`. 본문 300자 이상 **그리고** (조회수 **300위** 안 **또는**
        따로 확인한 정보가 붙음). 상세 715 → **272**, 사이트맵 817 → **374**(조회수의 약 92%는 그대로 남음).
        빠진 638쪽도 사이트에서는 그대로 열리고 목록 링크도 그대로다(3절). 옛 `isIndexable`은
        `types/welfare.ts`의 `hasEnoughBody`로 이름을 바꿨다. `audit-sitemap.mjs`도 같은 계산(WLF 번호를 글자로 읽음).
      · **상세에 「복지클릭이 따로 확인한 것」 절** — `lib/serviceExtras.ts`. 법령에서 찾은 지급일(인용·조문 링크),
        공식 공고로 확인한 신청 일정(D-day·캘린더 담기), 그 사업을 직접 다룬 안내 글. 전부 /guide에만 있고 상세에는
        안 이어져 있던 것이다. **29개 사업**에 붙고 목차 첫 칸. 붙을 것이 없으면 절을 안 그린다(빈 틀 = 틀 문구).
        안내 글만 붙는 사업에는 "법령·공고에서 찾았다"고 쓰지 않는다.
      · 방침 4조 첫 문장을 실제에 맞게("AdSense 코드를 싣고 있으며(광고 승인 심사 중)"), 시행일 09-01 → **09-13**.
        처리 방식(제3자 쿠키·광고 설정·aboutads.info)은 개정 전 조항과 같아 9조의 7일 사전 안내 대상으로 보지 않았다 —
        이미 실린 코드를 늦게 밝히는 쪽이 더 문제라서다.
      ── 라이브·색인 ─────────────────────────────────────────────
      · 라이브: 사이트맵 374개 전부 200·전부 광고 코드·noindex 0 / noindex 상세·검색·404·/login 광고 코드 0 · audit-indexability 통과.
      · 구글 사이트맵 재제출 → 즉시 「성공 374」. 새 절이 붙은 상위 상세 **11건 색인 요청**(그중 미색인 4 — 기초연금·
        부모급여·근로·자녀장려금·생계급여). 12번째 긴급복지 생계지원(`WLF00003180`, 미색인)은 「할당량 초과」가 세 번
        나 다음 몫으로 남겼다(같은 날 오전 20건과 합쳐 24시간에 31건째). 빙 사이트맵 재제출(Processing) +
        새 절 29건 URL 제출(할당량 100 → 71).
      ── 남은 일 · 재신청 전에 볼 것 ────────────────────────────────
      · **재신청은 서두르지 않는다.** 사이트가 2주째이고, 색인이 늘고(상세 272쪽 중 몇이 붙나) 검색 유입이 생긴 뒤
        2~4주 뒤가 낫다. 거절 뒤 재검토 요청은 사용자가 애드센스에서 한다(0절·애드센스는 사용자 몫).
      · 가장 큰 남은 지렛대는 **직접 쓴 글**이다. 조회수 1위 청년내일저축계좌처럼 상위 사업 다수가 아직 따로 확인한
        정보 0 — 사업별 안내 글(법령·보도자료 인용, 확인 못 한 것은 안 씀)을 늘리면 절이 저절로 붙는다(`serviceExtras` 표에 한 줄).
      · 허브 카드가 원문 요약을 그대로 반복한다(1위 요약문이 26곳). 「신청 전 체크」 자격 조건 줄이 지원 대상 첫 문장을
        한 번 더 싣는다. 둘 다 이번엔 안 고쳤다.
      · ⚠ SPA 이동이라, 광고 코드가 있는 페이지에서 링크로 noindex 화면에 들어가면 이미 불러온 스크립트가 남는다.
        심사 크롤러는 주소를 직접 열어서 영향이 없다고 봤다.
      ── 같은 날 재점검 (`587c485`, 배포) ────────────────────────────
      · 사이트맵 374 + 링크로 닿는 643 = **1,017쪽 전부 200**, 사이트맵 밖 페이지에 광고 코드 0, 모든 페이지 푸터에
        소개·문의·방침 링크, h1 하나씩, 제목 중복 0, robots·ads.txt·www 308 이상 없음.
      · **길잡이 화면에서 광고 코드를 더 뺐다** — 축 목록 5곳(본문 206~517자)과 항목 `AD_MIN_ITEMS`(10)건 미만 허브
        9곳(소득 기준 8·`/theme/energy`). 색인은 그대로. 애드센스 「탐색용 화면에 광고」 정책 대비.
      · 원문 비중(본문 줄 중 수록 원문과 겹치는 글자): 상세 중앙값 46%·최대 76%, 주제 허브 46%, 지역 39%, 안내 글 0%.
      · PageSpeed는 오늘도 API 429·웹 화면 멈춤으로 **미측정**.

      ── 이번에 걸린 함정 ────────────────────────────────────────
      · ⚠ **GSC URL 검사 루프를 두 번 띄워 결과가 섞였다.** `navigate`로 같은 GSC 주소를 열어도 SPA라 앞 루프가 안 죽는다.
        `location.reload()`로 끊고, 루프에 세대 번호(`window.__gen`)를 넣어 옛 루프가 스스로 멈추게 했다.
        판정은 「주소(`?id=`)가 바뀐 뒤 보이는 패널의 문구」로 읽는다 — 본문 전체에서 찾으면 앞 패널 문구를 읽는다.
      · 다른 세션(uk-9e)이 역할 분담 메시지를 보냈다가 **데이원코리아 작업을 잘못 읽은 것이라며 취소**했다. 0절의 「사이트는 셋」이
        세션 사이에서도 걸린다 — 받은 메시지가 어느 사이트 얘기인지 먼저 본다.
- [x] **09-13 사업별 해설 글 셋 — 애드센스 재신청 준비 (`851027b`, 배포).** 사용자 요청 "직접 쓴 글 만들어 줘".
      조회수 상위인데 상세에 따로 확인한 것이 0이던 사업에서 골랐다. **셋 다 복지로 원문이 아니라 원문에 이름·첨부로만
      걸린 1차 출처를 읽고** 쪽수·조항을 달았다. 안내 글 19 → 22편, 사이트맵 374 → 377.
      · `/guide/youth-tomorrow-savings` (1위 청년내일저축계좌, 조회수 13.4%) — 원문 첨부 PDF 「2026년 자활사업안내(Ⅱ)
        자산형성지원 통장사업 안내」(보건복지부, 196쪽). 정부지원금을 잃는 일곱 가지(74쪽 표), 일반 적립중지 6 → **12개월**
        (2026 개정)·특별 적립중지 2년(5년 연장, 매칭은 3년), 소득이 중위 100% 넘으면 **중도 지급**, 30세 미만 따로 사는
        청년은 1인 가구, 70일 내 결과 통보. 3년 합계는 10만원 줄만 안내서 값(1,440만원+이자)이고 나머지는 「저희가 계산」.
        · ⚠ **원문과 안내서가 어긋난다** — 복지로 원문엔 아직 「중위 50~100%: 10만원 매칭」이 있는데 안내서는
          「’26년부터 차상위 초과자 신규모집 중단」. 둘 다 옮기고 올해 신규 적용은 129에 확인하라고만 썼다.
      · `/guide/phone-bill-discount` (7위 이동통신요금감면) — 과기정통부 고시 「보편적역무손실보전금 산정방법 등에 관한 기준」
        (제2025-32호) 제5조 + 전기통신사업법 시행령 제2조제3항(누가). **법제처 API `target=admrul`로 고시 전문을 받았다**
        (`lawSearch.do?OC=test&target=admrul&query=…` → `lawService.do?…&ID=<행정규칙일련번호>`). 계산기
        `PhoneDiscountCalc` + `lib/phoneDiscount.ts`.
        · 교차 확인: 고시 한도대로 셈하면 복지로 최대액 셋과 **정확히 일치**(33,500·21,500·11,000).
        · **생계·의료급여의 26,000원 초과 월정액**은 고시가 50% 적용 여부를 안 적어 한 수가 아니라 범위(26,000~)로 낸다.
        · 기초연금·차상위·주거·교육급여는 **휴대폰만**(시행령 단서), 1인 1회선, 차상위 등 가구당 4명(6세 이하 제외).
      · `/guide/childcare-choice` (6·10·18위 보육료·유아학비·양육수당) — 원문 첨부 PDF 교육부 「2026년도 보육사업안내」
        93쪽 안내문의 **15일 규칙 네 방향**(15일 이내/16일 이후 × 집↔어린이집, 결과는 자부담 또는 그달 현금 없음),
        국민행복카드 결제(89쪽), 0~1세 퇴소 7일 전 신청(77쪽). 유치원 전환은 소급 불가(유아학비 원문).
        · PDF 쪽수는 인쇄 쪽(머리글 숫자)으로 적는다 — PDF 파일 쪽과 8쪽 어긋난다(파일 101쪽 = 인쇄 93쪽).
      · 세 글 모두 `serviceExtras.ts`에 이어 상세 「복지클릭이 따로 확인한 것」에 뜬다(7개 사업).
      · 색인: 빙 9건 제출(할당량 71 → 62). 구글 1건 요청(청년내일저축계좌 글). 휴대폰·보육 글은 「할당량 초과」 두 번씩 — 다음 날 몫(24시간에 32건째).
      ── 이번에 걸린 함정 ────────────────────────────────────────
      · **`pdftotext`는 이 정부 PDF들의 한글을 못 뽑는다**(글꼴에 ToUnicode가 없어 빈 칸만 나옴). 파이썬 `pypdf`는 된다 —
        출력은 `io.open(..., encoding='utf-8')`로 파일에 쓴다(콘솔은 cp949라 print가 깨진다).
      · PageSpeed API(키 없음)는 이날도 429, 웹 화면도 결과가 안 나왔다. 미측정.
- [x] **09-13 애드센스 거절 사유별 점검 · 보강 (`b3e14f5`·`fe79fc4`, 배포).** 사용자 요청 "탈락했다고 가정하고
      여러 이유를 바탕으로 점검, 부족한 부분 채워 줘".
      ── 사유별 결과 ───────────────────────────────────────────────
      | 거절 사유 유형 | 판정 | 근거 |
      |---|---|---|
      | 사이트 다운·사용 불가 | 통과 | 1,017쪽 전부 200 · ads.txt · robots(구글 봇 안 막음) · www/http 308 |
      | 탐색 문제 | 통과 | 모든 페이지 푸터에 소개·문의·방침 · h1 하나씩 · 깨진 링크 0 |
      | 게시자 콘텐츠 없는 화면의 광고 | 보강 | 짧은 상세 70쪽 광고 코드 제외(아래) · 광고 페이지 360 → 293 |
      | 오해·과장·사칭 문구 | 통과 | 빌드 1,020쪽을 「무조건·100%·지금 바로·정부 공식·선착순…」으로 훑음 — 걸린 것은 원문 인용·부정문뿐 |
      | 신뢰(운영자·정책·연락처) | 보강 | 소개 페이지 절 둘 추가, 안내 글 끝 작성 방법, 방침 요약에 광고 쿠키 |
      | 가치 낮은·복제 콘텐츠 | 보강 중 | 카드 요약 80자 · 새 안내 글(23편) — 가장 큰 남은 과제 |
      | 페이지 경험 | 통과(참고치) | 라이브 모바일 첫 화면 CLS 0 · LCP 약 1.1초(캐시 상태). PSI는 429로 미측정 |
      ── 고친 것 ─────────────────────────────────────────────────
      · **본문이 짧은 상세는 광고 코드를 뺀다** — `lib/indexable.ts`의 `showAds`: 색인 대상이면서 원문 본문
        `AD_MIN_BODY`(500)자 이상이거나 따로 확인한 정보가 있을 때만. 874~996자짜리 상세가 소개 페이지보다 짧은데
        광고 코드가 붙어 있었다. 색인은 그대로.
      · **목록 카드 요약 140 → 80자**(`hubRows.ts` SUMMARY_MAX + `display.ts` `clipSummary`, ServiceCard도 같은 함수).
        두 줄만 보이는데 HTML에 140자가 실려 허브 본문의 30~46%가 원문 요약이었다. /life/youth HTML 274KB.
      · **옛 숫자 「전체 5,219건」이 소개·FAQ 2곳·지역 글에 박혀 있었다**(09-13 재점검 실제 5,279) →
        `lib/sourceTotals.ts` 한 곳에서 읽는다. 다음 목록 점검 때 이 값을 같이 올린다. 소개의 「상위 500건이 86.5%」도
        렌더 계산으로(수록 910건 중 상위 100건 85.9%).
      · 소개 「내가 대상인지 바로 계산합니다」 → 「내 소득이 기준선의 어디쯤인지 계산합니다… 판정하지는 않습니다」(3절).
        「원문에 없는 것은 따로 확인합니다」(글 수·지급일·신청 일정 수는 계산값)·「광고와 운영비」(AdSense 심사 중,
        돈 받고 순서 안 바꿈, 제휴 링크 없음)·데이터 받은 날/재대조 날 추가.
      · `GuideNav`에 「이 글은 이렇게 썼습니다」 — 23편 전부 끝에 작성 주체·원칙·검수 기준·오류 신고 링크.
      · JSX 줄바꿈이 「검색 결과· 관심」처럼 **없던 띄어쓰기**를 만들었다(소개·방침). 가운뎃점·괄호 뒤에서 줄을 끊지 않는다.
      · **새 글 `/guide/mental-health-voucher`** (12위 정신건강 심리상담 바우처) — 계산기 `MentalVoucherCalc`
        + `lib/mentalVoucher.ts`. 전부 2026 원문 값(단가 1급 8만·2급 7만 × 본인부담률 0·10·30·50%, 8회, 120일 연장 불가,
        연 1회). 신청 자격 여섯 갈래와 증빙 유효기간(3개월·1년·5년).
        · ⚠ 원문 문장 「…법정한부모가족은 본인부담률, 재난피해자는 본인부담률 0%」가 **앞 무리 숫자 없이 끊겨 있다** —
          0%로 읽히지만 계산하지 않고 원문을 보여 준다. 첨부 안내서는 **2025년판(「전국민 마음투자 지원사업」)**이라
          숫자에 안 썼다.
        · 정신건강 주제라 맨 위에 위기 상담 전화(109·1577-0199, 129.go.kr/109·보건복지부 보도자료로 확인).
      · 색인: 구글 새 글 2건(심리상담·휴대폰) 요청, 보육 글에서 「할당량 초과」. 빙 5건(할당량 62 → 57).
      ── 남은 과제 ───────────────────────────────────────────────
      · 직접 쓴 글 더 — 후보 산모·신생아 건강관리(9위, 첨부가 2025년판이라 2026 사업안내를 먼저 찾아야 한다)·
        고교학비·교육급여(15·17위). 보육 글(`/guide/childcare-choice`) 구글 요청은 다음 날 몫.
      · 재검토 요청은 2~4주 뒤(사용자 몫). 운영자 실명·상호 표기는 사용자 결정 사항(지금 「복지클릭 운영자 (개인)」).
- [x] **09-15 남은 작업·버그 점검·색인 (`67075a0`·`a62ef6b`, 배포).** 사용자 요청 "나머지 작업 진행 · 애드센스 기준 재확인 ·
      사이트 점검·버그 · 서치콘솔·빙 10~30개씩".
      ── 새 글 둘 (안내 글 23 → 25편, 사이트맵 378 → 380) ─────────────────
      · `/guide/school-support` (15·17·27위 초중고 교육비·교육급여·교육정보화) — 원문 첨부 교육부 2026 지침 두 권
        (「초·중·고 학생 교육비 지원 안내」 124쪽, 「교육급여 운영 방안 안내」 158쪽). **복지로 원문에 없는 핵심: 교육급여
        교육활동지원비(초 502,000·중 699,000·고 860,000)는 바우처라 결정 뒤 한국장학재단에서 따로 신청**(’26.4.1.~’27.2.28.,
        e-voucher.kosaf.go.kr·농협앱·페이코앱, 전년 지급완료자는 자동신청). 교육비 지원은 신청일이 속한 달부터·법정 자격자도
        미신청 시 미지원·고1 입학금은 1분기 신청만. 시·도교육청별 기준표(23쪽)는 **PDF에서 열이 뭉개져 옮기지 않았다.**
      · `/guide/postpartum-care` (9위 산모·신생아 건강관리) — 복지로 첨부는 2025년판이라 **보건복지부 누리집 2026년 사업안내**
        (430쪽, 훈령/예규/고시/지침 게시판 list_no=1488490, `boardDownload.es?…&seq=2`로 PDF)를 받았다. 계산기 `MomCareCalc` +
        `lib/momCare.ts` — 서비스 가격·정부지원금 표(51쪽)를 천원 단위 그대로. **같은 표가 「주요 변경사항」(ii쪽)에도 있어 전 유형
        대조 일치**, 1일 기준가격 146,400원 × 10일 = 1,464천원으로 한 번 더 검산. 자율 가격(+5%)은 "최대 몇 원 더"로만.
        신청 기한 출산예정일 40일 전~출산 후 60일, 바우처 유효 출산일부터 90일, 건보료 150% 판정표(44쪽).
      · PDF 쪽수는 인쇄 쪽 — 교육비 안내는 파일 쪽 −6, 교육급여 안내 −6, 산모 안내 −14.
      ── 코드 점검에서 나온 버그 (에이전트 리뷰 → 확인 → 수정) ───────────────
      · **휴대폰 요금감면 계산기가 감면액을 부풀렸다** — 요금 한 칸을 전부 월정액으로 봤는데, 고시는 월정액만 한도까지 면제하고
        통화료는 50%·35%만 깎는다(생계·의료 월정액 15,000 + 통화료 8,000 → 23,000 표시, 고시대로 19,000). **틀리는 방향이 늘
        "더 받는다"** 쪽이라 3절 위반. 입력을 「월정액」·「그 밖의 통화료」 두 칸으로 나눴다(`calcPhoneDiscount(kind, plan, calls)`).
      · **지난 신청 일정에 「캘린더에 담기」가 남아 있었다** — 상세 「따로 확인한 것」과 `/guide/calendar`. 첫 화면 카드만 「기간 지남」을
        달고 있었다. `CalendarEntryActions`(브라우저가 오늘로 판정)로 셋을 맞췄다.
      · JSX에서 **가운뎃점 뒤 줄바꿈이 없던 띄어쓰기**를 만든 곳 6곳(소개·자가진단·생계 글·방침·검색·새 글) — 「집·차· 예금」.
        `grep -rn "·$" src/app src/components`로 찾는다.
      · 카드 요약 80자 자르기가 글머리 기호(-•○❏)에서 끝나던 5건 → 끝의 기호를 걷는다. `/income` 목록 광고 코드 제외(다른 축 목록과 같게).
        소개·방침 「검색 결과·관심 지원 목록에는 광고 코드를 싣지 않습니다」에 **SPA 이동 뒤엔 남을 수 있다**를 덧붙였다.
        `audit-sitemap.mjs`가 파일 전체의 WLF 번호를 긁던 것을 `id: "WLF…"` 줄과 GUIDE_SERVICES 표로 좁혔다.
      ── 애드센스 기준 재확인 · 라이브 ─────────────────────────────────
      · 라이브 사이트맵 380쪽 전부 200 · 사이트맵에 noindex 0 · 검색·저장·질문·조건 찾기·404·/login 광고 코드 0 · 과장·사칭 문구 0
        (빌드 1,023쪽 재검사) · 광고 코드 페이지 296 → /income 빼서 295. 서치콘솔 색인 보고서는 여전히 9/4 갱신분(270/576).
      · 색인: 빙 22건 제출(할당량 100 → 78) + 사이트맵 재제출(9/15 Processing). 구글 22건 전부 요청(할당량·오류 0) — 요청 전 검사에서 미색인 13(새 글 넷·교육급여·고교학비·교육정보화·유아학비·이동통신·심리상담·부모급여·긴급복지 생계·근로장려금) · 이미 색인 9(내용 변경으로 재요청). 한 건에 약 2분. 사이트맵 재제출(9/15, 마지막으로 읽은 날 9/14·378).
- [x] **09-15 2차 — 한부모가족 아동양육비 글 (`466c055`, 배포).** 사용자 요청 "직접 쓴 글 최소한 진행하고 배포·마무리".
      · `/guide/single-parent-support` (22위 한부모가족 아동양육비 · 청소년한부모 WLF00001109) — 원문 첨부 성평등가족부
        「2026년 한부모가족지원사업 안내」(566쪽, 인쇄 쪽 = 파일 쪽 −8). 원문에 없는 것: 급여 개시일은 **신청일**(그달 전액),
        생계급여·긴급복지 생계지원과 아동양육비 중복 가능(가정위탁 양육보조금은 불가), 학용품비 7월 일괄, 나이는 생일이 든 달의
        전 달까지, 이사 시 전입일 15일 기준, 34세 이하 근로소득 60만원+30% 공제, 2026 소득인정액 표(3쪽). 예시 합계 표는 「저희가 더한 값」.
      · ⚠ **원문끼리 어긋난다** — WLF00001109 원문은 「0~1세 37만원 · 2세 이상 40만원」, WLF00001068 원문과 지침(156·167쪽)은
        「0~1세 40만원 · 2세 이상 37만원」. 지침을 따르고 화면에 어긋난 사실을 적었다.
      · 안내 글 25 → 26편, 사이트맵 380 → 381. tsc·eslint·빌드·감사 둘 통과. 색인(같은 날 후속): **구글 2건 요청됨** — 새 글(미색인) · WLF00001068(미색인, 첫 시도 「할당량 초과」 → 재시도로 들어감).
        WLF00001109는 「할당량 초과」 두 번이라 미뤘다가 **같은 날 다시 넣어 한 번에 「색인 생성 요청됨」**(이때 검사 판정은 「등록되어 있지 않음」 — 앞서 「이미 색인」으로 읽은 것은 틀렸을 수 있다). **빙 3건 제출(할당량 78 → 75) + 사이트맵
        재제출(Processing, 380 → 381 대기).**
      · ⚠ **서치콘솔 검사 화면 주소의 `id=`는 URL이 아니라 암호 같은 번호다**(`id=RV4Z7dKoW3…`). 「주소에 넣은 URL이 들어갔는지」로
        판정 대기를 걸면 영원히 안 끝난다 — **주소가 바뀌었는지**로 기다린다. `inspect?id=<URL>`로 바로 여는 주소도 404다.
- [x] **09-15 3차 — 마지막 점검(애드센스 거절 가정 + 기능 버그) · 버그 셋 고침 (`05d9fe4`, 배포).**
      · 통과: tsc·eslint·빌드·감사 셋 · 라이브 사이트맵 381쪽 전부 200 · noindex 0 · canonical·h1·푸터(소개·문의·방침) 전부 정상 ·
        제목·설명 중복 0 · 내부 링크 1,303개(사이트맵 밖 920) 깨짐 0 · 사이트맵 밖 페이지 광고 코드 0 · 없는 주소 404 · www/http 308 ·
        ads.txt · 375px 가로 넘침 0(새 글 넷·홈·상세) · 콘솔 오류 0 · ☆ 저장·/saved·첫 화면 관심 지원 정상 · 휴대폰 요금감면 계산 14가지 입력 전부 고시대로.
      · 광고 코드 없는 사이트맵 페이지 84쪽은 **의도대로**다(축 목록 5·소득 허브·본문 500자 미만 상세 — `showAds`·`AD_MIN_ITEMS`).
        본문의 `&lt;`·`&gt;`는 한 겹 이스케이프라 화면에는 `<`·`>`로 나온다(`&amp;lt;` 0건 확인) — 결함 아님.
      · **고친 것 — 자가진단이 기준선을 조금 넘는 소득에도 「✓ 충족」**(`midIncome.ts` `percentOfMedian`). 비율을 반올림해 1인 월 1,283,000원
        (50.034%)이 50.0 → 50% 충족, 같은 줄엔 「1,282,119원 이하」. 판정하는 곳 넷(IncomeCheck·MyEligibility·IncomeMatch·저장값)이 전부
        이 함수를 거치므로 **한 곳에서** 「소득이 thresholdOf 이하가 되는 가장 작은 0.1%」로 바꿨다. `verify-mid-income.mjs`에 1~8인 × 1~250% ×
        기준선 ±2원 대조를 더했다(옛 코드에선 실패, 새 코드 통과). 라이브: 1,282,119원 ✓ / 1,282,120원 50.1%·✓ 없음.
        09-10의 `Math.floor` 저장 버그와 같은 계열 — **비율을 반올림한 값으로 기준선을 판정하지 않는다.**
      · K-패스 계산기: 못 읽은 입력(abc·1,000만원 초과)이 빈칸처럼 「계산 안 함」으로 묻히던 것 → 「만원 단위 숫자로 넣어 주세요」.
      · 이번 달 카드 정렬: 날짜 없는 항목 둘을 비교하면 양쪽 다 1을 돌려줘 순서가 흔들렸다 → 0.
      · 구글 WLF00001109 요청됨(같은 날 재시도, 검사 판정은 「등록되어 있지 않음」).
      · ⚠ 사이트가 `frame-ancestors 'none'`이라 **같은 출처 iframe으로 여러 쪽을 한꺼번에 재는 방법은 안 된다** — 한 쪽씩 연다.
- [x] **09-15 4차 — 복지 정보 변경·신규·마감 점검 · 마감 규칙이 놓친 2건 고침 (배포).**
      · 목록 5,279건(중앙 461 · 지자체 4,818)을 새로 받아 **09-13 목록과 직접 대조** — 새 사업 0 · 없어진 사업 0 · 항목 변경 0.
        `check-updates.mjs`는 parsed.json(08-31 기준)과 대조하므로 09-13에 안 넣은 세종 43건 등 53건이 "새로 생김"으로 또 나온다 —
        **직전 점검 폴더의 `central.json`·`local.json`과도 대조해야** 진짜 새 것만 보인다.
      · 상위 100건(중앙 81 · 지자체 19) 상세 재수집 → **100건 내용 같음**, 옛 원본으로 되돌림. `rechecks.json`에 09-15(checked만) → 상세 문장이 「09-15에 다시 받아 대조, 내용 그대로」. lastmod는 안 움직임.
      · **고친 것 — 조회수 2위 「청년월세 지원사업」에 마감 표시가 없었다.** 원문 「'26년 신규 신청기간: 3.30(월) 09:00 ~ 5.29(금) 16:00까지」가
        시작에 연도가 없고 시각이 붙어 `statedApplyPeriod`가 못 읽었다(같은 일정의 신청 달력 항목은 「기간 지남」이었다). 시작 연도가 없으면 앞말 40자 안의
        「'26년·2026년」을 쓰고, 시각(`09:00`)을 건너뛰게 했다. 앞말에 「시기」도 더했다(밀양 공공근로사업 「신청시기」).
      · ⚠ **줄지은 기간은 앞 기간에 이어 읽는다** — 「상반기: 2025.12.29.~2026.1.7. - 하반기: 2026.6.17.~6.24.」에서 하반기는 앞말과 40자 넘게 떨어져
        처음엔 **상반기만** 잡혔다(끝날 1/7). 하반기가 아직 남은 사업이었다면 "지났다"고 잘못 말했을 자리다. 바로 앞 기간 끝에서 20자 안이면 같이 본다.
      · 규칙 변경 전/후를 910건 전부 대조: 새로 잡힌 것 **딱 2건**(2위 청년월세 · 449위 밀양 공공근로사업), 기존 11건 그대로. 마감 표 24 → 26건.
        「이용기간」(환경보건이용권 2차 ~10.30)은 여전히 안 잡힌다 — 맞다.
      · 오늘이 끝날인 것: 신청 달력 근로·자녀장려금 반기신청(9/15) — 내일 배포 없이 「기간 지남」. 다음으로 D-10 안에 들어올 것은 청년희망적금(9/30, 9/20부터)·에너지바우처 여름 몫 사용(9/30).
      · 청년월세(2위) 상세는 내용이 바뀌었는데 lastmod가 안 움직였다 — 다음 색인 요청 때 넣을 후보.
- [x] **09-15 5차 — 정확성 전체 점검 · 신청 안내 · 질문 응답 · 함께 받기 · 버그 (배포).** 사용자 요청 다섯 갈래.
      ── 정보가 틀린 곳 ─────────────────────────────────────────────
      · **소득 기준선이 "받는다" 쪽으로 나가던 33건** — `build-data.mjs`가 원문에 중위소득 %가 여럿이면 **가장 큰 값**을 썼다.
        조회수 2위 청년월세는 "청년가구 60% 이하 **그리고** 원가구 100% 이하"인데 「100% 이하」로 나가 80%인 1인 청년에게 ✓가 붙었고,
        한부모 아동양육비는 65%(일반)·72%(청소년한부모)가 72%로 나갔다. **값이 하나일 때만** 쓰고(`pickMedianPercent`), 도시근로자
        월평균소득을 함께 쓰는 사업(행복주택 45%)도 뺐다. 223 → 190건. `/income/72·85·140·200`이 3건 아래로 줄어 사라져
        **`/income`으로 308**(`permanentRedirect`), 사이트맵 381 → 377.
      · **상세 「처리 절차(원문)」 순서가 뒤섞여 있었다 — 119건(상위 100건 중 43건).** API `applmetList` 배열 순서가 흔들린다. 청년월세
        1번이 「보장 결정」, 기초연금 1번이 「조사 및 심사」였다. 원문 단계 이름(신청→조사→결정→제공→사후관리→이의)으로 줄 세운다
        (`orderedSteps`, 중앙 330건 끝말 여섯 가지뿐 전수 확인). 위 안내문도 "신청 창구는 원문에 없다" → ‘서비스 신청’ 줄이 있으면 그걸 가리킨다.
      · **FAQ 「여러 지원을 동시에 받을 수 있나요」** 옛 답 "대체로 가능 · 중앙과 지자체는 함께 되는 경우가 많다"는 근거가 없고 원문이
        반례를 적고 있었다(지자체 난방연료비가 에너지바우처 수급자 제외). 수록 원문에 적힌 예 넷(막는 예 3 · 된다고 적은 예 1)만 들고
        "원문에 없다고 함께 받을 수 있다는 뜻이 아니다 · 129"로. 원문에 중복 문장이 있는 사업은 910건 중 47건(상위 100건 중 4건)뿐 —
        **함께 받기를 데이터로 답할 수는 없다.** 자주 묻는 조합(부모급여+아동수당, 기초연금+장애인연금 등)은 1차 출처로 글을 따로 써야 한다(남은 일).
      · **안내 글·FAQ 대조(법령 API·금융위·원문)에서 나온 것** — 청년미래적금 우대형에 「중소기업 재직자·연매출 1억 이하 소상공인」
        요건이 빠져 계산기에서 누구나 12%를 고를 수 있었다(더 받는다). 「자녀장려금은 5월에만」 → 틀림: 조특법 제100조의31①이
        반기신청(제100조의6⑦)만 준용에서 빼고 기한 후 신청(⑧)은 둔다. 근로장려금 계산기에 **최소 지급 규정**(제100조의7③: 1만5천원
        미만 0 · 증가 구간 10만원 · 감소 구간 3만원)이 없어 저소득 구간을 적게 알려 줬다 — 원문에 없는 규정이라 글의 "밖에서 가져온 숫자
        0"도 고쳤다. FAQ 일반화("가장 흔합니다"·"대부분 건강보험료로" — 기초연금은 선정기준액), 이의신청(시·군·구 거쳐 시·도지사, 제38조),
        30세 미만 가구 규칙은 기초생활보장 한정, 갱신 문구. 실업급여 "어떤 제도보다 많이 찾는다"(K패스·문화누리카드가 더 큼),
        baby-money "원문에 창구 없음"(있음), 청년내일저축 나이 하한 원문대로, 교육정보화 1,650원에 시·도별 조건, 휴대폰 감면 제목에 「월 최대」.
      ── 질문 응답(/ask) — 라이브 60문항: 좋음 37 · 아쉬움 15 · 틀림 5 · 0건 3 → 고침 ─────
      · **이름을 댄 사업이 조건에 먹혀 사라졌다** — 「부모급여 아동수당 같이」의 「아동」이 조건이 돼 부모급여(영유아)가 빠짐. 네 글자 이상
        통째 낱말(`names`)이 이름의 60% 이상이면 생애주기·대상으로 거르지 않고 위로 올린다(지역은 건다).
      · **생애주기·대상 칸이 빈 사업이 조건에서 빠졌다**(lifeStages 빈 것 241 · targets 빈 것 459) — 22위 한부모 아동양육비가 「한부모
        아동양육비」에서 사라짐. 빈 칸은 거르지 않되 **낱말까지 걸린 것만** 보인다(「조건에 맞는 것」으로 내보내면 우리가 맞다고 말하는 셈).
      · 빼는 말 추가(받아·같이·중복·방법·기간·언제·어디서·가정…), 어미 「하면·으면·려면·는지」, 한 글자 「암」, 붙여 쓴 문장 쪼개기,
        조건어는 **앞·끝에서만** 뗌(「지역아동센터」가 「지역센터」가 됐다), 뗀 나머지가 조건어면 버림, 「4인」 숫자, 부서명 대조 제거
        (「치매 부모님 돌봄」 1위가 부서 「…돌봄국」의 청년월세), 「60대」 버렸다고 밝힘, "어디에도 없다" → 보는 칸을 적음.
      · **신청 방법·함께 받기·지급일·온라인·실업급여를 물으면 해당 글로 잇는다**(`ASK_GUIDES`) — 목록이 답이 아닌 질문이다.
      ── 버그 ─────────────────────────────────────────────────────
      · **자가진단이 입력을 지우는 도중의 값을 저장했다** — 350 → 3(1.2%)이 남아 상세마다 「충족」. 칸을 떠날 때(onBlur)만 저장.
      · 근로장려금 합계 1원 어긋남(따로 반올림한 두 줄의 합으로) · 끝 연도 없는 기간이 해를 넘기면 끝날이 1년 앞당겨짐(`applyPeriod` rolled).
      · 상위 100건 누리집 링크 177개 중 이 PC에서 안 열린 원문 링크: 국민연금공단 옛 jsp(500)·team.mw.go.kr/blss·health.mw.go.kr·
        oneclick.moe.go.kr·ableservice :8443 — **원본(복지로) 값이라 고치지 않았다**(3절). molit.go.kr은 봇 차단(307 반복)이라 결함 아님.
      ── 공식 출처 대조 추가분(정책브리핑·법령 API로 직접 확인) ─────────────
      · **K-패스 「반값 모두의 카드」(2026년 4월부터 6개월)** — 정액형 기준금액 50% 인하·시차시간 기본형 +30%p를 계산기·글에
        밝혔다(정책브리핑 2026-04-17). 공단 표는 평상시 값이라 계산은 그대로 두고 "이 기간엔 더 받을 수 있다"를 붙였다.
      · **청년미래적금 유형별 기준 전체**(정책브리핑 2026-06-19) — 중소기업 **신규취업자**는 총급여 6,000만원·가구 200% 이하면
        우대형 12%, 6,000만 초과~7,500만원은 기여금 없이 비과세만. 「두 문서가 다릅니다」로 보류했던 구간에 공식 답이 생겼다.
      · 근로장려금 계산기에 "5월 정기신청 기준 — 기한 후 신청 95%, 반기 상반기분 35%" 단서.
      · 신청 달력 마감일: 반기신청 **3월 16일**(정책브리핑 03-12)·정기신청 **6월 1일**(04-30) — 말일이 일요일이라 밀렸다.
      · 지급일 인용문 넷에 뺀 자리 「(…)」 표시(법령 API 전문 대조). pay-dates 「N개 조문」 → 「N개 사업의 조문」, 보훈 인용문 원문대로.
      · 기준 중위소득 2026 전 구간·건보료율·조특법 산식·휴대폰 감면 고시·심리상담·산모 가격표는 **일치** 확인.
        ⚠ 2027년 기준 중위소득 고시가 이미 나왔다(2026-07-29) — **1월에 `midIncome.ts`를 바꾼다.**
      ── 애드센스 심사관 관점 재점검 → 고친 것 ─────────────────────────
      · **상세 광고 코드를 「따로 확인한 것」이 있는 사업에만**(`showAds`, 205쪽 → 39쪽). 약 165쪽이 원문+공통 틀뿐이었다 —
        거절 사유 그대로. 승인 뒤 넓히려면 주석의 옛 조건으로 되돌린다. 719위 달서구 사업을 baby-money 연결에서 뺐다(인용 한 줄뿐).
      · 같은 원문이 두 절에 찍히던 상세(선정 기준 = 지원 대상 또는 지원 내용) — 한 절만 싣고 그 사실을 적는다(`criteriaDupOf`).
      · 날짜 불일치: 검수 기준·출처·첫 화면이 "처음 받은 날"만 적어 상세의 「09-15 확인」과 어긋났다 → `LAST_CHECKED`(항목별
        checkedAt 최댓값)를 같이 적는다. `SOURCE_TOTALS.checkedAt` 09-15. 출처 페이지의 "세종은 자체 사업 0건"(지금은 있음) 고침.
      · 판정처럼 읽히는 첫인상 문구: "내가 받을 수 있는 복지 지원금" → "내 상황에 맞는 복지 지원금 찾기", "내가 대상자인지 1분이면
        압니다" → "내 소득이 기준선 어디쯤인지 1분 계산", /check·소개·공유 이미지도.
      · 「따로 확인한 것」 머리말이 "원문에 없는 내용"이라 해 놓고 출처가 복지로인 일정이 있었다 → 문구를 넓혔다.
      · 첫 화면에 "복지로에 없는 것은 따로 찾습니다 — 안내 글 N편 · 지급일 N건" 한 줄(숫자는 표에서 센다).
      · 사이트맵 381 → 376(소득 허브 4 · 달서구 1). 안내 글 11편 updated 09-15.
      ── 남은 일(안 한 것) ──────────────────────────────────────────
      · 허브(카드만 늘어놓은 /life/youth 등)에 우리 문장 2~3문단 또는 광고 한정 · 상세 한 페이지 안의 되풀이(요약·자격 줄) 구조 손보기.
      · /theme/legal 소개 문장이 실제 목록(첫 카드 청소년특별지원)과 안 맞음 · 원문 주기 값에 붙인 틀 설명("매월 지급 — 매달 나옵니다")이
        본문(동절기 5개월)과 어긋나는 상세 약 11건.
      · 함께 받기 조합 글(부모급여+아동수당·기초연금+장애인연금·국민연금 연계감액 등) — 1차 출처로.
      · 「마감」 딱지는 브라우저에서만 그려진다(서버 HTML엔 없음) — 참고.
- [x] **09-16 함께 받기 글 · 허브 머리말 (`c167c4a`, 배포).** 라이브 확인 — 사이트맵 **377개 전부 200** · 새 글 200·광고 코드·canonical 정상 · 허브 머리말(생애주기·대상) · FAQ·/ask·기초연금 상세에서 새 글로 이어짐. 사용자 요청 — 「허브에 설명 문단」과
      「자주 묻는 조합 글」을 구글·네이버 검색으로 진행.
      ── 새 글 `/guide/combined-support` (안내 글 26 → 27편, 사이트맵 376 → 377) ────
      · **데이터로는 답이 안 나오는 질문이라 밖에서 찾았다.** 원문에 「중복」이라는 말이 한 번이라도 나오는 사업은
        910건 중 **63건**(화면 숫자는 렌더 시점 집계). 그래서 법령 전문(DRF API)과 부처 지침 PDF를 직접 받아 썼다.
      · 확인한 것 — ㉠ **부모급여는 아동수당법 제4조제5항이 "추가로 지급"하는 아동수당**이라 둘 다 나온다(별개 제도가
        아니다). 나이는 2026.3.20 개정 **부칙 제2조**의 연도별 특례로 2026년 9세·2027년 10세·2028년 11세·2029년 12세.
        ㉡ **장애인연금 기초급여는 기초연금과 함께 못 받는다**(장애인연금법 제6조제5항) — 65세가 되는 달부터 기초연금으로
        바뀌고 **따로 신청**해야 한다. 부가급여(제7조)는 이어진다. ㉢ 기초연금은 국민연금 급여액등이 기준연금액의 150%
        (2026년 월 524,550원)를 넘으면 산식으로 줄 수 있고, **장애인연금·기초생활 수급권자는 기준연금액 그대로**
        (기초연금법 제5조제7항·제6조, 기초연금 누리집 「기초연금액 산정」으로 교차 확인). ㉣ 부모급여+보육료는 0세만
        차액 41만 6천원(복지로 원문), ㉤ 청년월세는 주거급여 월차임분을 뺀 금액만(원문).
      · **생계급여 옆의 다른 돈 — 지침이 갈래를 나눠 놨다.** 보건복지부 「2026년 국민기초생활보장 사업안내」(545쪽)
        인쇄 102·103·124~125쪽: 기초연금·국민연금·실업급여는 **소득으로 치고 공제도 없다**(그만큼 줄어듦),
        장애인연금·장애수당·한부모 아동양육비는 **넣었다가 다시 뺀다**(안 줄어듦), 아동수당·부모급여·보육료·양육수당·
        근로자녀장려금은 **아예 소득으로 안 본다**. 표 한 장(인쇄 124쪽)에 ○/× 로 정리돼 있다 — 다음에 이 질문이 오면
        그 표부터 본다. ⚠ 인쇄 쪽 = PDF 파일 쪽 − 26.
      · 지침 PDF는 **네이버 웹문서 검색으로 찾았다**(easylaw 미러). 링크는 보건복지부 게시글(bid=0009, list_no=1488483).
      · 2023년 정책브리핑에도 같은 취지의 문장이 있었지만 **2026년 지침으로 다시 확인하고 썼다** — 작년 자료를 올해로
        옮기지 않는다(3절).
      · FAQ 「여러 지원을 동시에」 답과 `/ask`의 중복 질문 안내(`ASK_GUIDES`)를 이 글로 이었다. 글이 다루는 사업 7건
        (부모급여·아동수당·보육료·장애인연금·기초연금·생계급여·청년월세)을 `serviceExtras`에 넣었다 — 그 상세에
        「복지클릭이 따로 확인한 것」이 붙고 광고 대상이 된다.
      ── 허브 머리말 (`components/HubIntro.tsx` + `lib/hubNotes.ts`) ──────────────
      · 생애주기 7 · 대상 6 허브에 「이 목록은 이렇게 모았습니다」를 붙였다. **숫자는 전부 렌더 시점 집계**(건수·중앙/지자체·
        많은 시·도 셋·지급형태 셋·소득 기준선이 하나로 적힌 건수), 문장은 허브마다 따로 적었다.
      · 문장에 넣은 것은 **목록을 읽는 법**이다 — 청년 상한이 34·39세로 갈린다, 「청년」 칸에 장애인연금이 섞이는 이유는
        원문이 한 사업에 여러 시기를 적어서다, 장애인연금과 장애수당은 장애 정도로 갈린다 등. 쓰기 전에 전부 데이터로
        확인했고, **확인이 약한 문장 둘은 지웠다**(지자체 다자녀는 소득을 안 본다 → 40건 중 16건에만 소득 문구,
        장애인 감면은 "대부분 신청주의" → 근거 없음).
      · ⚠ `axes.ts` 머리말은 대상·생애주기가 「지자체 사업에만 붙는다」고 적어 두었는데 **지금은 중앙부처에도 붙어 있다**
        (청년 310건 중 107건이 중앙). 머리말을 믿고 "이 목록은 전부 지자체"라고 쓸 뻔했다 — 세어 보고 잡았다.
      · 주제(15)·혜택(7)·지역(17) 허브에는 아직 안 붙였다. 다음 후보.
      ── 남은 일 ──────────────────────────────────────────────────
      ── 색인 (같은 날 후속) ────────────────────────────────────────
      · **빙 — 사이트맵 재제출 + URL 21건 제출 완료.** `⋮ → Re-submit` 뒤 Status `Success` → `Processing`, 새로고침하니
        **Last crawl 9/15 · URLs discovered 378 → 377**로 라이브와 맞았다(재크롤링이 곧바로 끝난 경우다 — 옛 수가 남아
        있으면 실패가 아니라 대기다). URL 제출은 새 글 1 · 허브 13 · 상세 7 = 21건, 「Success: 21 URLs submitted」,
        **할당량 75 → 54**. ⚠ 왼쪽 메뉴의 실제 주소는 `/webmasters/submiturl`이다 — `/webmasters/url-submission`으로
        열면 화면이 「No pages found」로만 그려져 비어 있는 줄 알기 쉽다.
      · **구글 — 1건만 들어갔다.** `/guide/combined-support`(미색인) 「색인 생성 요청됨」. 그다음 `/life/pregnancy`·
        `/life/infant`에서 **「할당량 초과」**가 났고, 09-12 기록대로 **페이지를 새로 고친 뒤 한 번 더 시도했지만 또
        할당량 초과**였다. 전날(09-15) 오후 14:23~16:12에 22건을 넣어 **굴러가는 24시간 창이 아직 안 풀린 것**이다 —
        오늘 오후 이후에 남은 20건(허브 13 · 상세 7)을 넣는다.
      · ⚠ **검사·요청을 루프로 돌릴 때 판정을 `document.body.innerText` 전체에서 찾으면 앞 화면의 「할당량 초과」가
        남아 다음 URL까지 같은 판정으로 읽힐 수 있다.** 이번엔 닫기 단추를 눌러 지워졌고 재시도로도 같은 값이 나와
        진짜였지만, 다음에는 **요청 직후 새로 뜬 상자만** 읽도록 좁힌다.
- [x] **09-16 경쟁 조사 · 후속 개선 (`cea9ef5` 배포) · 보조금24 연동 (커밋함, 배포는 사용자 결정).**
      사용자 요청 — "다른 복지사이트에서 우리에 추가할 게 있는지 효율·실효성 기반으로 보고" → 조사 → "1~4번 전체 진행".
      ── 조사 (에이전트 셋, 전부 실제 화면·문서) ──────────────────────
      · 공공 5곳 — 복지로(총 5,606건 = 중앙 461·지자체 4,818·**민간 327**, 우리와 중앙·지자체 수가 정확히 일치) ·
        정부24 혜택알리미(10,925건, 옛 보조금24) · 온통청년(2,789건, **정책 비교하기**) · 고용24(계산기 4종·길잡이) ·
        서울복지포털(216건, 카드에 담당자 직통번호).
      · 민간 6곳 — **해피나눔이 보조금24+복지로를 함께 쓴다**(그래서 마감일 632건·구비서류가 있다. 매거진 99편이 전부
        자기 데이터를 센 분석 기사) · 웰로(welfarehello.com, 광고 5슬롯·포인트·쇼핑·B2B API) · 복지맵(80쪽, 서울 전용) ·
        혜택포털(62건 + 금액순 랭킹) · 지원금비서(과장 문구 위험) · 사설 보조금24 앱(평점 1.0).
      · 이용자 질문 109건 — 자격 20 · 신청·서류 16 · **중복 12** · 소득인정액 11 · 지급일 10 · **환수·탈락 7(글 0편)** ·
        **바우처 사용법 6(글 0편)**. 검색량(실업급여=1): K패스 2.43 · 문화누리카드 1.30 · 근로장려금 신청 1.02 ·
        국민연금 예상수령액 0.72 · 육아휴직급여 0.69 · **긴급복지지원 0.009**. 15개 중 **8개가 1월 정점**, 근로장려금은 5월이 저점의 14배.
      · **우리 서치콘솔 실적(28일)** — 클릭 32 · 노출 2,290 · 평균 순위 10.2. 노출되는 검색어 무리: 중위소득 %(「60 이하 **연봉**」 16) ·
        지역+청년월세(구미·인천) · 지급일 · **지침 PDF 파일 이름**(「★2026년 국민기초생활보장 사업안내.pdf」 외 7건).
      ── 만든 것 (`cea9ef5`, 배포·라이브 확인) ────────────────────────
      · 집계 글 셋 — `/guide/online-share`(복지로 온라인 신청 54건) · `/guide/deadline-share`(끝날 있는 것 15건·예산소진 32건) ·
        `/guide/official-docs`(지침 파일 335개·2025년 이하 83개). **원문을 한 글자도 안 옮기는 글**이라 애드센스 「원문 반복」에서 가장 멀다.
      · 질문 갈래 둘 — `/guide/refund`(조특법 제100조의8⑧ 정산 환수·제100조의9① 2년/5년 제한, 기초생활 제47조 반환명령·제46조② 징수) ·
        `/guide/voucher-use`(문화누리카드 12.31. 23:59 국고 반납·12월 취소 함정·합산·재발급 기한, 한국문화예술위원회 누리집).
      · 상세 「우리 수록에서 어디쯤인가」(`lib/amongListed.ts`) · 「신청 전 체크」 문의 전화 · `/compare` 나란히 보기(noindex,
        audit-sitemap 결과 화면 3 → 4) · `/income/*` 연봉 환산 칸. 안내 글 27 → 32편, 사이트맵 377 → 382.
      ── 보조금24 연동 (두 번째 원천) ────────────────────────────────
      · **키는 새 계정이다.** 사용자가 옛 계정 정보를 못 찾아 09-16에 새로 가입했다 → `GOV24_SERVICE_KEY`(64자, 복지로
        `DATA_GO_KR_SERVICE_KEY_ENCODED`와 **다른 계정의 다른 키**). 자동승인·무료·이용허락 제한 없음·개발계정 하루 10,000건.
        ⚠ 신청서에 **캡차**가 있어 마지막 제출은 사용자가 눌렀다(캡차는 대신 풀지 않는다).
      · `scripts/fetch-gov24.mjs`(원본 10,925건 × 2종 → `data-research/gov24/`, 22콜) · `scripts/build-gov24.mjs`(매칭 →
        `src/data/gov24.ts`). 상세에 **「신청할 때 필요한 서류」 절**(318쪽)과 근거 법령 조문 보강.
      · **맞물린 것 393건** — 구비서류 318 · 법령 259 · 자치법규 149 · 온라인신청 URL 130.
      · ⚠ **이름으로 맞추면 조용히 틀린다. 네 겹으로 걸렀다.**
        ㉠ 기관 급 일치(중앙 = 부·처·청…, 지자체 = 그 시·도 이름) — 「충청북도」가 지자체로 안 걸리던 버그가 있어 **끝 글자**로 판정한다.
        ㉡ 이름 유사도(2글자 Dice) **0.75 이상** — **0.71에서 「교육급여」와 「생계급여」가 둘 다 「주거급여」에 붙었다.**
        ㉢ 급여 종류·대상 낱말 집합 일치(생계/주거/교육/의료급여·장애인·아동…).
        ㉣ **도(道)와 시·군을 가른다** — 「충북 청년 월세 지원」이 「옥천군 청년 월세 지원」에 붙었다. 27건이 이 규칙에서 빠졌다.
      · 화면 규칙: 값은 **원문 그대로**, 절 머리에 「행정안전부 공공서비스 정보에서 옮김 · 정부24 사업명 · 소관기관 · 확인일」,
        맞물린 것이 없으면 절을 안 그린다. 복지로 원문 절은 건드리지 않는다.
      · 남은 것 — 신청기한에 **날짜가 든 것 60건**을 마감 표(closing)에 넣을지는 아직 안 정했다(지금 D-day는 15건뿐).
        못 맞춘 384건·애매 113건은 버렸다(3절).
      ── 색인 ────────────────────────────────────────────────────
      · 빙 — 사이트맵 재제출 + URL 21건(09-16 오전) · 12건(오후). 할당량 100 → 88.
      · 구글 — 오전 1건(`/guide/combined-support`), 오후 1건(`/guide/online-share`). **나머지는 할당량 초과** —
        전날 오후 14:23~16:12에 22건을 넣어 24시간 창이 안 풀렸다. ~~오후 4시 이후 남은 네 편을 넣는다~~ —
        **같은 날 12:20에 네 편 전부 「색인 생성 요청됨」**(deadline-share·official-docs·refund·voucher-use, 넷 다 미색인이었다).
        4시를 기다릴 필요가 없었다 — 한도는 시각이 아니라 굴러가는 창이라 **막혔던 날도 몇 시간 뒤 한 번 더 시도해 본다.**
        · ⚠ 개요 화면의 검사창에 스크립트로 Enter를 쏘니 **「실적」 화면으로 넘어갔다**(포커스가 딴 데 있었다).
          `computer`로 검사창을 **실제로 클릭 → 타이핑** 한 뒤 `document.activeElement`에 Enter를 쏘면 검사 화면으로 간다.
          다음 URL부터는 JS로 검사창에 focus만 주고 타이핑은 `computer type`으로 했다 — 네 건 사고 0.
      ── 이번에 걸린 함정 ────────────────────────────────────────
      · ⚠ **Bash heredoc의 python 스크립트에서도 역슬래시가 벗겨진다** — `.split(/?
/)`이 `.split(/?<개행>/)`으로 들어가
        tsc가 「Unterminated regular expression」을 냈다. 파일이 CRLF라 Edit 도구의 여러 줄 매칭도 실패했다.
        **정규식이 든 줄은 node에서 `String.fromCharCode(92)`로 짓는다**(09-13에 적어 둔 것과 같은 자리, 이번엔 python 경유였다).
      · 스크롤한 뒤의 Browser 캡처가 또 **빈 지면**으로 나왔다(09-10 기록). DOM 글자로 확인했다.
      · 다른 세션이 쓰는 크롬 탭을 `navigate`로 빼앗을 뻔했다 — 조사 에이전트가 웰로를 보고 있었다. **탭을 새로 만들어 쓴다.**
- [x] **09-16 좁은 화면 손질 — 붙는 헤더 99→57px · 서식 파일 이름 (`8a7bcb2`, 배포·라이브 확인).**
      사용자가 "모바일로 어떻게 보이는지" 열어 보라고 해서 375px로 훑은 뒤, 눈에 걸린 둘을 고쳤다.
      · **헤더가 두 줄(99px) 통째로 붙어 있었다** — 스크롤 내내 화면 위 12%. 로고·검색 줄만 붙이고
        분류 칩 줄은 좁은 화면에서 함께 흘려보낸다. 넓은 화면(sm 이상)은 자리가 남으니 `sm:sticky sm:top-14`로 그대로 붙인다.
        · ⚠ **두 줄을 `<header>` 하나로 감싸면 안 된다.** `sticky`는 **부모 상자 안에서만** 붙어서,
          100px짜리 헤더 안에 넣으면 그 헤더가 지나가는 순간 로고 줄까지 같이 사라진다(실제로 그렇게 만들었다가
          DOM 값 `top: -4690`으로 잡았다). 둘 다 `<body>`의 자식(형제)이어야 한다. 라이브 실측 —
          375px: 헤더 `top:0`·높이 57, 칩 줄 화면 밖 / 1280px: 칩 줄 `top:56`~`bottom:99` 그대로.
        · 목차 앵커 여백도 폭으로 갈랐다 — `scroll-mt-16 sm:scroll-mt-28`. 안 그러면 좁은 화면에서 제목 위가 빈다.
      · **서식 파일 이름 한 줄에 서로 다른 셋이 섞여 있었다** — 서식 번호·문서 이름·어느 고시에 딸린 것인가.
        `lib/formName.ts`가 갈라서 이름은 크게, 번호와 출처는 밑에 작게 적는다. 확장자는 작은 글씨로 뒤에.
        · **글자를 고치거나 요약하지 않는다** — 자리만 옮긴 것이라 합치면 원문이고, 잘라 낸 조각도 화면에 남으며,
          원래 파일 이름은 링크 `title`에 통째로 들어간다(3절).
        · 출처로 **확실히 읽히는 괄호만** 뗀다(고시·시행규칙·시행령·조례·훈령·예규·지침·규정·법률 · 제○○호 · 8자리 날짜).
          「(신규, 변경)」·「(사회복지시설의 대표자가 신청할 경우)」는 문서 이름이라 그대로 둔다. 같은 이름이 둘일 때 붙는
          「(2)」는 note에도 안 적고 버린다 — 화면에 「· 2」가 떠서 무슨 말인지 알 수 없었다.
        · 실측 1,178개: 이름 길이 **평균 29.2 → 21.4자**, 40자 초과 **159 → 23개**, 라벨이 비는 것 0.
        · ⚠ **높이로 얻은 것은 크지 않다** — 서식 절 기초연금 158→147px, 가장 긴 모성보호육아지원 198→187px(6~7%).
          처음에 "파일 넷이 화면 절반"이라고 보고했는데 과장이었다. **실제 이득은 이름이 제목 줄에 한 줄로 온전히
          보이는 것**이다. 화면 얘기를 할 때는 눈으로 본 인상 말고 `getBoundingClientRect`로 재서 적는다.
      · 라이브: 사이트맵 **382개 전부 200** · 375px 가로 넘침 0 · 서식 절 옛 모양(파일 이름 통째) 0건 · `title`에 원문 이름 확인.
      · ⚠ 스크롤한 뒤의 Browser 캡처가 **또** 빈 지면·엉뚱한 배율로 나왔다(09-10·09-16 기록). 화면 판정은 DOM 값으로 했다.
- [x] **09-16 마무리 점검 — 오늘 커밋 넷을 리뷰해 버그 여섯 고침 (`8124c62`, 배포·라이브 확인).**
      사용자 요청 "오늘 작업 버그·오류 확인하고 마무리". 정적 검사·빌드·감사·라이브 382쪽 전부 200은 원래 통과였고,
      코드 리뷰 에이전트가 **실제 데이터로** 돌려서 나온 것들이다.
      · **보조금24에 남의 지역 서류가 붙어 있었다(가장 무거움).** 경상남도교육청 교복비 상세에 「경상북도 김천시」 서류.
        시·도를 **앞 두 글자**로 대조해서 경상남·북, 충청남·북이 같은 「경상」「충청」이 됐다. → 기관명 첫 낱말이
        `sidoName`과, 둘째 낱말이 시·군·구와 **낱말째** 같아야 통과. 시·군·구 칸이 비어도 부서가 「강원특별자치도
        인제군 …」이면 군 사업으로 본다. 「인천광역시서해구시설관리공단」(띄어쓰기 없음)이 「공단」으로 끝나 중앙으로
        읽히던 것도 막았다. 한 보조금24 사업이 우리 둘에 붙으면(월세대출 ↔ 월세대출 **보증**) 이름이 똑같은 하나만 남긴다.
        393 → **390건**(빠짐 6 · 제자리 찾은 군·시 사업 3). 중앙 11건은 소관기관이 부처 산하기관이라 달라도 맞는 짝이었다
        (국세청·서민금융진흥원·국민건강보험공단…) — 부처 이름 대조는 넣지 않았다.
      · **자치법규만 있는 126건에서 근거 법령 절이 안 나왔다** — 조건이 `laws`만 봤다. 목차도 같은 함수(`hasLawSection`).
      · **신청 서류의 「○ 대리신청시 구비서류 / - 대리인 신분증」을 한 단계로 펴서** 누구나 내는 서류처럼 보였다(316건 중 61건).
        가장 얕은 줄보다 더 들여쓴 줄과 ○ 아래 - 줄을 들여 그린다. 글자는 그대로.
      · 집계 글 둘의 **제목·설명에 910·54·15를 손으로 적어 뒀다**(본문은 계산값) → metadata를 집계값으로. `guides.ts` 제목은
        숫자 없이. 「마감일이 적힌」도 틀린 말이었다 — 그 15건은 **사업 기간 끝날**이라 2050·2099년이 섞여 있다.
      · `/compare` 「어떤 지원인가」 줄이 실제로는 지원 대상 첫 문장 → 「지원 대상」. 이름 옆에 마감·D-day 딱지(여기만 없었다).
      · 허브 머리말 「“영유아”이 적힌」 조사 오류 → 「칸 값이 “…”인 사업」(받침과 무관). 「라고」로 바꿨다가 그것도
        받침에 따라 「이라고」가 돼서 다시 고쳤다.
      ── 이번에 걸린 함정 둘 ──────────────────────────────────────
      · ⚠ **`node -e '…'`(bash 작은따옴표) 안의 템플릿 문자열에 `\\r?\\n`을 적어도 역슬래시가 벗겨져** 파일에 진짜 CR·LF가
        들어갔다(tsc 「Unterminated regular expression」). heredoc만의 문제가 아니다. **역슬래시가 드는 코드는 Write 도구로
        .cjs 파일을 쓰고 `String.fromCharCode(92)`로 짓는다.**
      · ⚠ **`service/[id]/page.tsx`는 한 파일 안에 CRLF와 LF가 섞여 있다**(HEAD 기준 641줄만 CR). 줄바꿈을 통째로 맞춰
        쓰면 diff가 2,726줄이 된다. 원본 줄의 끝맺음을 살리고 바뀐 덩어리만 끼워 넣어 40줄로 되돌렸다 — 고친 뒤
        `git diff --stat`이 수천 줄이면 내용이 아니라 줄바꿈이다.
- [x] **09-17 복지 정보 점검 — 복지로 변경 0 · 보조금24 신청기한으로 마감 10건 더 (`d53361a`, 배포).**
      사용자 요청 "새로운 거, 마감된 거 꼼꼼하게".
      · 복지로 목록 5,279건을 **09-15 목록과 직접** 대조 — 새 사업·없어진 사업·항목 변경 0. (parsed.json 대조는
        세종 53건이 늘 "새로 생김"으로 나오니 직전 점검 폴더와 본다 — 09-15에 적은 그대로.)
      · 상세는 상위 100건을 또 받지 않고 **한 번도 재대조 안 한 중앙부처 95건**(102~268위)을 받았다 — 95건 같음,
        옛 원본으로 되돌림. 재대조 이력은 `rechecks.json`의 rechecked를 모아 고른다. 중앙 수록 330 중 179건이 한 번 이상 대조됨.
      · 지자체는 목록의 `lastModYmd`가 하나도 안 바뀌어 상세를 다시 안 받았다.
      · 복지로 본문에 지난 날짜가 적혔는데 마감 표에 없는 것 8건을 훑었다 — 전부 출생일·이용기간·시행일이라 결함 아님.
      ── 보조금24 신청기한을 마감 표에 합쳤다 ─────────────────────────────
      · 보조금24를 다시 받아 대조(33건 변경)했지만 **화면에 나가는 값(서류·법령·URL·신청기한)은 0건 변경.**
      · 그 「신청기한」 칸에 **날짜가 온전히 적힌** 우리 사업이 28건 — 그중 복지로 쪽에 기간이 없어 「마감」이 안 붙던
        것이 10건(평택 청년월세 4/17 · 부산 청년 기쁨카드 4/24 · 강원 디딤돌 2배 적금 · 제주 청년 이사비 등).
        `build-gov24.mjs`가 `src/data/gov24Closing.ts`를 만들고 `closing.ts`가 합친다. 「기간이 지난 지원」 13 → **22건**.
      · **좁게 읽는다** — `~` 범위만, 왼쪽은 연도까지 적힌 날짜, 일이 없으면(「매년 3~4월」「2026.3.~2026.11.」) 버림,
        범위가 여럿이면 가장 늦은 끝날, 끝날이 **작년이면 버림**(「'25.7.21.~'25.11.21」 서울형 가사서비스 — 올해 다시
        받는지 모른다. 틀리게 「마감」을 붙이면 받을 수 있는 사람이 포기한다).
      · **합치는 규칙** — stated는 그대로 · 복지로에 사업 기간(program)만 있으면 보조금24 신청기한이 이김(전북형
        청년활력수당: 사업 ~2027, 신청 3/20 끝) · 둘 다 신청 기간이면 **끝날이 늦은 쪽**. ⚠ 아동건강체험활동비는 복지로
        본문에 작년 기간(2025.1.8.~12.31.)만 남아 **그동안 「마감」으로 잘못 나가고 있었다** — 보조금24엔 올해
        2026.1.2.~12.31. 이제 딱지 없음. 경기여성 취업지원금은 복지로 4.20 · 보조금24 4.30으로 어긋난다(둘 다 지남).
      · 화면: 딱지·띠·「신청 전 체크」 신청 기간 줄·모음 목록에 **「보조금24」 출처를 밝힌다.** 상세 띠는 새 갈래 `gov24`.
      · 곧 D-10에 드는 것: 대구 다자녀 고등학교 입학축하금 **9/30**(9/20부터) · 청년희망적금 9/30.
      · 받은 날을 UTC로 찍던 것을 KST로(`fetch-gov24`·`build-gov24`) — 오전 9시 전에 돌리면 어제 날짜가 됐다.
      · **남은 것**: 보조금24 신청기한의 「2026년은 신규 대상자 미 모집」(청년 전월세보증금 대출이자, 360위) 같은 문장형
        마감은 안 읽는다. 「원문에 신청 마감이라고 적혀 있다」는 기존 stated 문구와 뜻이 달라서다.
- [x] **09-17 첫 아침 루틴(스킬 `bokjiclick-morning`) — 새 글 아이돌봄서비스 (`9686102`, 배포).**
      사용자가 매일 하던 요청을 스킬로 묶었다(`~/.claude/skills/bokjiclick-morning/SKILL.md`, 저장소 밖). 같은 날 오전 점검 직후라 가볍게 돌았다.
      · 복지로 목록을 다시 받아(`check/2026-09-17b`, 대조 뒤 지움) 오전 목록과 대조 — 0/0/0. 중앙 상세는 오전에 95콜을 써서 다시 안 받았다.
      · 보조금24 재수집 — 매칭 390 그대로, git diff 0. 마감 표 47건 중 지남 22 · 열흘 안 0(9/30 둘은 9/20부터 D-10).
      · 트렌드: 뉴스는 추석 민생지원금(시·군마다 달라 수록 모델 밖)·기초연금 차등 지급 개편(데이터랩 실업급여의 0.02배)·발달장애인 돌봄
        국가책임제(0.01배). **아이돌봄 0.87배**인데 복지로 조회수 110만 사업에 우리 글이 없어 골랐다. 서치콘솔 7일 검색어: 과학(문화)바우처 32노출 ·
        중위소득 60·65% · 인천·구미 청년월세 · 근로장려금 지급일 · 두루누리 사회보험 9 — 과학문화바우처·두루누리는 다음 글 후보.
      · `/guide/childcare-service` + `ChildcareCalc` + `lib/childcareService.ts` — 사이트맵 382 → 383, 안내 글 33편.
        · ⚠ **원문 첨부 「2026년 아이돌봄 지원사업 안내」 PDF(96MB·309쪽)는 글자가 그림이라 pypdf·pdftotext 둘 다 0자.** 같은 부처 누리집
          `idolbom.go.kr/front/biz/srvcGuide`에 요금표가 HTML로 있다(curl로 받아 표를 칸째 읽음). 30분당 정부지원금 × 2가 복지로 원문 시간당
          지원금과 전 칸 일치, 본인부담금 = 30분 요금 − 정부지원금이 표 32칸 전부 일치.
        · 어긋난 것 — 한부모 등 가정 **취학(B) 가형**: 누리집 안내문 「75% → 80%」 vs 복지로 10,872원(85%). 누리집 일반 요금표가 이미 B 가형 80%라
          안내문이 옛 값으로 보이지만 우리 해석이다. 계산하지 않고 화면에 둘 다 적었다. 다자녀 10%·인구감소지역 5%는 겹칠 때 순서 안내가 없어 계산 제외.
      · 라이브: 사이트맵 383개 전부 200 · 새 글 canonical·광고 코드·공유 이미지 200 · 상세 WLF00000024에서 새 글로 이어짐.
      · 색인: 구글 새 글 요청(미색인) + WLF00000024 재요청(이미 색인, 내용 변경). 빙 사이트맵 재제출(Processing) + URL 2건(할당량 100 → 98).
        · ⚠ 두 번째 검사에서 본문 전체 정규식이 **앞 URL 판정 「있지 않음」을 다시 읽었다**(09-13에 적은 함정). 보이는 잎 요소만 골라 읽으니 「있음」이었다.
- [x] **09-17 애드센스 기준 재점검 — 목록 화면 광고 코드 뺌 (`0c15595`, 배포).** 사용자 요청 "애드센스 기준 점검".
      · 통과: tsc·eslint·빌드·감사 셋 · 라이브 사이트맵 383쪽 전부 200 · noindex·canonical·h1·푸터·제목/설명 중복 0 ·
        사이트맵 밖 내부 링크 645개 깨짐 0·광고 코드 0 · 결과·개인 화면·404 광고 코드 0 · ads.txt·robots·www/http 308 ·
        방침(AdSense·쿠키·광고 설정)·소개(운영자)·문의(메일) · 과장 문구는 FAQ 「무조건 탈락인가요? → 아닙니다」 부정문뿐.
      · **새로 잰 것 — 광고 코드 페이지마다 원문 글자 대 나머지.** 텍스트 조각이 `services.ts` 문자열(12자 이상)에 들어
        있으면 원문으로 셌다(카드 끝 「…」는 떼고). 안내 글·홈·소개·FAQ는 원문 0~3%. **주제·혜택·지역·소득 허브와 `/service`
        목록은 50~74%**였고 "나머지"도 대부분 칸 이름·건수다(`/theme/legal` 약 520자). 생애주기·대상 허브는 비율은 비슷하지만
        `HubIntro` 머리말이 있다.
      · 그래서 **이 다섯 갈래에서 광고 코드를 뺐다**(138 → 93쪽, `AD_MIN_ITEMS` 조건도 쓸 곳이 없어 지움). 아직 승인 전이라
        광고가 안 나와 잃는 것이 없다. 색인·사이트맵(383)은 그대로.
      · **같은 날 후속 — 주제 15 · 혜택 7 · 지역 16 · 소득 14 허브에 머리말을 붙이고 광고 코드를 되돌렸다.**
        `hubNotes.ts`에 THEME_NOTES·BENEFIT_GUIDES·REGION_NOTES·INCOME_NOTES(혜택 문단은 `benefits.ts` note 재사용).
        문장은 허브마다 사실 목록(건수·상위 사업·지급형태·주기·시군구·부서)을 뽑아 보고 썼고, 원문을 옮긴 곳은 「」로 묶었다
        (서울형 기초보장 대상 문장, 대구 청년희망적금 선발 방식, 천사지원금 「소득수준 무관」, 세종 미래적금 매칭 등 — 전부 수록 원문 대조).
        · `HubIntro`를 구성에 따라 가른다 — 전부 중앙(주제)이면 "전부 중앙부처 사업", 한 시·도뿐(지역)이면 **시·군·구로 한 단계 내려**
          세고 교육청은 따로 센다, 소득 허브는 기준선 줄을 뺀다(`incomeLine={false}`). 지급 주기 줄을 새로 넣었다.
        · 빌드 HTML에서 잡은 어색한 문장 셋 — 「전남광주통합특별시 1건 순으로 많습니다」(1건짜리 순위), 「현금 10건 순」(값이 하나),
          「서구 2건 순」. 1건끼리거나 하나뿐이면 순위를 안 쓰고 「모두 …」로 쓴다.
        · 광고 코드는 **머리말이 있고 항목 `AD_MIN_ITEMS`(10)건 이상**인 허브에만(주제 에너지·소득 32·40·48·52·70·75·80은 제외).
          색인 허브 65쪽 전부 머리말, 광고 코드 57쪽. `/service` 전체 목록은 머리말이 없어 계속 뺀다. 사이트맵 383 그대로.
- [x] **09-17 다시 올 이유 다섯 — 이용자 관점 점검 후속 (`06f0595`, 배포·라이브 확인).**
      · **지역을 고르면 그 지역 사업 먼저** — `/find`는 「경기에서 직접 하는 사업」·「전국 어디서나」 두 절,
        `/ask`는 조건만 맞는 목록에서 지역 사업을 앞으로(안정 정렬). 전: 경기·청년 첫 20건 중 경기 0건.
      · 첫 화면 **관심 지원 칸을 물어보기 상자 바로 밑으로**(375px 1,788 → 851px). 물어보기 위로는 안 올린다 —
        브라우저에서 늦게 그려져 첫 화면 안이면 아래가 밀린다.
      · 관심 지원에 **「9/13 내용 바뀜」**(`lib/changedAt.ts`, changedAt 있는 20건만 props로). 저장한 날을 안 적으므로
        「저장 뒤에」라고 쓰지 않는다.
      · **보내기 단추**(`ShareButton`) — 상세 제목 밑·안내 글 끝. 기기 공유 창, 없으면 주소 복사, 복사도 막히면 안내 문구.
        카카오 SDK는 안 싣는다.
      · **「다음에 놓치지 않게」 확인 일정**(`NextYearCheck` · `lib/reminder.ts` · `/remind/<id>.ics`) — 신청 기간
        (period·gov24)이 지난 사업에만. 날짜 = 지난 신청 시작일 + 1년 − 14일, 오늘이 지났으면 다음 해.
        **공고일이 아니라 우리가 정한 확인할 날**이라고 화면·일정 설명 둘 다에 적는다(3절). 파일은 요청 때 만든다
        (날짜가 오늘에 달림). 확인: `node scripts/verify-reminder.mjs`. `ics.ts`는 감싸는 부분만 뗐고 기존 `/calendar/*.ics`는 바이트까지 같다.
      · **안 한 것 — 이메일 알림.** 계정 스위치(`NEXT_PUBLIC_AUTH_ON`)·방침 7일 전 공지와 묶여 있어 위 다섯의 반응을 본 뒤 정한다.
- [ ] 색인 요청 한도는 **고정된 수가 아니다.** 굴러가는 24시간 창이라
      전날 얼마나 썼는지에 따라 그날 들어가는 수가 달라진다. 실측: 09-03 3건,
      09-04 11건, 09-05 11건, 09-06 16건, **09-07 48건(할당량 안 걸림)**,
      09-08 25건, 09-09 오전 26 + 오후 15 = **41건(오후에 「할당량 초과」)**.
      "하루 11건"으로 외우면 09-06처럼 다섯 건을 남기고 그만두게 된다.
      · **09-07에 한도가 자릿수째로 달라졌다.** 48건을 넣도록 "할당량 초과"가
        한 번도 안 떴다. 앞선 나흘(3·11·11·16)과 같은 규모가 아니다. 그래서
        "끝까지 넣어 본다"는 규칙이 **끝이 없는 작업**이 됐다 — 한 건에 75초씩
        드니 48건에 한 시간이 걸렸다. 이제는 **넣을 값어치가 있는 데까지**만
        넣는다. 조회수 200위 아래는 사이트맵만으로도 붙고 있어 수동 요청의
        값어치가 거의 없다(아래 "이미 색인됨" 항목들 참고).
      · **요청 전에 URL 검사부터 한다.** 09-04에 상위 20건을 훑었더니 절반이
        이미 색인돼 있었다. 이미 색인된 건에 요청을 쓰면 그날 치가 사라진다.
      · 09-04 요청 11건: 이동통신요금감면·기초연금·유아학비·정신건강바우처·
        부모급여·초중고교육비·긴급복지생계·교육급여·기저귀조제분유·
        청소년특별지원·근로자녀장려금.
      · 09-05 요청 11건: 자산형성지원(WLF00000100)·생계급여·디딤씨앗통장·
        초중고교육정보화·자립준비청년자립수당·여성청소년생리용품·노인일자리·
        청년월세지원(WLF00004004)·장애인활동지원·장제급여·노인맞춤돌봄.
        12번째 WLF00003265 장애수당에서 할당량이 걸렸다. 뽑는 명령은 docs/02 3절.
      · 09-06 요청 16건(조회수 42~83위): 장애수당·긴급돌봄·20세이하운전면허학원비·
        스포츠강좌이용권·평생교육이용권·방과후학교자유수강권·학생교육비꿈드리미·
        백내장수술비·농식품바우처·독거노인장애인응급안전안심·의료급여틀니임플란트·
        개인채무조정·임신사전건강관리·경기도어린이청소년교통비·가사간병방문·
        긴급복지연료비전기요금.
        **17번째 WLF00000917 긴급복지 주거지원에서 할당량이 걸렸다.**
      · **09-07 요청 48건.** 새 글 세 편(`/guide/calendar`·`/guide/tax-credit`·
        `/guide/pay-dates`)을 먼저 넣고, WLF00000917부터 조회수 84~166위를
        훑어 45건을 더 넣었다. 같은 구간에서 **35건은 이미 색인돼 있어
        건너뛰었다** — 요청한 것보다 건너뛴 것이 더 많은 구간이 나오기 시작했다.
        **다음은 조회수 167위 WLF00005409(장애인 버스요금 지원)부터**다.
      · **다만 다음 회차는 조회수 순서보다 먼저 넣을 것이 다섯 있다.**
        전부 **내용이 바뀌었는데 사이트맵 lastmod가 안 움직인** 것들이라,
        수동 요청을 안 하면 구글이 늦게 온다. 상세가 전부 `SERVICES_UPDATED`를
        쓰는 대가다(800개를 오늘로 미는 것보다 낫다).

        | URL | 무엇이 바뀌었나 |
        |---|---|
        | ~~`/service/WLF00005440`~~ | 제목이 「…(모두의카드·K-패스)」로 — 09-08 완료 |
        | ~~`/service/WLF00000092`~~ | 제목이 「…(기저귀바우처)」로 — 09-08 완료 |
        | ~~`/service/WLF00006266`~~ | "6월 예정" 지남 띠 — 09-08 완료 |
        | ~~`/service/WLF00000055`~~ | 제목이 「통합문화이용권(문화누리카드)」로 — 09-08 완료 |
        | ~~`/guide/unemployment`~~ | 새 글 — 09-08 완료 (09-10 색인 확인) |
        | ~~`/guide/pay-dates`~~ | 15일이 들어감 — 09-08 완료 (09-10 색인 확인) |
        | ~~`/guide/k-pass`~~ | 09-09 요청 완료 (09-10 색인 확인) |
        | ~~`/guide/tax-credit-amount`~~ | 09-09 요청 완료 (09-10 색인 확인) |
        | ~~`/guide/youth-savings`~~ | 09-09 요청 완료 (09-10 색인 확인) |
        | ~~`/guide/emergency`~~ | 09-09 요청 완료 (09-10 색인 확인) |

        **이 표는 전부 끝났다.** ⚠ 위 여섯 줄은 09-08에 넣고도 취소선을 안 그어
        둬서, **09-10에 "아직 안 넣은 것"으로 읽고 오늘 목록에 또 올릴 뻔했다.**
        같은 자리가 두 번 읽히는 표는 넣은 날 바로 지운다.
        조회수 백로그(167위 `WLF00005409`~)는 09-10에 **그만뒀다** —
        아래 「색인 요청 기준을 바꿨다」.
      · **빙은 이번엔 재제출이 필요 없다.** URL 수가 804 그대로다(추가 0 ·
        삭제 0 · lastmod 0). 빙 URL 제출은 예정대로 **501~600위**를 넣는다.
      · **09-07에 한 건이 「색인 생성 요청이 거부됨」으로 돌아왔다** —
        WLF00005448 다문화가족 자녀 교육활동비. "실시간 테스트 중에 URL에서
        색인 생성 문제가 감지되었습니다." 사이트가 아니라 **내 후보 목록이
        틀렸다.** 그 페이지는 noindex이고 사이트맵에도 없다.
        원인은 docs/02 3절의 뽑는 명령이 **상투 문구를 길이에서 안 뺀
        옛 규칙**이라서다(그 명령은 713건, 실제 색인 대상은 707건).
        **후보를 뽑을 때는 `types/welfare.ts`의 `bodyText`와 같은 규칙을
        쓴다** — `isCriteriaBoilerplate`인 selectionCriteria는 빼고 센다.
        뽑은 뒤 라이브 사이트맵과 대조하면 이 실수는 한 번에 걸린다.
      · 09-06에 검사만 하고 건너뛴 것(이미 색인됨) 15건: 국민취업지원제도·
        의료급여본인부담면제·의료급여·암검진·연장형보육료·주거안정월세대출·
        긴급복지의료·행복주택·버팀목전세·재난적의료비·대중교통비환급·
        차상위본인부담경감·청년월세(5691)·발달재활·고위험임산부 외.
        **조회수 42~85위를 훑어 절반 넘게 이미 들어와 있었다.** 사이트맵만으로
        색인이 붙고 있다는 뜻이고, 수동 요청의 값어치는 그만큼 줄어든다.
      · **판정과 클릭을 한 번에 끝내는 법.** 좌표로 「색인 생성 요청」을 누르면
        이미 색인된 페이지에서는 버튼 위치가 달라 헛클릭이 난다. 본문에서
        "등록되어 있지 않음"을 확인한 뒤에만 누르도록 자바스크립트 한 줄로
        묶는다 — `버튼.click()`은 실제로 요청이 들어간다(09-06 확인).
      · 09-05에 검사만 하고 건너뛴 것(이미 색인됨): 한부모아동양육비·
        자활성공지원금·의료급여임신출산·통합문화이용권·과학문화바우처·
        일상돌봄·양곡할인·자활근로. 조회수 19~46위를 훑어 절반이 이미
        들어와 있었다 — 사이트맵만으로 색인되고 있다는 뜻이다.
      · 스크린샷 좌표를 믿지 말 것. 09-04에 GSC 캡처가 깨져 화면이 축소된 것처럼
        보였다(실제 뷰포트는 1910px). `k = 캡처 프레임 폭 / innerWidth`로 DOM
        사각형을 환산해 누른다. **프레임 폭은 고정값이 아니다** — 09-05에는
        1287로 시작해 세션 도중 1314로 바뀌었다. 좌표가 한 번 빗나가면 상수를
        의심하지 말고 다시 잰다. 클릭 뒤에는 본문 텍스트로 "색인 생성 요청됨"을
        확인한다.
      · **검사창은 `ctrl+a`로 안 지워진다.** 09-05에 ctrl+a가 글자 `a`로
        들어가 `ahttps://...`가 됐고, 엔터를 눌러도 앞 URL 결과가 그대로
        떠 있었다. 그 화면을 새 URL의 결과로 읽으면 **엉뚱한 판정 위에
        "다시 요청"을 누르게 된다.** `triple_click`으로 줄을 통째로 잡고
        덮어쓴다. 그리고 판정 전에 **화면에 찍힌 URL이 방금 넣은 것인지**
        본문에서 정규식으로 확인한다.
      · 요청 뒤 대화상자는 **닫기로 닫는다.** Return을 누르면 포커스가 남은
        "다시 요청"이 눌려 할당량이 한 건 날아간다.
- [ ] 빙은 **URL 제출이 하루 100건**이고 구글 한도와 완전히 별개다.
      · 09-03 상위 1~100위, 09-04 101~200위, 09-05 201~300위,
        09-06 301~400위, 09-07 401~500위, **09-08 501~600위** 제출
        (매번 잔여 할당량 0으로 확인). 09-08 제출 뒤 목록 총계가
        `1 — 25 of 600`. **09-09 오후에 601~700위 100건 제출** —
        「성공: 100개의 URL을 제출하였습니다」, 총계 `1 — 25 700의`,
        할당량 100 → 0. **09-10에 상세 아닌 100건을 넣어 총계 800이 됐다**(아래).
      · **초기화 문구를 값으로 읽고 다시 와 보면 된다.** 09-09 아침에
        `2 hrs`라 그날 몫으로 남겼는데, 오후에 다시 열어 보니 100으로
        차 있었다. 기다릴 만한 문구면 하루를 버리지 않는다.
      · **09-09에는 초기화가 2시간이었다.** 할당량 0으로 시작한 것은 09-08과
        같은데 화면 문구가 `Quota will reset in **2 hrs**`였다(09-08은 3분).
        **초기화까지 얼마인지는 그때그때 다르다** — 문구를 값으로 읽고
        기다릴 만하면 기다리고, 아니면 그날 몫으로 남긴다.
        09-09분(601~700위)은 **같은 날 오후에 넣었다**(위 항목).
      · **목록은 docs/02 3절 명령을 그대로 쓰지 말 것.** 그 명령은 상투 문구를
        본문 길이에서 안 빼는 옛 규칙이라 색인 대상이 아닌 URL이 섞인다
        (713 대 707). `types/welfare.ts`의 `bodyText`와 같게 세고, 뽑은 뒤
        **라이브 사이트맵에 전부 있는지 대조한다**(09-07에 100건 전부 확인).
      · 제출 전에 **`Quota left for today`를 값으로 읽는다.** 09-07엔 100으로
        시작해 제출 뒤 0이 됐다. 넣기 전에 이미 0이면 그날은 끝난 것이다.
      · 빙은 배포로 URL이 늘어도 **사이트맵을 스스로 다시 읽지 않는다.**
        9/1자 556개 → (재제출) 669개 → 804개에서 또 멈춰 있었다(라이브는 806).
        **수집·배포 뒤에는 빙 사이트맵을 반드시 다시 제출한다.**
        09-06 재제출 뒤 806개로 라이브와 일치. Status는 `Processing`으로 바뀌었다.
      · **같은 날 두 번째 재제출(09-06).** 그날 두 번째 배포로 라이브가 803이
        됐는데(상투 문구 정리로 6건 빠지고 새 글 1편 추가) 빙은 806에 멈춰
        있었다. `⋮ → Re-submit` 뒤 **Last submit이 9/6으로, Status가
        `Processing`으로** 바뀌는 것을 값으로 확인했다.
        `URLs discovered`는 빙이 다시 긁은 뒤에 바뀐다 — 누른 직후에는 옛 수가
        그대로 806으로 남아 있으니 그걸 실패로 읽지 말 것.
      · 상단 통계가 `Known sitemaps 2`인데 표에는 행이 하나뿐이다(필터는 전부
        All). 예전 `bokjimap.co.kr` 사이트가 계정에 남은 것과 관련일 수 있다.
        사이트 삭제는 되돌릴 수 없어 사용자만 한다.
      · 재제출은 사이트맵 행 오른쪽 **`⋮` → Re-submit**이다. 상단의
        "Submit sitemap" 단추는 새 사이트맵을 등록하는 자리라 눌러도
        패널이 안 열릴 때가 있다. 누른 뒤 **Status가 `Processing`으로
        바뀌는지** 값으로 확인한다(09-05 확인).
      · 제출창은 리액트 제어 필드라 키 입력이 안 들어간다.
        `ta.focus(); ta.select(); document.execCommand("insertText", ...)`로
        넣는다(09-05엔 마우스 클릭 없이 이것만으로 들어갔다). 그리고 **넣은
        값을 다시 읽어 줄 수·첫 줄·끝 줄을 확인한다.** 툴의 "Typed ..." 성공
        반환은 근거가 못 된다.
      · 제출 패널의 Submit 단추는 `.ms-Panel.is-open` 안에 있다. 본문 전체에서
        `button`을 훑으면 안 잡힌다 — 패널을 먼저 찾고 그 안에서 고른다.
      · 빙에서는 `javascript_tool`이 쿼리스트링 때문에 차단될 때가 있다.
        `location.href` 같은 걸 반환하지 말고 필요한 값만 돌려받는다.

---

## 7. 작업 원칙

우선순위: **정확성 > 검증 > 최소 변경 > 명확성 > 유지보수성**

- 파일·API·스키마가 있다고 가정하지 말고 먼저 읽어서 확인한다.
- 요청된 작업에만 변경을 국한하고, 관련 없는 리팩토링은 하지 않는다.
- 검증(빌드·린트·타입·실제 화면) 없이 "됐다"고 말하지 않는다.
- 막히면 멈추고 무엇이 막혔는지, 무엇이 검증됐는지 명확히 보고한다.

### 검증 루틴 (데이터·기능 변경 후)

1. `npx tsc --noEmit` + `npx eslint src --max-warnings=0` + `npm run build`
2. 데이터 정합성 스크립트 (id 중복, 날짜 논리, 필수 필드 누락)
3. `node scripts/audit-sitemap.mjs` — **몇 개가 나갈 것인가**(소스의 규칙)
4. `node scripts/audit-indexability.mjs` — **그게 색인될 수 있는가**(빌드 결과물)
   · 사이트맵에 있는데 noindex, 자기를 안 가리키는 canonical, **제목·설명 중복**
   · 3번은 소스를, 4번은 렌더된 HTML을 본다. 규칙이 맞아도 HTML이 틀릴 수 있다.
5. 실제 화면 확인 (모바일 가로 스크롤 포함)

### 렌더된 HTML을 문자열로 검사할 때 (두 번 걸린 함정)

**검사 스크립트가 틀려서 멀쩡한 사이트를 "깨졌다"고 보고하는 일이 반복됐다.**
문장이 안 잡히면 사이트를 고치기 전에 **검사 쪽을 먼저 의심한다.**

- **React는 보간값 둘레에 `<!-- -->`를 넣는다.** `{SERVICES_UPDATED}에 받아`가
  HTML에서는 `<!-- -->2026-09-04<!-- -->에 받아`다. 그래서 `grep -F`가 실패하고,
  태그를 지우면 그 자리가 공백이 되어 `2026-09-04 에 받아`처럼 **없던 띄어쓰기가
  생긴다.** 태그를 지운 뒤 `.replace(/\s+/g," ")`로 접고, 정규식에도 ` ?`를 둔다.
  (09-06에 "5개 조문이 전부"와 "출처 줄"에서 각각 한 번씩 걸렸다.)
- **`&#x27;`·`&quot;`·`&amp;`는 버그가 아니다.** React가 `'`·`"`를 이스케이프한
  것이고 브라우저는 제대로 그린다. 깨진 글자를 찾을 때는 HTML이 아니라
  **innerText**를 보거나, 이름 엔티티(`&rarr;` `&rsquo;`)만 골라 센다.
- **bash `&&` 사슬은 `grep -c`가 0을 세면 끊긴다**(종료코드 1). 뒤 명령이
  조용히 안 돌아 **옛 파일을 그대로 읽고** 오판한다. 검사 스크립트는 사슬 대신
  줄을 나눠 쓴다.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
