/**
 * 안내 글 목록.
 *
 * 왜 만들었나. 경쟁 사이트를 실측해 보니(2026-09-01) 복지맵닷컴은 정책 목록
 * 155건에 더해 **직접 쓴 글 23편**과 편집방침 페이지를 두고 애드센스를 달고
 * 있었다. 우리는 목록이 900건으로 훨씬 많은데 직접 쓴 글이 **한 편도 없었다.**
 *
 * 목록만 있는 사이트는 "공공데이터를 그대로 옮겨 놓은 곳"으로 읽힌다.
 * 데이터가 정확한 것과, 그 데이터를 읽고 얻은 것이 있느냐는 다른 문제다.
 *
 * ── 이 글들의 규칙 ──────────────────────────────────────────────
 * · 숫자는 전부 **수록 900건을 직접 집계한 값**이다. 어디서 본 수치를
 *   옮기지 않는다. 집계 시점(2026-09-01)을 글에 밝힌다.
 * · 제도 해석·자격 판정은 하지 않는다(CLAUDE.md 3절). 어디서 어떻게
 *   확인하는지까지가 우리 역할이다.
 * · 분량을 채우려고 아는 척하지 않는다. 짧아도 확인된 것만 쓴다.
 * ────────────────────────────────────────────────────────────────
 */
export type Guide = {
  slug: string;
  title: string;
  /** 목록 카드에 쓰는 한 줄 */
  summary: string;
  /** 마지막으로 내용을 손본 날 — 사이트맵 lastmod로 쓴다 */
  updated: string;
};

export const GUIDES: readonly Guide[] = [
  {
    slug: "apply",
    title: "복지 지원금, 어디서 어떻게 신청하나",
    summary:
      "복지로·정부24·주민센터 세 창구가 어떻게 다른지, 우리 데이터 505건을 집계해 어느 쪽이 실제로 쓰이는지 정리했습니다.",
    updated: "2026-09-01",
  },
  {
    slug: "documents",
    title: "복지 신청 서류, 무엇을 준비하나",
    summary:
      "사업이 달라도 반복해서 요구되는 공통 서식 네 가지와, 서류를 준비하는 순서를 정리했습니다.",
    updated: "2026-09-01",
  },
  {
    slug: "online",
    title: "집에서 온라인으로 신청할 수 있는 지원",
    summary:
      "주민센터에 가지 않고 신청 가능한 사업만 모았습니다. 수록 900건 중 54건입니다.",
    updated: "2026-09-01",
  },
  {
    slug: "terms",
    title: "복지 공고에서 자주 막히는 말들",
    summary:
      "소득인정액·차상위·바우처·융자처럼 공고문에 그냥 적혀 있어 넘어가기 쉬운 말을 풀었습니다.",
    updated: "2026-09-01",
  },
] as const;

const BY_SLUG = new Map(GUIDES.map((g) => [g.slug, g]));
export const guideBySlug = (s: string) => BY_SLUG.get(s);
