/**
 * 애드센스 코드 — **내용이 있는 페이지에만** 싣는다 (2026-09-13).
 *
 * ── 전: layout.tsx `<head>`에 박아 모든 페이지 ────────────────────
 * 09-05 소유권 확인 때 layout에 넣었다. 그 결과 검색·질문·조건 찾기의 빈 화면,
 * 저장 목록, 404, 켜지 않은 로그인 주소(본문 0자)와 **스스로 noindex한 얇은
 * 상세 195쪽**에도 광고 코드가 실렸다. 애드센스 정책의 「게시자 콘텐츠가 없는
 * 화면에 Google 광고」에 정면으로 걸리는 자리이고, 09-13 「가치가 별로 없는
 * 콘텐츠」 거절과 같은 방향의 신호다.
 *
 * ── 후: 페이지가 이 부품을 그릴 때만 ───────────────────────────
 * React 19는 `async`가 붙은 `<script src>`를 어디서 그려도 `<head>`로 올리고
 * 같은 주소는 한 번만 넣는다. 그래서 layout을 조건부로 만들 필요 없이, 싣고 싶은
 * 페이지(색인 대상)가 이 부품을 그리면 된다. 크롤러가 받는 HTML 원문의
 * `<head>`에 그대로 나간다(빌드 HTML로 확인).
 *
 * 넣는 곳: 홈 · 안내 글(모든 글 끝의 `GuideNav`) · 색인되는 상세·허브 · 소개·
 * 검수 기준·FAQ·출처·자가진단. **안 넣는 곳**: noindex 페이지 전부, 404·오류,
 * 계정 화면, 약관·방침·문의(정형 문서).
 * 새 페이지를 만들면 "색인시킬 페이지인가"로 같이 정한다.
 *
 * ── 09-13 재점검에서 더 뺀 곳 ──────────────────────────────────
 * 사이트 전체 1,017쪽을 다시 훑어 본문 글자 수를 셌다. 색인 대상이어도
 * **길잡이 화면**은 광고 자리가 아니다.
 * · 축 목록 `/region`·`/target`·`/life`·`/benefit`·`/theme` — 칸 몇 개뿐(본문 206~517자).
 * · 항목이 `AD_MIN_ITEMS`건 미만인 허브 — 소득 기준 허브 여덟 개와 `/theme/energy`(6건)가
 *   걸린다. 본문 650~960자로, 상세 페이지 가장 짧은 것(874자)보다 짧다.
 * 색인은 그대로 둔다 — 검색으로 찾아올 값어치와 광고를 붙일 값어치는 다른 문제다.
 */
export const AD_MIN_ITEMS = 10;
export default function AdSenseScript() {
  return (
    <script
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-3333691556845206"
      crossOrigin="anonymous"
    />
  );
}
